OpenAI Codex v0.153.4
--------
workdir: G:\Claude Projects\md reader
model: gpt-6-astra
provider: openai
approval: never
sandbox: read-only
reasoning effort: none
reasoning summaries: none
session id: 01a074b9-c6b9-7761-bea4-29c00ffc5219
--------
user
Ты ревьюишь порт Plain на macOS — личного Markdown reader/editor (Tauri 2.11 + React + CodeMirror 6 + Rust). Ничего не правь — только читай и пиши отзыв.

Контекст: норма — `ТЗ-Plain.md` v2.4, **§13a (macOS)**, плюс §8 (сохранность — не экономим), §12 (клавиши по `KeyboardEvent.code`). У разработчика нет Mac: код проверен только `cargo test`/`vitest` на `macos-latest` в GitHub Actions (зелёный, universal dmg собрался), живого запуска на Mac не было. Диф порта: `git diff 54129c7..f32704f` (коммиты «macOS port…», «tests pinned…», «gate … cfg(windows)»).

Что смотреть:
1. **Rust** (`src-tauri/src/fs.rs`, `lib.rs`, `assoc.rs`, `history.rs`, `Cargo.toml`, `tauri.conf.json`): unix-ветка атомарной записи `commit_by_rename` (temp в той же папке → `sync_all` → перенос режима файла → `rename`): что с владельцем/ACL/xattr/квaрантином, hard links, файлом на другой FS, `rename` поверх каталога; `fsync` каталога после rename (нужен ли для гарантии §8 на APFS?); `plain_path`/`canonical` на unix; `read_only` по `permissions().readonly()` на macOS (это только owner-write бит — файл может быть недоступен по ACL); `RunEvent::Opened { urls }` — обработка `file://` с percent-encoding и кириллицей, папки vs файлы, что при старте с уже открытым окном (single-instance на macOS — плагин тоже работает?), очередь pending paths до подписки фронта; `bundle.fileAssociations` — корректность полей для Info.plist (`ext`, `name`, `role`, `mimeType`?); `minimumSystemVersion 13.0` vs CSS Custom Highlight API (Safari 17.2 = macOS 14) — согласовано ли с фолбэком; `beside_exe` None на unix; заглушки `assoc` под `cfg(not(windows))` — сигнатуры совпадают с Windows-версией для `generate_handler!`?
2. **Фронтенд** (`src/app/platform.ts`, `chords.ts`, `keys.ts`, `registry.ts` (`macOverrides`, `applyMacChords`), `setup.ts` фильтр занятых чордов с `Mod`, `menu.ts`/`Shortcuts.tsx`/`QuickSearch.tsx` через `chordText`, `sync.ts`/`StatusBar.tsx` через `idle()`, `find.ts` через `hasHighlights()`, `settings.ts` `defaultEol()`): `isMac()` детект в WKWebView (`navigator.platform` = "MacIntel" даже на Apple Silicon; `userAgentData` в Safari отсутствует) — надёжен ли; `Ctrl`→⌘: где ещё в коде проверяется `event.ctrlKey` напрямую (ReadView `Ctrl+клик` открыть в фоне, FileTree, QuickSearch `ctrl ↵`, toolbar, images paste `Ctrl+V` — grep `ctrlKey`) и не пропущены ли места; конфликты ⌘-сочетаний с системным меню Tauri на macOS (⌘Q/⌘W/⌘H/⌘M/⌘, — что перехватит native menu раньше webview; `Ctrl+W` закрыть документ vs ⌘W закрыть окно!); `Ctrl+Tab`; `Alt+↑/↓` (⌥↑/↓) и `Alt+Shift+↑/↓`; `Ctrl+Enter` (⌘↩); `Ctrl+/` (⌘/ на mac — ок?); `F11`→⌃⌘F; `Ctrl+Shift+9`, `Ctrl+=/-/0` zoom — WKWebView не имеет встроенного зума как WebView2 (в ТЗ зум «встроенный WebView2») — на mac наш `setZoom` есть в реестре?; `contextmenu` в редакторе — на mac native menu WKWebView даёт орфографию? (spellcheck attribute в WKWebView); DnD файлов на окно — Tauri drag-drop на macOS требует `dragDropEnabled` — включён; `window.print()` в WKWebView; `CRLF`→`lf` дефолт только для новых файлов — не ломает ли существующие тесты/сессии.
3. **CI** (`.github/workflows/macos.yml`): `tagName` пустой на dispatch — tauri-action действительно не создаёт релиз при пустом `tagName`?; `APPLE_SIGNING_IDENTITY: "-"` — ad-hoc подпись universal; `upload-artifact` путь; кэш; безопасность (permissions `contents: write` только на теге?).
4. **README** раздел macOS — точность инструкций (Gatekeeper на macOS 15: правый клик → Open больше не работает для неподписанных без нотаризации? — проверь актуальные правила Sequoia: нужен `xattr -d com.apple.quarantine` или System Settings → Privacy & Security → Open Anyway).

Формат: находки файл:строка, суть, почему важно, предложение; «критично / стоит поправить / вкусовщина». Без пересказа и похвал. По-русски, термины по-английски.

codex
Прочитаю диф и указанные разделы ТЗ, затем проверю спорные особенности macOS/Tauri по первичным источникам. Файлы менять не буду.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "git diff --stat 54129c7..f32704f; git status --short; rg --files -g AGENTS.md -g '*Plain*' -g '*platform*' -g '*chords*' -g '*keys*' -g '*registry*' -g '*setup*' -g '*macos*'" in G:\Claude Projects\md reader
 succeeded in 616ms:
warning: Not a git repository. Use --no-index to compare two paths outside a working tree
usage: git diff --no-index [<options>] <path> <path>

Diff output format options
    -p, --patch           generate patch
    -s, --no-patch        suppress diff output
    -u                    generate patch
    -U, --unified[=<n>]   generate diffs with <n> lines context
    -W, --function-context
                          generate diffs with <n> lines context
    --raw                 generate the diff in raw format
    --patch-with-raw      synonym for '-p --raw'
    --patch-with-stat     synonym for '-p --stat'
    --numstat             machine friendly --stat
    --shortstat           output only the last line of --stat
    -X, --dirstat[=<param1,param2>...]
                          output the distribution of relative amount of changes for each sub-directory
    --cumulative          synonym for --dirstat=cumulative
    --dirstat-by-file[=<param1,param2>...]
                          synonym for --dirstat=files,param1,param2...
    --check               warn if changes introduce conflict markers or whitespace errors
    --summary             condensed summary such as creations, renames and mode changes
    --name-only           show only names of changed files
    --name-status         show only names and status of changed files
    --stat[=<width>[,<name-width>[,<count>]]]
                          generate diffstat
    --stat-width <width>  generate diffstat with a given width
    --stat-name-width <width>
                          generate diffstat with a given name width
    --stat-graph-width <width>
                          generate diffstat with a given graph width
    --stat-count <count>  generate diffstat with limited lines
    --compact-summary     generate compact summary in diffstat
    --binary              output a binary diff that can be applied
    --full-index          show full pre- and post-image object names on the "index" lines
    --color[=<when>]      show colored diff
    --ws-error-highlight <kind>
                          highlight whitespace errors in the 'context', 'old' or 'new' lines in the diff
    -z                    do not munge pathnames and use NULs as output field terminators in --raw or --numstat
    --abbrev[=<n>]        use <n> digits to display object names
    --src-prefix <prefix>
                          show the given source prefix instead of "a/"
    --dst-prefix <prefix>
                          show the given destination prefix instead of "b/"
    --line-prefix <prefix>
                          prepend an additional prefix to every line of output
    --no-prefix           do not show any source or destination prefix
    --default-prefix      use default prefixes a/ and b/
    --inter-hunk-context <n>
                          show context between diff hunks up to the specified number of lines
    --output-indicator-new <char>
                          specify the character to indicate a new line instead of '+'
    --output-indicator-old <char>
                          specify the character to indicate an old line instead of '-'
    --output-indicator-context <char>
                          specify the character to indicate a context instead of ' '

Diff rename options
    -B, --break-rewrites[=<n>[/<m>]]
                          break complete rewrite changes into pairs of delete and create
    -M, --find-renames[=<n>]
                          detect renames
    -D, --irreversible-delete
                          omit the preimage for deletes
    -C, --find-copies[=<n>]
                          detect copies
    --find-copies-harder  use unmodified files as source to find copies
    --no-renames          disable rename detection
    --rename-empty        use empty blobs as rename source
    --follow              continue listing the history of a file beyond renames
    -l <n>                prevent rename/copy detection if the number of rename/copy targets exceeds given limit

Diff algorithm options
    --minimal             produce the smallest possible diff
    -w, --ignore-all-space
                          ignore whitespace when comparing lines
    -b, --ignore-space-change
                          ignore changes in amount of whitespace
    --ignore-space-at-eol
                          ignore changes in whitespace at EOL
    --ignore-cr-at-eol    ignore carrier-return at the end of line
    --ignore-blank-lines  ignore changes whose lines are all blank
    -I, --ignore-matching-lines <regex>
                          ignore changes whose all lines match <regex>
    --indent-heuristic    heuristic to shift diff hunk boundaries for easy reading
    --patience            generate diff using the "patience diff" algorithm
    --histogram           generate diff using the "histogram diff" algorithm
    --diff-algorithm <algorithm>
                          choose a diff algorithm
    --anchored <text>     generate diff using the "anchored diff" algorithm
    --word-diff[=<mode>]  show word diff, using <mode> to delimit changed words
    --word-diff-regex <regex>
                          use <regex> to decide what a word is
    --color-words[=<regex>]
                          equivalent to --word-diff=color --word-diff-regex=<regex>
    --color-moved[=<mode>]
                          moved lines of code are colored differently
    --color-moved-ws <mode>
                          how white spaces are ignored in --color-moved

Other diff options
    --relative[=<prefix>]
                          when run from subdir, exclude changes outside and show relative paths
    -a, --text            treat all files as text
    -R                    swap two inputs, reverse the diff
    --exit-code           exit with 1 if there were differences, 0 otherwise
    --quiet               disable all output of the program
    --ext-diff            allow an external diff helper to be executed
    --textconv            run external text conversion filters when comparing binary files
    --ignore-submodules[=<when>]
                          ignore changes to submodules in the diff generation
    --submodule[=<format>]
                          specify how differences in submodules are shown
    --ita-invisible-in-index
                          hide 'git add -N' entries from the index
    --ita-visible-in-index
                          treat 'git add -N' entries as real in the index
    -S <string>           look for differences that change the number of occurrences of the specified string
    -G <regex>            look for differences that change the number of occurrences of the specified regex
    --pickaxe-all         show all changes in the changeset with -S or -G
    --pickaxe-regex       treat <string> in -S as extended POSIX regular expression
    -O <file>             control the order in which files appear in the output
    --rotate-to <path>    show the change in the specified path first
    --skip-to <path>      skip the output to the specified path
    --find-object <object-id>
                          look for differences that change the number of occurrences of the specified object
    --diff-filter [(A|C|D|M|R|T|U|X|B)...[*]]
                          select files by diff type
    --output <file>       output to a specific file

fatal: detected dubious ownership in repository at 'G:/Claude Projects/md reader'
'G:/Claude Projects/md reader' is owned by:
	'S-1-5-21-2269766826-3758770725-3789985863-1002'
but the current user is:
	'S-1-5-21-2269766826-3758770725-3789985863-1005'
To add an exception for this directory, call:

	git config --global --add safe.directory 'G:/Claude Projects/md reader'
ТЗ-Plain.md
vitest.setup.ts
archive\ТЗ-Plain-v1.2-full.md
src\editor\setup.ts
src\app\registry.ts
src\app\platform.ts
src\app\keys.ts
src\app\chords.ts
src\app\chords.test.ts

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "git -c safe.directory='G:/Claude Projects/md reader' diff 54129c7..f32704f -- src-tauri src .github README.md" in G:\Claude Projects\md reader
 succeeded in 40ms:
diff --git a/.github/workflows/macos.yml b/.github/workflows/macos.yml
new file mode 100644
index 0000000..91f32d9
--- /dev/null
+++ b/.github/workflows/macos.yml
@@ -0,0 +1,65 @@
+# The macOS build, which is also the only place it is built at all: there is
+# no Mac on the development machine (spec §13a). Runs the tests first, then
+# bundles a .dmg. On a v* tag the .dmg is attached to that release; on a manual
+# run it is uploaded as a workflow artifact so the build can be checked without
+# cutting a release.
+name: macOS
+
+on:
+  push:
+    tags: ["v*"]
+  workflow_dispatch:
+
+jobs:
+  macos:
+    runs-on: macos-latest
+    permissions:
+      contents: write
+
+    steps:
+      - uses: actions/checkout@v4
+
+      - uses: actions/setup-node@v4
+        with:
+          node-version: 22
+          cache: npm
+
+      - uses: dtolnay/rust-toolchain@stable
+        with:
+          targets: aarch64-apple-darwin,x86_64-apple-darwin
+
+      - uses: Swatinem/rust-cache@v2
+        with:
+          workspaces: src-tauri
+
+      - run: npm ci
+
+      # The suites the development machine cannot vouch for on this platform.
+      - run: npx vitest run
+
+      - run: cargo test
+        working-directory: src-tauri
+
+      - uses: tauri-apps/tauri-action@v0
+        env:
+          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
+          # Ad-hoc: unsigned and un-notarised on purpose (spec §13a). Without
+          # an identity the universal binary would not be signed at all and
+          # Gatekeeper would refuse it outright rather than ask.
+          APPLE_SIGNING_IDENTITY: "-"
+        with:
+          # Empty on a manual run: tauri-action then only builds.
+          tagName: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}
+          releaseName: ${{ startsWith(github.ref, 'refs/tags/') && format('Plain {0}', github.ref_name) || '' }}
+          releaseDraft: false
+          prerelease: false
+          # One .dmg for both architectures; the Windows zip is attached to
+          # the same release by hand.
+          args: --target universal-apple-darwin --bundles dmg
+
+      - uses: actions/upload-artifact@v4
+        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
+        with:
+          name: Plain-macos-dmg
+          path: src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg
+          if-no-files-found: error
diff --git a/README.md b/README.md
index 2b2855a..96537a2 100644
--- a/README.md
+++ b/README.md
@@ -49,10 +49,20 @@ In `%APPDATA%\Plain` (that is `C:\Users\<you>\AppData\Roaming\Plain`):
 | `state.json` | open files, reading positions, the last folder |
 | `history\` | snapshots of what you saved, kept for thirty days |
 
-Put a folder called `data` next to `plain.exe` and all four live in it instead — that is portable mode.
+Put a folder called `data` next to `plain.exe` and all four live in it instead — that is portable mode. On macOS the same four live in `~/Library/Application Support/Plain`; the app is a bundle, so there is no portable mode there.
 
 Plain keeps no indexes, caches, or hidden service files in your folders. The only things it writes there are the documents you save and, if you paste an image, the `assets/` folder next to the document.
 
+## macOS
+
+Download the `.dmg` from the releases page, open it, and drag Plain into Applications.
+
+The app is not signed or notarised, so the first launch needs one extra step: right-click it in Applications and choose **Open**, then confirm. Once done, it opens normally from then on. If macOS refuses outright, `xattr -cr /Applications/Plain.app` in Terminal clears the quarantine flag and the right-click trick works again.
+
+Shortcuts use `⌘` where this page says `Ctrl`: `⌘S` saves, `⌘O` opens, `⌘/` switches between reading and editing, `⌘,` opens settings, `⌘⇧P` is the command palette. A few differ where macOS already owns the combination — `⌘[` and `⌘]` go back and forward, `⌃⌘F` is fullscreen. The full list is in the app under `help → shortcuts`.
+
+Double-clicking a `.md` file in Finder opens it in Plain once you have chosen Plain in **Get Info → Open with**; the bundle offers itself for `.md` and `.markdown` from the first launch.
+
 ## How it treats your files
 
 - Bytes you did not touch do not change: list markers, indentation, trailing spaces, the byte order mark, the final newline.
@@ -73,4 +83,6 @@ npm run pack          # release build, produces dist-win\Plain-0.2.0-win-x64.zip
 
 Checks: `npm run build`, `npx vitest run`, and `cargo test` inside `src-tauri`. The manual checklist used before a release is in `CHECKLIST.md`.
 
+The macOS build happens on CI only (`.github/workflows/macos.yml`, on a `v*` tag or by hand): it runs the same three checks on `macos-latest` and attaches a universal `.dmg` to the release. Cross-checking it from Windows is not possible — one of Tauri's macOS dependencies compiles a C shim and needs an Apple toolchain.
+
 Stack: Tauri 2, React, CodeMirror 6, unified (remark and rehype), Shiki, KaTeX, Mermaid. Rust handles the file system, folder watching, and search.
diff --git a/src-tauri/Cargo.lock b/src-tauri/Cargo.lock
index c9170ab..0069582 100644
--- a/src-tauri/Cargo.lock
+++ b/src-tauri/Cargo.lock
@@ -2915,7 +2915,7 @@ checksum = "f6b464fbc74e149a392436b17d523f769e057cb6877f6a5c4618bc6f11800548"
 
 [[package]]
 name = "plain"
-version = "0.1.0"
+version = "0.2.0"
 dependencies = [
  "blake3",
  "chardetng",
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index aedd17f..af8ef5b 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -33,6 +33,11 @@ blake3 = "1.8.7"
 ignore = "0.4.23"
 regex = "1.11.1"
 rayon = "1.11.0"
+
+# ReplaceFileW and the HKCU file association are Windows and nothing else;
+# on macOS the atomic write is `rename` and the association is in the bundle
+# (spec §13a).
+[target.'cfg(windows)'.dependencies]
 windows = { version = "0.62.2", features = ["Win32_Storage_FileSystem", "Win32_Foundation", "Win32_UI_Shell"] }
 winreg = "0.56.0"
 
diff --git a/src-tauri/src/assoc.rs b/src-tauri/src/assoc.rs
index f7368b0..61805ac 100644
--- a/src-tauri/src/assoc.rs
+++ b/src-tauri/src/assoc.rs
@@ -1,18 +1,32 @@
 // Per-user file association for `.md` and `.markdown` (spec §13). Everything
 // is written under HKCU: the app offers itself in "open with" and appears in
 // Settings → Default apps, and it never makes itself the default.
+//
+// Windows only. macOS takes its associations from `bundle.fileAssociations`
+// in tauri.conf.json, which the bundler turns into `CFBundleDocumentTypes` —
+// there is nothing to register while the app runs (spec §13a). The commands
+// still exist there so the front end has the same four to call.
 
+#[cfg(windows)]
 use std::io;
 
+#[cfg(windows)]
 use winreg::enums::{HKEY_CURRENT_USER, REG_NONE};
+#[cfg(windows)]
 use winreg::{RegKey, RegValue};
 
+#[cfg(windows)]
 const PROGID: &str = "Plain.md";
+#[cfg(windows)]
 const EXTENSIONS: [&str; 2] = [".md", ".markdown"];
+#[cfg(windows)]
 const CLASSES: &str = r"Software\Classes";
+#[cfg(windows)]
 const CAPABILITIES: &str = r"Software\Plain\Capabilities";
+#[cfg(windows)]
 const REGISTERED: &str = r"Software\RegisteredApplications";
 
+#[cfg(windows)]
 fn exe_path() -> Result<String, String> {
     std::env::current_exe()
         .map(|path| path.to_string_lossy().into_owned())
@@ -20,10 +34,12 @@ fn exe_path() -> Result<String, String> {
 }
 
 /// What Explorer runs for a double-clicked file.
+#[cfg(windows)]
 fn open_command(exe: &str) -> String {
     format!("\"{exe}\" \"%1\"")
 }
 
+#[cfg(windows)]
 fn write(exe: &str) -> io::Result<()> {
     let hkcu = RegKey::predef(HKEY_CURRENT_USER);
 
@@ -64,6 +80,7 @@ fn write(exe: &str) -> io::Result<()> {
 }
 
 /// Undoing a registration that is already gone is not an error.
+#[cfg(windows)]
 fn ignore_missing(result: io::Result<()>) -> io::Result<()> {
     match result {
         Err(error) if error.kind() == io::ErrorKind::NotFound => Ok(()),
@@ -71,6 +88,7 @@ fn ignore_missing(result: io::Result<()>) -> io::Result<()> {
     }
 }
 
+#[cfg(windows)]
 fn erase() -> io::Result<()> {
     let hkcu = RegKey::predef(HKEY_CURRENT_USER);
 
@@ -96,11 +114,13 @@ fn erase() -> io::Result<()> {
 
 /// Explorer caches associations; without this the change shows up on the
 /// next logon.
+#[cfg(windows)]
 fn notify_shell() {
     use windows::Win32::UI::Shell::{SHChangeNotify, SHCNE_ASSOCCHANGED, SHCNF_IDLIST};
     unsafe { SHChangeNotify(SHCNE_ASSOCCHANGED, SHCNF_IDLIST, None, None) };
 }
 
+#[cfg(windows)]
 #[tauri::command]
 pub fn register_file_association() -> Result<(), String> {
     write(&exe_path()?).map_err(|error| error.to_string())?;
@@ -108,6 +128,7 @@ pub fn register_file_association() -> Result<(), String> {
     Ok(())
 }
 
+#[cfg(windows)]
 #[tauri::command]
 pub fn unregister_file_association() -> Result<(), String> {
     erase().map_err(|error| error.to_string())?;
@@ -116,6 +137,7 @@ pub fn unregister_file_association() -> Result<(), String> {
 }
 
 /// What `about` reports. True as long as `.md` still offers our ProgID.
+#[cfg(windows)]
 #[tauri::command]
 pub fn file_association_registered() -> bool {
     RegKey::predef(HKEY_CURRENT_USER)
@@ -124,17 +146,46 @@ pub fn file_association_registered() -> bool {
         .is_ok()
 }
 
+/* ------------------------------------------------- everywhere else (§13a) */
+
+/// The bundle already claims `.md`; there is nothing a running app can add,
+/// and `about` has no button to offer. The commands stay so the front end
+/// does not need to know which platform it is on.
+#[cfg(not(windows))]
+#[tauri::command]
+pub fn register_file_association() -> Result<(), String> {
+    Ok(())
+}
+
+#[cfg(not(windows))]
+#[tauri::command]
+pub fn unregister_file_association() -> Result<(), String> {
+    Ok(())
+}
+
+/// Launch Services owns this on macOS, and it is decided by the bundle
+/// rather than by anything we could read back cheaply.
+#[cfg(not(windows))]
+#[tauri::command]
+pub fn file_association_registered() -> bool {
+    true
+}
+
 /// Runs at every release start, so the recorded exe path follows the folder
 /// when it is moved. Cheap, idempotent, and silent when it fails — a broken
 /// association is not a reason to refuse to open a file.
-#[cfg(not(debug_assertions))]
+#[cfg(all(windows, not(debug_assertions)))]
 pub fn register_quietly() {
     if let Err(error) = register_file_association() {
         eprintln!("plain: could not register the .md association: {error}");
     }
 }
 
-#[cfg(test)]
+/// Nothing to do: the association travels with the bundle (spec §13a).
+#[cfg(all(not(windows), not(debug_assertions)))]
+pub fn register_quietly() {}
+
+#[cfg(all(test, windows))]
 mod tests {
     use super::open_command;
 
diff --git a/src-tauri/src/fs.rs b/src-tauri/src/fs.rs
index fbeae5d..f6e5758 100644
--- a/src-tauri/src/fs.rs
+++ b/src-tauri/src/fs.rs
@@ -5,6 +5,7 @@
 
 use std::fs;
 use std::io::Write;
+#[cfg(windows)]
 use std::os::windows::ffi::OsStrExt;
 use std::path::{Path, PathBuf};
 use std::sync::Mutex;
@@ -13,7 +14,9 @@ use std::time::UNIX_EPOCH;
 use chardetng::{EncodingDetector, Iso2022JpDetection, Utf8Detection};
 use encoding_rs::{Encoding, UTF_16BE, UTF_16LE, UTF_8};
 use serde::{Deserialize, Serialize};
+#[cfg(windows)]
 use windows::core::PCWSTR;
+#[cfg(windows)]
 use windows::Win32::Storage::FileSystem::{ReplaceFileW, REPLACEFILE_IGNORE_MERGE_ERRORS};
 
 /// The three cases the front end tells apart: `conflict` stops the save and
@@ -179,9 +182,18 @@ fn line_endings(text: &str) -> (&'static str, &'static str) {
 
 /* ------------------------------------------------------ one path per file */
 
+/// Elsewhere `canonicalize` already hands back an ordinary path, and a
+/// backslash is a legal character in a POSIX file name — so nothing is
+/// stripped there (spec §13a).
+#[cfg(not(windows))]
+fn plain_path(path: PathBuf) -> PathBuf {
+    path
+}
+
 /// `fs::canonicalize` hands back a verbatim path (`\\?\C:\…`) that no user
 /// ever types and half of Windows will not take. This puts it back into the
 /// ordinary form without giving up what canonicalizing found.
+#[cfg(windows)]
 fn plain_path(path: PathBuf) -> PathBuf {
     let text = path.as_os_str().to_string_lossy();
     if let Some(rest) = text.strip_prefix(r"\\?\UNC\") {
@@ -323,6 +335,7 @@ fn apply_eol(text: &str, eol: &str) -> String {
     }
 }
 
+#[cfg(windows)]
 fn wide(path: &Path) -> Vec<u16> {
     path.as_os_str().encode_wide().chain(Some(0)).collect()
 }
@@ -341,6 +354,7 @@ pub fn stage(dir: &Path, bytes: &[u8]) -> FsResult<PathBuf> {
     Ok(path)
 }
 
+#[cfg(windows)]
 /// `ReplaceFileW` can fail after it has already moved the original aside
 /// (ERROR_UNABLE_TO_MOVE_REPLACEMENT). If the target is gone, the staged file
 /// is the only copy of the text left and it goes back under the target name;
@@ -358,11 +372,35 @@ fn recover_replacement(target: &Path, temp: &Path) -> std::io::Result<()> {
     Ok(())
 }
 
+/// POSIX `rename` is atomic and replaces whatever is there, so one call
+/// covers both cases. The mode has to be carried over by hand: a temp file is
+/// created 0600 and the file being replaced may be more permissive (§13a).
+///
+/// Compiled on every platform, not only the one that uses it, so a Windows
+/// build still type-checks it and a Windows test can still run it — there is
+/// no Mac here to find out on.
+fn commit_by_rename(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
+    if existed {
+        if let Ok(meta) = fs::metadata(target) {
+            // Best effort: a read-only target would otherwise hand the copy
+            // permissions the original never had.
+            let _ = fs::set_permissions(temp, meta.permissions());
+        }
+    }
+    fs::rename(temp, target).map_err(FsError::from)
+}
+
+#[cfg(not(windows))]
+pub fn commit(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
+    commit_by_rename(target, temp, existed)
+}
+
 /// ReplaceFileW keeps the creation time and the attributes of the original;
 /// a file that is not there yet is just renamed into place (spec §8).
+#[cfg(windows)]
 pub fn commit(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
     if !existed {
-        return fs::rename(temp, target).map_err(FsError::from);
+        return commit_by_rename(target, temp, existed);
     }
     let replaced = wide(target);
     let replacement = wide(temp);
@@ -840,6 +878,7 @@ mod tests {
 
     /// The dangerous half of a failed ReplaceFileW: the original is already
     /// gone and the staged file holds the only copy of the text.
+    #[cfg(windows)]
     #[test]
     fn a_failed_replace_puts_the_staged_text_back() {
         let dir = tempfile::tempdir().unwrap();
@@ -852,6 +891,7 @@ mod tests {
         assert!(!temp.exists());
     }
 
+    #[cfg(windows)]
     #[test]
     fn a_failed_replace_that_left_the_original_drops_the_staged_file() {
         let dir = tempfile::tempdir().unwrap();
@@ -916,6 +956,7 @@ mod tests {
 
     /// The 8.3 alias Windows keeps for a long folder name, or `None` when the
     /// volume has short names turned off.
+    #[cfg(windows)]
     fn short_name(path: &Path) -> Option<PathBuf> {
         use windows::Win32::Storage::FileSystem::GetShortPathNameW;
         let wide_path = wide(path);
@@ -932,7 +973,9 @@ mod tests {
     }
 
     /// One file, one identity: the short name, the wrong case and the wrong
-    /// separator all have to land on the same string (spec §8).
+    /// separator all have to land on the same string (spec §8). 8.3 aliases
+    /// and case-insensitive paths are a Windows story.
+    #[cfg(windows)]
     #[test]
     fn a_canonical_path_is_the_one_the_disk_uses() {
         let dir = tempfile::tempdir().unwrap();
@@ -961,6 +1004,11 @@ mod tests {
 
     #[test]
     fn a_file_that_does_not_exist_yet_is_resolved_through_its_folder() {
+        #[cfg(not(windows))]
+        fn short_name(_path: &Path) -> Option<PathBuf> {
+            None
+        }
+
         let dir = tempfile::tempdir().unwrap();
         let folder = dir.path().join("Another Long Folder");
         fs::create_dir(&folder).unwrap();
@@ -973,6 +1021,32 @@ mod tests {
         }
     }
 
+    /// The macOS write path, run here because there is no Mac to run it on.
+    /// `rename` replaces the target on Windows too, so the behaviour this
+    /// asserts is the same behaviour macOS will get (spec §13a).
+    #[test]
+    fn a_rename_commit_replaces_the_target_and_keeps_its_mode() {
+        let dir = tempfile::tempdir().unwrap();
+        let target = dir.path().join("note.md");
+
+        // Nothing there yet: the staged file simply becomes the file.
+        let temp = stage(dir.path(), b"first
+").unwrap();
+        commit_by_rename(&target, &temp, false).unwrap();
+        assert_eq!(fs::read(&target).unwrap(), b"first
+");
+        assert!(!temp.exists());
+
+        // Something there: it is replaced, and only it is left behind.
+        let temp = stage(dir.path(), b"second
+").unwrap();
+        commit_by_rename(&target, &temp, true).unwrap();
+        assert_eq!(fs::read(&target).unwrap(), b"second
+");
+        assert_eq!(fs::read_dir(dir.path()).unwrap().count(), 1);
+    }
+
+    #[cfg(windows)]
     #[test]
     fn a_verbatim_prefix_is_taken_back_off() {
         assert_eq!(
diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rs
index 518666c..44923fc 100644
--- a/src-tauri/src/lib.rs
+++ b/src-tauri/src/lib.rs
@@ -58,10 +58,18 @@ pub fn pick_data_dir(beside_exe: Option<&Path>, app_data: &Path) -> PathBuf {
     }
 }
 
+/// Windows only: on macOS the app is a bundle and a folder next to it is
+/// not something a user can rely on (spec §13a).
+#[cfg(windows)]
 fn beside_exe() -> Option<PathBuf> {
     Some(std::env::current_exe().ok()?.parent()?.join("data"))
 }
 
+#[cfg(not(windows))]
+fn beside_exe() -> Option<PathBuf> {
+    None
+}
+
 /// The one folder every file of ours lives in. Rust and the front end have to
 /// agree on it, so the front end asks rather than working it out again.
 pub fn data_dir<R: Runtime>(app: &tauri::AppHandle<R>) -> PathBuf {
@@ -257,8 +265,33 @@ pub fn run() {
             history::snapshot_text,
             history::delete_snapshot
         ])
-        .run(tauri::generate_context!())
-        .expect("error while running Plain");
+        .build(tauri::generate_context!())
+        .expect("error while building Plain")
+        .run(|_app, _event| {
+            // Finder does not pass a path in argv: it sends the app an Apple
+            // Event, which Tauri turns into this (spec §13a). Same queue the
+            // command line uses, same nudge to the front end.
+            #[cfg(target_os = "macos")]
+            if let tauri::RunEvent::Opened { urls } = _event {
+                let paths: Vec<String> = urls
+                    .iter()
+                    .filter_map(|url| url.to_file_path().ok())
+                    .filter(|path| path.is_file())
+                    .map(|path| path.to_string_lossy().into_owned())
+                    .collect();
+                let folders: Vec<String> = urls
+                    .iter()
+                    .filter_map(|url| url.to_file_path().ok())
+                    .filter(|path| path.is_dir())
+                    .map(|path| path.to_string_lossy().into_owned())
+                    .collect();
+                if !paths.is_empty() || !folders.is_empty() {
+                    queue_paths(_app, paths);
+                    queue_folders(_app, folders);
+                    let _ = _app.emit("open-path", ());
+                }
+            }
+        });
 }
 
 #[cfg(test)]
@@ -314,6 +347,7 @@ mod tests {
         );
     }
 
+    #[cfg(windows)] // C:\-shaped paths
     #[test]
     fn resolves_relatives_against_the_given_cwd() {
         let dir = tempfile::tempdir().unwrap();
diff --git a/src-tauri/src/watch.rs b/src-tauri/src/watch.rs
index 7ef3adb..c853069 100644
--- a/src-tauri/src/watch.rs
+++ b/src-tauri/src/watch.rs
@@ -133,6 +133,7 @@ mod tests {
     use super::{ignored, under};
     use std::path::Path;
 
+    #[cfg(windows)] // C:\-shaped paths
     #[test]
     fn a_neighbour_folder_is_not_inside_the_library() {
         let root = Path::new(r"C:\notes");
@@ -144,6 +145,7 @@ mod tests {
         assert!(!under(Path::new(r"D:\notes"), root));
     }
 
+    #[cfg(windows)] // C:\-shaped paths
     #[test]
     fn skips_the_folders_nobody_wants_events_from() {
         assert!(ignored(Path::new(r"C:\notes\.git\index")));
diff --git a/src-tauri/tauri.conf.json b/src-tauri/tauri.conf.json
index 7cda036..c05f8f4 100644
--- a/src-tauri/tauri.conf.json
+++ b/src-tauri/tauri.conf.json
@@ -32,8 +32,19 @@
     }
   },
   "bundle": {
-    "active": false,
+    "active": true,
     "targets": [],
-    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.ico"]
+    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.ico", "icons/icon.png"],
+    "fileAssociations": [
+      {
+        "ext": ["md", "markdown"],
+        "name": "Markdown",
+        "description": "Markdown document",
+        "role": "Editor"
+      }
+    ],
+    "macOS": {
+      "minimumSystemVersion": "13.0"
+    }
   }
 }
diff --git a/src/app/chords.ts b/src/app/chords.ts
index ade4856..4cb8ec4 100644
--- a/src/app/chords.ts
+++ b/src/app/chords.ts
@@ -2,6 +2,8 @@
 // Russian layout (spec §12). This turns readable chords like "Ctrl+Shift+F"
 // into a physical-key description, and into a tinykeys pattern.
 
+import { isMac } from "./platform";
+
 export interface Chord {
   ctrl: boolean;
   shift: boolean;
@@ -67,6 +69,16 @@ export function toCode(token: string): string {
   throw new Error(`unknown key token: ${token}`);
 }
 
+/**
+ * Modifier vocabulary (spec §13a):
+ *
+ * - `Ctrl` — the platform's primary modifier: `Ctrl` on Windows, ⌘ on macOS.
+ *   Almost every chord in the registry is written with it.
+ * - `Cmd` / `Meta` / `Win` — literally the meta key, on both platforms.
+ * - `Control` — literally the control key, on both. Only macOS needs it, for
+ *   the handful of system chords that really are ⌃ (fullscreen).
+ * - `Alt` / `Option` — ⌥ on macOS.
+ */
 export function parseChord(chord: string): Chord {
   const parts = chord.split("+").map((p) => p.trim());
   const last = parts.pop();
@@ -82,6 +94,10 @@ export function parseChord(chord: string): Chord {
   for (const part of parts) {
     switch (part.toLowerCase()) {
       case "ctrl":
+      case "mod":
+        if (isMac()) out.meta = true;
+        else out.ctrl = true;
+        break;
       case "control":
         out.ctrl = true;
         break;
@@ -104,6 +120,56 @@ export function parseChord(chord: string): Chord {
   return out;
 }
 
+/* --------------------------------------------------------------- display */
+
+/** `KeyB` -> `B`, `Comma` -> `,`, `ArrowLeft` -> `←`, for the macOS glyphs. */
+const KEY_GLYPHS: Record<string, string> = {
+  ArrowLeft: "←",
+  ArrowRight: "→",
+  ArrowUp: "↑",
+  ArrowDown: "↓",
+  Enter: "↩",
+  Tab: "⇥",
+  Escape: "⎋",
+  Backspace: "⌫",
+  Delete: "⌦",
+  Space: "space",
+};
+
+function keyLabel(code: string): string {
+  const glyph = KEY_GLYPHS[code];
+  if (glyph) return glyph;
+  if (code.startsWith("Key")) return code.slice(3);
+  if (code.startsWith("Digit")) return code.slice(5);
+  if (/^F\d+$/.test(code)) return code;
+  for (const [punct, name] of Object.entries(PUNCT)) {
+    if (name === code) return punct;
+  }
+  return code;
+}
+
+/**
+ * How a chord is written in the menu, the palette and the shortcuts screen.
+ * Windows spells it out in the app's lowercase microcopy (`ctrl shift s`);
+ * macOS uses the glyphs in Apple's order, ⌃⌥⇧⌘, with nothing between them.
+ */
+export function chordText(chord: string): string {
+  if (!isMac()) {
+    return chord
+      .split("+")
+      .map((part) => part.trim().toLowerCase())
+      .join(" ");
+  }
+  const c = parseChord(chord);
+  return (
+    (c.ctrl ? "⌃" : "") +
+    (c.alt ? "⌥" : "") +
+    (c.shift ? "⇧" : "") +
+    (c.meta ? "⌘" : "") +
+    keyLabel(c.code)
+  );
+}
+
 /** Exact match, including "no other modifier is held". */
 export function matchesChord(chord: string | Chord, event: KeyLike): boolean {
   const c = typeof chord === "string" ? parseChord(chord) : chord;
diff --git a/src/app/mac.test.ts b/src/app/mac.test.ts
new file mode 100644
index 0000000..1456094
--- /dev/null
+++ b/src/app/mac.test.ts
@@ -0,0 +1,126 @@
+// macOS behaviour without a Mac (spec §13a): the platform is forced and the
+// same code paths are exercised. Everything here is a pure function.
+
+import { afterEach, describe, expect, it } from "vitest";
+import { chordText, matchesChord, parseChord } from "./chords";
+import { isMac, primaryModifierLabel, setMacForTests } from "./platform";
+import { applyMacChords, command, macOverrides } from "./registry";
+import { defaultEol } from "./settings";
+
+// Back to the suite-wide default, not to detection: the next test must not
+// depend on which machine is running it.
+afterEach(() => setMacForTests(false));
+
+function press(over: Partial<Record<string, unknown>> & { code: string }) {
+  return {
+    ctrlKey: false,
+    shiftKey: false,
+    altKey: false,
+    metaKey: false,
+    ...over,
+  } as { code: string; ctrlKey: boolean; shiftKey: boolean; altKey: boolean; metaKey: boolean };
+}
+
+describe("the platform switch", () => {
+  it("can be forced either way and handed back", () => {
+    setMacForTests(true);
+    expect(isMac()).toBe(true);
+    expect(primaryModifierLabel()).toBe("⌘");
+    setMacForTests(false);
+    expect(isMac()).toBe(false);
+    expect(primaryModifierLabel()).toBe("ctrl");
+  });
+});
+
+describe("chords on macOS", () => {
+  it("reads `Ctrl` as ⌘ and refuses the real control key", () => {
+    setMacForTests(true);
+    expect(matchesChord("Ctrl+B", press({ code: "KeyB", metaKey: true }))).toBe(true);
+    // ⌘B is not ⌃B: the modifiers have to match exactly (spec §12).
+    expect(matchesChord("Ctrl+B", press({ code: "KeyB", ctrlKey: true }))).toBe(false);
+    expect(matchesChord("Ctrl+B", press({ code: "KeyB", metaKey: true, ctrlKey: true }))).toBe(
+      false,
+    );
+  });
+
+  it("keeps `Ctrl` as the control key on Windows", () => {
+    setMacForTests(false);
+    expect(matchesChord("Ctrl+B", press({ code: "KeyB", ctrlKey: true }))).toBe(true);
+    expect(matchesChord("Ctrl+B", press({ code: "KeyB", metaKey: true }))).toBe(false);
+  });
+
+  it("has a token for the real control key, on both platforms", () => {
+    setMacForTests(true);
+    expect(matchesChord("Control+Cmd+F", press({ code: "KeyF", ctrlKey: true, metaKey: true }))).toBe(
+      true,
+    );
+    expect(parseChord("Control+F")).toMatchObject({ ctrl: true, meta: false });
+    setMacForTests(false);
+    expect(parseChord("Control+F")).toMatchObject({ ctrl: true, meta: false });
+  });
+
+  it("leaves Alt alone — it is ⌥ and matches as alt", () => {
+    setMacForTests(true);
+    expect(matchesChord("Ctrl+Alt+Left", press({ code: "ArrowLeft", metaKey: true, altKey: true })))
+      .toBe(true);
+  });
+});
+
+describe("how a chord is written", () => {
+  it("spells it out on Windows", () => {
+    setMacForTests(false);
+    expect(chordText("Ctrl+Shift+S")).toBe("ctrl shift s");
+    expect(chordText("F11")).toBe("f11");
+  });
+
+  it("uses Apple's glyphs, in Apple's order, with nothing between", () => {
+    setMacForTests(true);
+    expect(chordText("Ctrl+Shift+S")).toBe("⇧⌘S");
+    expect(chordText("Ctrl+Alt+Left")).toBe("⌥⌘←");
+    expect(chordText("Control+Cmd+F")).toBe("⌃⌘F");
+    expect(chordText("Ctrl+,")).toBe("⌘,");
+    expect(chordText("Ctrl+1")).toBe("⌘1");
+    expect(chordText("F1")).toBe("F1");
+  });
+});
+
+describe("defaults that follow the platform", () => {
+  it("writes LF on macOS and CRLF on Windows", () => {
+    setMacForTests(true);
+    expect(defaultEol()).toBe("lf");
+    setMacForTests(false);
+    expect(defaultEol()).toBe("crlf");
+  });
+});
+
+describe("macOverrides", () => {
+  it("moves history off the arrow keys macOS uses for words", () => {
+    expect(macOverrides["nav.back"]).toBe("Ctrl+Alt+Left");
+    expect(macOverrides["nav.forward"]).toBe("Ctrl+Alt+Right");
+    setMacForTests(true);
+    expect(chordText(macOverrides["nav.back"] as string)).toBe("⌥⌘←");
+  });
+
+  it("gives fullscreen the system chord, with the real control key", () => {
+    expect(macOverrides["view.fullscreen"]).toBe("Control+Cmd+F");
+    setMacForTests(true);
+    expect(chordText(macOverrides["view.fullscreen"] as string)).toBe("⌃⌘F");
+  });
+
+  it("rewrites exactly the commands in the table and no others", () => {
+    const list = [
+      { id: "nav.back", title: "back", chord: "Alt+Left", run: () => {} },
+      { id: "file.save", title: "save", chord: "Ctrl+S", run: () => {} },
+    ];
+    applyMacChords(list);
+    expect(list[0]?.chord).toBe("Ctrl+Alt+Left");
+    expect(list[1]?.chord).toBe("Ctrl+S");
+  });
+
+  it("leaves the registry on Windows chords when this is not a Mac", () => {
+    // The suite runs as Windows (vitest.setup.ts), so the load-time
+    // application must not have fired.
+    expect(command("nav.back")?.chord).toBe("Alt+Left");
+    expect(command("view.fullscreen")?.chord).toBe("F11");
+  });
+});
diff --git a/src/app/menu.ts b/src/app/menu.ts
index af4d5ca..5281f01 100644
--- a/src/app/menu.ts
+++ b/src/app/menu.ts
@@ -6,6 +6,7 @@
 // written once and the palette, the keyboard layer and the menu agree.
 
 import { openLibraryPath, openPaths } from "./commands";
+import { chordText } from "./chords";
 import { basename } from "./paths";
 import { command } from "./registry";
 import { reopenAs } from "./save";
@@ -50,14 +51,6 @@ export interface MenuSection {
   items: MenuNode[];
 }
 
-/** "Ctrl+Shift+S" -> "ctrl shift s", the way the spec writes chords. */
-export function chordText(chord: string): string {
-  return chord
-    .split("+")
-    .map((part) => part.trim().toLowerCase())
-    .join(" ");
-}
-
 let separators = 0;
 
 function separator(): MenuSeparator {
diff --git a/src/app/platform.ts b/src/app/platform.ts
new file mode 100644
index 0000000..db45c22
--- /dev/null
+++ b/src/app/platform.ts
@@ -0,0 +1,68 @@
+// The one place that knows which platform this is, and the few browser
+// capabilities that differ between WebView2 and WKWebView (spec §13a).
+// Everything else asks here rather than sniffing on its own.
+
+let forced: boolean | null = null;
+
+/**
+ * Tests drive both platforms through this; `null` hands detection back to
+ * the real navigator.
+ */
+export function setMacForTests(value: boolean | null): void {
+  forced = value;
+}
+
+function detect(): boolean {
+  if (typeof navigator === "undefined") return false;
+  const data = (navigator as { userAgentData?: { platform?: string } }).userAgentData;
+  // userAgentData.platform is the modern one; navigator.platform still works
+  // in WKWebView and is what Safari reports.
+  return /mac/i.test(data?.platform ?? navigator.platform ?? "");
+}
+
+export function isMac(): boolean {
+  return forced ?? detect();
+}
+
+/**
+ * What the primary modifier is called here. Chords are written with `Ctrl`
+ * everywhere (spec §13a); on macOS that key is ⌘.
+ */
+export function primaryModifierLabel(): string {
+  return isMac() ? "⌘" : "ctrl";
+}
+
+/* ------------------------------------------------------------ capability */
+
+interface IdleWindow {
+  requestIdleCallback?: (fn: () => void, options?: { timeout: number }) => number;
+  cancelIdleCallback?: (handle: number) => void;
+}
+
+/**
+ * `requestIdleCallback` where there is one, a timeout where there is not —
+ * Safari has never shipped it (spec §13a). Returns its own canceller so no
+ * caller has to know which of the two it got.
+ */
+export function idle(fn: () => void, timeout = 500): () => void {
+  const host = window as unknown as IdleWindow;
+  if (host.requestIdleCallback && host.cancelIdleCallback) {
+    const handle = host.requestIdleCallback(fn, { timeout });
+    return () => host.cancelIdleCallback?.(handle);
+  }
+  // Sooner than the timeout: this is the fallback, not the deadline.
+  const handle = window.setTimeout(fn, Math.min(200, timeout));
+  return () => window.clearTimeout(handle);
+}
+
+/**
+ * The CSS Custom Highlight API, which read's find bar paints with. Safari
+ * only has it from 17.2, so find degrades to "no highlight" without it.
+ */
+export function hasHighlights(): boolean {
+  return (
+    typeof CSS !== "undefined" &&
+    (CSS as unknown as { highlights?: unknown }).highlights !== undefined &&
+    typeof (globalThis as { Highlight?: unknown }).Highlight === "function"
+  );
+}
diff --git a/src/app/registry.ts b/src/app/registry.ts
index d12e26e..bcbfb9d 100644
--- a/src/app/registry.ts
+++ b/src/app/registry.ts
@@ -46,6 +46,7 @@ import {
   emitGotoLinePrompt,
   emitReplace,
 } from "../read/events";
+import { isMac } from "./platform";
 import { activeDoc, useStore } from "./store";
 
 export interface Command {
@@ -324,6 +325,36 @@ export const commands: Command[] = [
   { id: "app.about", title: "about", run: () => void showAbout() },
 ];
 
+/**
+ * The only chords macOS spells differently (spec §13a). Everything else is
+ * the same string, because `Ctrl` already means ⌘ there (chords.ts).
+ *
+ * | id                | windows       | macOS   | why                        |
+ * |-------------------|---------------|---------|----------------------------|
+ * | nav.back          | Alt+Left      | ⌘⌥←     | ⌥←/→ move by word on macOS |
+ * | nav.forward       | Alt+Right     | ⌘⌥→     | and ⌘[/⌘] is the editor's  |
+ * | view.fullscreen   | F11           | ⌃⌘F     | the system chord           |
+ *
+ * ⌘[ / ⌘] stay with the editor'''s indent, the way VS Code has them: indenting
+ * is a per-keystroke thing and history is not. Both would also collide —
+ * the editor keymap and the app layer would each fire once.
+ */
+export const macOverrides: Record<string, string> = {
+  "nav.back": "Ctrl+Alt+Left",
+  "nav.forward": "Ctrl+Alt+Right",
+  "view.fullscreen": "Control+Cmd+F",
+};
+
+/** Exported so the table can be tested without re-importing this module. */
+export function applyMacChords(list: Command[]): void {
+  for (const command of list) {
+    const override = macOverrides[command.id];
+    if (override) command.chord = override;
+  }
+}
+
+if (isMac()) applyMacChords(commands);
+
 const byId = new Map(commands.map((command) => [command.id, command]));
 
 export function command(id: string): Command | undefined {
diff --git a/src/app/settings.test.ts b/src/app/settings.test.ts
index 92360dd..bbc8548 100644
--- a/src/app/settings.test.ts
+++ b/src/app/settings.test.ts
@@ -25,7 +25,9 @@ describe("parseSettings", () => {
     expect(settings.edit.indentUnit).toBe("tab");
     expect(settings.library.extensions).toEqual([".md", ".txt"]);
     // Untouched sections still come from the defaults.
+    // The suite runs pinned to Windows (vitest.setup.ts), so this is crlf.
     expect(settings.files.newFileEol).toBe(DEFAULTS.files.newFileEol);
+    expect(DEFAULTS.files.newFileEol).toBe("crlf");
   });
 
   it("replaces a bad value without discarding the rest of the file", () => {
diff --git a/src/app/settings.ts b/src/app/settings.ts
index ed1c03f..47eb106 100644
--- a/src/app/settings.ts
+++ b/src/app/settings.ts
@@ -6,6 +6,7 @@ import { invoke } from "@tauri-apps/api/core";
 import { appDataDir, join } from "@tauri-apps/api/path";
 import { exists, readTextFile } from "@tauri-apps/plugin-fs";
 import { inTauri } from "./env";
+import { isMac } from "./platform";
 
 export type Theme = "system" | "light" | "dark";
 export type IndentUnit = "tab" | 2 | 4;
@@ -21,6 +22,11 @@ export interface Settings {
 
 export const SETTINGS_FILE = "settings.json";
 
+/** macOS writes LF, Windows writes CRLF (spec §13a). */
+export function defaultEol(): Eol {
+  return isMac() ? "lf" : "crlf";
+}
+
 export const DEFAULTS: Settings = {
   appearance: { theme: "system", fontSize: 13.5, contentWidth: 560 },
   read: { codeWrap: false },
diff --git a/src/editor/setup.ts b/src/editor/setup.ts
index 8779d3a..dc5a5a9 100644
--- a/src/editor/setup.ts
+++ b/src/editor/setup.ts
@@ -15,6 +15,7 @@ import type { SyntaxNode } from "@lezer/common";
 import { search, searchKeymap } from "@codemirror/search";
 import { commands } from "../app/registry";
 import { parseChord, toCode, type Chord } from "../app/chords";
+import { isMac } from "../app/platform";
 import {
   Compartment,
   EditorState,
@@ -200,7 +201,12 @@ function cmChord(key: string): Chord | null {
   if (!last) return null;
   const chord: Chord = { ctrl: false, shift: false, alt: false, meta: false, code: "" };
   for (const part of parts) {
-    if (part === "Mod" || part === "Ctrl" || part === "Control") chord.ctrl = true;
+    // CodeMirror's `Mod` is ⌘ on macOS and Ctrl everywhere else — the same
+    // rule the registry's `Ctrl` follows (spec §13a).
+    if (part === "Mod") {
+      if (isMac()) chord.meta = true;
+      else chord.ctrl = true;
+    } else if (part === "Ctrl" || part === "Control") chord.ctrl = true;
     else if (part === "Shift") chord.shift = true;
     else if (part === "Alt") chord.alt = true;
     else if (part === "Meta" || part === "Cmd") chord.meta = true;
@@ -222,7 +228,9 @@ function appKeymap(): readonly KeyBinding[] {
   for (const command of commands) {
     if (!command.chord) continue;
     try {
-      owned.add(chordKey(parseChord(command.chord)));
+      for (const chord of [command.chord, ...(command.chords ?? [])]) {
+        if (chord) owned.add(chordKey(parseChord(chord)));
+      }
     } catch {
       // A chord the app cannot parse cannot collide either.
     }
@@ -241,7 +249,8 @@ function appKeymap(): readonly KeyBinding[] {
   ].filter(
     (binding) =>
       !taken(binding.key, binding.shift !== undefined) &&
-      !taken(binding.win, binding.shift !== undefined),
+      // Each platform has its own override field in a CM6 binding.
+      !taken(isMac() ? binding.mac : binding.win, binding.shift !== undefined),
   );
   return shared;
 }
diff --git a/src/editor/sync.ts b/src/editor/sync.ts
index e90f8bf..e3ec75b 100644
--- a/src/editor/sync.ts
+++ b/src/editor/sync.ts
@@ -6,6 +6,7 @@
 import { EditorView } from "@codemirror/view";
 import type { EditorState } from "@codemirror/state";
 import type { Doc } from "../app/store";
+import { idle } from "../app/platform";
 import { useStore } from "../app/store";
 import { headingsOf } from "../read/headings";
 import { isDirty } from "./buffers";
@@ -30,7 +31,8 @@ export function caretOf(state: EditorState): { line: number; col: number } {
   return { line: line.number, col: head - line.from + 1 };
 }
 
-let idle: number | null = null;
+/** The canceller the shared idle wrapper handed back, or null. */
+let stopIdle: (() => void) | null = null;
 let frame: number | null = null;
 
 /**
@@ -50,10 +52,8 @@ export function syncTarget(): string | null {
 }
 
 function cancelIdle(): void {
-  if (idle === null) return;
-  if (typeof window.cancelIdleCallback === "function") window.cancelIdleCallback(idle);
-  else window.clearTimeout(idle);
-  idle = null;
+  stopIdle?.();
+  stopIdle = null;
 }
 
 function cancelFrame(): void {
@@ -94,15 +94,11 @@ export function flushText(view: EditorView, id: string): void {
 }
 
 function scheduleText(view: EditorView, id: string): void {
-  if (idle !== null) return;
-  const run = () => {
-    idle = null;
+  if (stopIdle !== null) return;
+  stopIdle = idle(() => {
+    stopIdle = null;
     flushText(view, id);
-  };
-  idle =
-    typeof window.requestIdleCallback === "function"
-      ? window.requestIdleCallback(run, { timeout: 500 })
-      : window.setTimeout(run, 200);
+  }, 500);
 }
 
 /** Caret, plus the dirty dot — it shouldn't wait for the text flush. */
diff --git a/src/read/find.ts b/src/read/find.ts
index 6c47d48..b153c86 100644
--- a/src/read/find.ts
+++ b/src/read/find.ts
@@ -2,6 +2,8 @@
 // the source: this searches what is on screen, and paints with the CSS
 // Custom Highlight API so the DOM is never touched.
 
+import { hasHighlights } from "../app/platform";
+
 interface Chunk {
   node: Text;
   start: number;
@@ -91,9 +93,14 @@ interface HighlightRegistryLike {
   delete(name: string): void;
 }
 
-const registry = (CSS as unknown as { highlights?: HighlightRegistryLike }).highlights;
-const HighlightCtor = (globalThis as unknown as { Highlight?: new (...ranges: Range[]) => object })
-  .Highlight;
+// Safari has the Custom Highlight API only from 17.2; without it find still
+// finds and scrolls, it just does not paint (spec §13a).
+const registry = hasHighlights()
+  ? (CSS as unknown as { highlights?: HighlightRegistryLike }).highlights
+  : undefined;
+const HighlightCtor = hasHighlights()
+  ? (globalThis as unknown as { Highlight?: new (...ranges: Range[]) => object }).Highlight
+  : undefined;
 
 export const ALL = "plain-find";
 export const CURRENT = "plain-find-active";
diff --git a/src/ui/QuickSearch.tsx b/src/ui/QuickSearch.tsx
index 3f7f65d..a1f3d2c 100644
--- a/src/ui/QuickSearch.tsx
+++ b/src/ui/QuickSearch.tsx
@@ -5,6 +5,7 @@
 import { useEffect, useMemo, useRef, useState } from "react";
 import { Command } from "cmdk";
 import { openInEdit, openLibraryPath, openPaths } from "../app/commands";
+import { chordText } from "../app/chords";
 import { basename, dirname } from "../app/paths";
 import { availableCommands, type Command as AppCommand } from "../app/registry";
 import { useStore } from "../app/store";
@@ -40,7 +41,7 @@ function commandEntries(query: string, close: () => void): Entry[] {
     .map((command: AppCommand) => ({
       key: `action:${command.id}`,
       label: plain(command.title),
-      where: (command.chord ?? command.hint ?? "").toLowerCase(),
+      where: command.chord ?? command.hint ? chordText((command.chord ?? command.hint) as string) : "",
       run: () => {
         close();
         void command.run();
diff --git a/src/ui/Settings.tsx b/src/ui/Settings.tsx
index 0996236..09d9e1d 100644
--- a/src/ui/Settings.tsx
+++ b/src/ui/Settings.tsx
@@ -4,7 +4,7 @@
 
 import { Fragment, useEffect, useState, type ReactNode } from "react";
 import { openSettingsFile } from "../app/commands";
-import { AUTOSAVE, DEFAULTS, type Settings } from "../app/settings";
+import { AUTOSAVE, DEFAULTS, defaultEol, type Settings } from "../app/settings";
 import { useStore } from "../app/store";
 import { TREE_CHANGED } from "../app/watcher";
 import { spellConf, spellExtension, syncLineNumbers } from "../editor/setup";
@@ -249,7 +249,7 @@ export function SettingsScreen() {
           />
         </Row>
 
-        <Row name="files.newFileEol" about="line endings for files plain creates">
+        <Row name="files.newFileEol" about={`line endings for files plain creates; ${defaultEol()} here`}>
           <Choice
             value={settings.files.newFileEol}
             options={[
diff --git a/src/ui/Shortcuts.tsx b/src/ui/Shortcuts.tsx
index d3888d9..9481de9 100644
--- a/src/ui/Shortcuts.tsx
+++ b/src/ui/Shortcuts.tsx
@@ -3,7 +3,7 @@
 // out of the command registry, so this page cannot drift from the bindings.
 
 import { Fragment, useEffect } from "react";
-import { chordText } from "../app/menu";
+import { chordText } from "../app/chords";
 import { commands } from "../app/registry";
 import { useStore } from "../app/store";
 import "./dialogs.css";
diff --git a/src/ui/StatusBar.tsx b/src/ui/StatusBar.tsx
index ea6433f..945fed4 100644
--- a/src/ui/StatusBar.tsx
+++ b/src/ui/StatusBar.tsx
@@ -2,25 +2,10 @@ import { Fragment, useEffect, useState } from "react";
 import { encodingLabel } from "../app/eol";
 import { reopenAs } from "../app/save";
 import { countWords } from "../read/words";
+import { idle } from "../app/platform";
 import { activeDoc, useStore, type Doc } from "../app/store";
 import "./dialogs.css";
 
-interface IdleWindow {
-  requestIdleCallback?: (fn: () => void, options?: { timeout: number }) => number;
-  cancelIdleCallback?: (handle: number) => void;
-}
-
-/** Runs `fn` when the browser has a moment; returns its canceller. */
-function idle(fn: () => void): () => void {
-  const host = window as unknown as IdleWindow;
-  if (host.requestIdleCallback && host.cancelIdleCallback) {
-    const handle = host.requestIdleCallback(fn, { timeout: 2000 });
-    return () => host.cancelIdleCallback?.(handle);
-  }
-  const handle = window.setTimeout(fn, 50);
-  return () => window.clearTimeout(handle);
-}
-
 /** Spec §4, in the order the spec lists them. */
 function state(doc: Doc): string {
   if (doc.deleted) return "deleted on disk";
@@ -88,7 +73,8 @@ export function StatusBar() {
     const count = () => {
       if (live) setWords(countWords(text));
     };
-    const cancel = idle(count);
+    // The word count can wait longer than a text flush can.
+    const cancel = idle(count, 2000);
     return () => {
       live = false;
       cancel();

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n '13a|"'^## 8|''^## 12|macOS|сохранност'"' ТЗ-Plain.md; rg -n 'ctrlKey|metaKey|setZoom|contextmenu|spellcheck|pending|open-path' src src-tauri/src" in G:\Claude Projects\md reader
 succeeded in 616ms:
src\app\bootstrap.ts:9:  pendingFolders,
src\app\bootstrap.ts:10:  pendingPaths,
src\app\bootstrap.ts:64:  await listen("open-path", () => void drainPendingPaths());
src\app\bootstrap.ts:65:  const args = await pendingPaths();
src\app\bootstrap.ts:66:  const folders = await pendingFolders();
src\app\chords.test.ts:5:  return { code, ctrlKey: false, shiftKey: false, altKey: false, metaKey: false, ...mods };
src\app\chords.test.ts:23:    expect(matchesChord("Ctrl+B", key("KeyB", { ctrlKey: true }))).toBe(true);
src\app\chords.test.ts:27:    expect(matchesChord("Ctrl+F", key("KeyF", { ctrlKey: true }))).toBe(true);
src\app\chords.test.ts:28:    expect(matchesChord("Ctrl+F", key("KeyF", { ctrlKey: true, shiftKey: true }))).toBe(false);
src\app\chords.ts:17:  ctrlKey: boolean;
src\app\chords.ts:20:  metaKey: boolean;
src\app\chords.ts:178:    event.ctrlKey === c.ctrl &&
src\app\chords.ts:181:    event.metaKey === c.meta
src-tauri/src\lib.rs:133:/// Drained by the frontend at startup and again on every `open-path` signal,
src-tauri/src\lib.rs:136:fn take_pending_paths(pending: State<'_, PendingPaths>) -> Vec<String> {
src-tauri/src\lib.rs:137:    pending
src-tauri/src\lib.rs:145:fn take_pending_folders(pending: State<'_, PendingFolders>) -> Vec<String> {
src-tauri/src\lib.rs:146:    pending
src-tauri/src\lib.rs:206:                let _ = app.emit("open-path", ());
src-tauri/src\lib.rs:241:            take_pending_paths,
src-tauri/src\lib.rs:242:            take_pending_folders,
src-tauri/src\lib.rs:291:                    let _ = _app.emit("open-path", ());
src\app\commands.ts:136:export async function pendingPaths(): Promise<string[]> {
src\app\commands.ts:137:  return inTauri ? invoke<string[]>("take_pending_paths") : [];
src\app\commands.ts:141:export async function pendingFolders(): Promise<string[]> {
src\app\commands.ts:142:  return inTauri ? invoke<string[]>("take_pending_folders") : [];
src\app\commands.ts:146:  const folders = await pendingFolders();
src\app\commands.ts:149:  const paths = await pendingPaths();
src\editor\EditView.tsx:35:let pending: { goto: Goto; at: number } | null = null;
src\editor\EditView.tsx:163:    else pending = { goto, at: Date.now() };
src\editor\EditView.tsx:176:  if (!pending || Date.now() - pending.at >= PENDING_MS) {
src\editor\EditView.tsx:177:    pending = null;
src\editor\EditView.tsx:180:  const { goto } = pending;
src\editor\EditView.tsx:181:  pending = null;
src\editor\EditView.tsx:278:      if (jumped && !measured) pending = { goto: jumped, at: Date.now() };
src\read\dom.ts:236:/** Runs the whole pass; the returned function stops anything still pending. */
src\app\drafts.test.ts:11:  pendingDrafts,
src\app\drafts.test.ts:132:    expect(pendingDrafts()).toEqual([]);
src\app\drafts.test.ts:136:    expect(pendingDrafts()).toEqual([doc.id]);
src\app\drafts.test.ts:143:    expect(pendingDrafts()).toEqual([]);
src\app\drafts.ts:155:const pending = new Set<string>();
src\app\drafts.ts:158:  const ids = [...pending];
src\app\drafts.ts:159:  pending.clear();
src\app\drafts.ts:164:  pending.add(id);
src\app\drafts.ts:173:export function pendingDrafts(): string[] {
src\app\drafts.ts:174:  return [...pending];
src\app\drafts.ts:209:  pending.delete(doc.id);
src\ui\Library.tsx:36:  const pending = useRef<ReturnType<typeof setTimeout> | undefined>(undefined);
src\ui\Library.tsx:60:      clearTimeout(pending.current);
src\ui\Library.tsx:61:      pending.current = setTimeout(() => setCounted((n) => n + 1), 60);
src\ui\Library.tsx:77:      clearTimeout(pending.current);
src\library\tree.ts:85:let pending: Promise<void> = Promise.resolve();
src\library\tree.ts:97:    return pending;
src\library\tree.ts:100:  pending = (async () => {
src\library\tree.ts:125:  return pending;
src\ui\QuickSearch.tsx:156:        if (event.key === "Enter") withCtrl.current = event.ctrlKey;
src\app\keys.ts:53:  window.addEventListener("contextmenu", noMenu);
src\app\keys.ts:71:    window.removeEventListener("contextmenu", noMenu);
src\app\mac.test.ts:16:    ctrlKey: false,
src\app\mac.test.ts:19:    metaKey: false,
src\app\mac.test.ts:21:  } as { code: string; ctrlKey: boolean; shiftKey: boolean; altKey: boolean; metaKey: boolean };
src\app\mac.test.ts:38:    expect(matchesChord("Ctrl+B", press({ code: "KeyB", metaKey: true }))).toBe(true);
src\app\mac.test.ts:40:    expect(matchesChord("Ctrl+B", press({ code: "KeyB", ctrlKey: true }))).toBe(false);
src\app\mac.test.ts:41:    expect(matchesChord("Ctrl+B", press({ code: "KeyB", metaKey: true, ctrlKey: true }))).toBe(
src\app\mac.test.ts:48:    expect(matchesChord("Ctrl+B", press({ code: "KeyB", ctrlKey: true }))).toBe(true);
src\app\mac.test.ts:49:    expect(matchesChord("Ctrl+B", press({ code: "KeyB", metaKey: true }))).toBe(false);
src\app\mac.test.ts:54:    expect(matchesChord("Control+Cmd+F", press({ code: "KeyF", ctrlKey: true, metaKey: true }))).toBe(
src\app\mac.test.ts:64:    expect(matchesChord("Ctrl+Alt+Left", press({ code: "ArrowLeft", metaKey: true, altKey: true })))
src\editor\setup.ts:62:  return EditorView.contentAttributes.of({ spellcheck: on ? "true" : "false" });
src\editor\setup.ts:329:    spellConf.of(spellExtension(settings.edit.spellcheck)),
src\ui\Settings.tsx:224:        <Row name="edit.spellcheck" about="uses the windows dictionaries">
src\ui\Settings.tsx:226:            value={settings.edit.spellcheck}
src\ui\Settings.tsx:228:            onPick={(spellcheck) => {
src\ui\Settings.tsx:229:              patch({ ...settings, edit: { ...settings.edit, spellcheck } });
src\ui\Settings.tsx:233:                view.dispatch({ effects: spellConf.reconfigure(spellExtension(spellcheck)) });
src\editor\toolbar.ts:42:  /** Only the newest pending mouseup may place the panel. */
src\read\ReadView.tsx:46:let pendingAnchor: { id: string; hash: string } | null = null;
src\read\ReadView.tsx:266:    if (pendingAnchor?.id === docId) {
src\read\ReadView.tsx:267:      const hash = pendingAnchor.hash;
src\read\ReadView.tsx:268:      pendingAnchor = null;
src\read\ReadView.tsx:351:          if (hash && id) pendingAnchor = { id, hash };
src\read\ReadView.tsx:395:      follow(link, event.ctrlKey || event.metaKey);
src\app\session.test.ts:43:    store.setZoom(1.3);
src\app\settings.test.ts:16:        edit: { lineNumbers: false, indentUnit: "tab", spellcheck: false },
src\app\settings.test.ts:46:  edit: { lineNumbers: false, indentUnit: "tab", spellcheck: false },
src\app\settings.test.ts:64:      "spellcheck",
src\app\settings.ts:18:  edit: { lineNumbers: boolean; indentUnit: IndentUnit; spellcheck: boolean };
src\app\settings.ts:33:  edit: { lineNumbers: true, indentUnit: 2, spellcheck: true },
src\app\settings.ts:87:      spellcheck: bool(edit["spellcheck"], DEFAULTS.edit.spellcheck),
src\app\settings.ts:150:        spellcheck: settings.edit.spellcheck,
src\app\settings.ts:181:let pending: Settings | null = null;
src\app\settings.ts:196:  const next = pending;
src\app\settings.ts:197:  pending = null;
src\app\settings.ts:212:  pending = settings;
src\app\store.ts:277:  setZoom: (zoom: number) => void;
src\app\store.ts:401:  setZoom: (zoom) => set({ zoom }),
src\app\zoom.ts:16:  store.setZoom(next);
src\app\zoom.ts:20:      .setZoom(next)
src\app\zoom.ts:39:  useStore.getState().setZoom(zoom);
src\app\zoom.ts:42:      .setZoom(zoom)

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'src-tauri/src/lib.rs','src/app/platform.ts','src/app/chords.ts','src/app/keys.ts','src/app/bootstrap.ts','src/app/registry.ts'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=0; Get-Content $f | ForEach-Object { $n++; "${n}: $_" } }' in G:\Claude Projects\md reader
 succeeded in 201ms:
FILE src-tauri/src/lib.rs
1: mod assoc;
2: // `fs`, `search` and `tree` are the three modules the integration test in
3: // `tests/pack.rs` drives directly (spec �16). Nothing about them changes �?"
4: // only who is allowed to say their names.
5: pub mod fs;
6: pub mod history;
7: pub mod search;
8: pub mod tree;
9: mod watch;
10: 
11: use std::path::{Path, PathBuf};
12: use std::sync::Mutex;
13: 
14: use tauri::plugin::{Builder as PluginBuilder, TauriPlugin};
15: use tauri::{Emitter, Manager, Runtime, State, Url};
16: use tauri_plugin_fs::FsExt;
17: 
18: /// Paths waiting for the frontend to collect them: launch arguments and
19: /// whatever a second launch handed over.
20: #[derive(Default)]
21: struct PendingPaths(Mutex<Vec<String>>);
22: 
23: /// The same, for folders: a folder given as an argument opens as the library
24: /// (spec �6).
25: #[derive(Default)]
26: struct PendingFolders(Mutex<Vec<String>>);
27: 
28: /// argv -> absolute paths. A relative argument belongs to the working
29: /// directory of the process that produced it, which for a second launch is
30: /// not our own, so the cwd is always passed in. Only arguments that really
31: /// are files survive: `tauri dev` passes `--color always`, and dropping
32: /// anything that starts with `-` would still leave `always` behind.
33: fn resolved_args<I: IntoIterator<Item = String>>(argv: I, cwd: &Path) -> Vec<PathBuf> {
34:     argv.into_iter()
35:         .skip(1)
36:         .map(|arg| {
37:             let path = PathBuf::from(arg);
38:             let absolute = if path.is_absolute() {
39:                 path
40:             } else {
41:                 cwd.join(path)
42:             };
43:             // Drops the "." components a shell leaves behind.
44:             absolute.components().collect()
45:         })
46:         .collect()
47: }
48: 
49: /* ------------------------------------------------------- where data lives */
50: 
51: /// Portable mode: a `data` folder next to `plain.exe` holds settings, drafts,
52: /// the session and the history instead of `%APPDATA%\Plain` (spec �2a).
53: /// Nothing else changes �?" the folder either exists or it does not.
54: pub fn pick_data_dir(beside_exe: Option<&Path>, app_data: &Path) -> PathBuf {
55:     match beside_exe {
56:         Some(folder) if folder.is_dir() => folder.to_path_buf(),
57:         _ => app_data.to_path_buf(),
58:     }
59: }
60: 
61: /// Windows only: on macOS the app is a bundle and a folder next to it is
62: /// not something a user can rely on (spec �13a).
63: #[cfg(windows)]
64: fn beside_exe() -> Option<PathBuf> {
65:     Some(std::env::current_exe().ok()?.parent()?.join("data"))
66: }
67: 
68: #[cfg(not(windows))]
69: fn beside_exe() -> Option<PathBuf> {
70:     None
71: }
72: 
73: /// The one folder every file of ours lives in. Rust and the front end have to
74: /// agree on it, so the front end asks rather than working it out again.
75: pub fn data_dir<R: Runtime>(app: &tauri::AppHandle<R>) -> PathBuf {
76:     let app_data = app.path().app_data_dir().unwrap_or_default();
77:     pick_data_dir(beside_exe().as_deref(), &app_data)
78: }
79: 
80: #[tauri::command]
81: fn data_path(app: tauri::AppHandle) -> String {
82:     data_dir(&app).to_string_lossy().into_owned()
83: }
84: 
85: fn path_args<I: IntoIterator<Item = String>>(argv: I, cwd: &Path) -> Vec<String> {
86:     resolved_args(argv, cwd)
87:         .into_iter()
88:         .filter(|path| path.is_file())
89:         .map(|path| path.to_string_lossy().into_owned())
90:         .collect()
91: }
92: 
93: /// A folder argument is a library, not a document (spec �6).
94: fn folder_args<I: IntoIterator<Item = String>>(argv: I, cwd: &Path) -> Vec<String> {
95:     resolved_args(argv, cwd)
96:         .into_iter()
97:         .filter(|path| path.is_dir())
98:         .map(|path| path.to_string_lossy().into_owned())
99:         .collect()
100: }
101: 
102: /// Widens the fs scope �?" the dialog plugin only covers what the user picked
103: /// in a dialog �?" and parks the paths until the frontend asks for them.
104: fn queue_paths<R: Runtime, M: Manager<R>>(manager: &M, paths: Vec<String>) {
105:     if paths.is_empty() {
106:         return;
107:     }
108:     if let Some(scope) = manager.try_fs_scope() {
109:         for path in &paths {
110:             let _ = scope.allow_file(path);
111:         }
112:     }
113:     if let Ok(mut queue) = manager.state::<PendingPaths>().0.lock() {
114:         queue.extend(paths);
115:     }
116: }
117: 
118: /// Parks folder arguments the same way, and widens the scope they need.
119: fn queue_folders<R: Runtime, M: Manager<R>>(manager: &M, folders: Vec<String>) {
120:     if folders.is_empty() {
121:         return;
122:     }
123:     if let Some(scope) = manager.try_fs_scope() {
124:         for folder in &folders {
125:             let _ = scope.allow_directory(folder, true);
126:         }
127:     }
128:     if let Ok(mut queue) = manager.state::<PendingFolders>().0.lock() {
129:         queue.extend(folders);
130:     }
131: }
132: 
133: /// Drained by the frontend at startup and again on every `open-path` signal,
134: /// so a path that arrives before the listener exists is never lost.
135: #[tauri::command]
136: fn take_pending_paths(pending: State<'_, PendingPaths>) -> Vec<String> {
137:     pending
138:         .0
139:         .lock()
140:         .map(|mut queue| std::mem::take(&mut *queue))
141:         .unwrap_or_default()
142: }
143: 
144: #[tauri::command]
145: fn take_pending_folders(pending: State<'_, PendingFolders>) -> Vec<String> {
146:     pending
147:         .0
148:         .lock()
149:         .map(|mut queue| std::mem::take(&mut *queue))
150:         .unwrap_or_default()
151: }
152: 
153: /// Hands a folder to the asset protocol so its images can be shown, and to
154: /// the file-system scope so links inside it can be opened (spec �9).
155: /// Called by the frontend for the folder of the open file, for the library,
156: /// and for whatever an `outside folder �� allow` placeholder points at.
157: #[tauri::command]
158: fn allow_asset_dir(app: tauri::AppHandle, path: String) -> Result<(), String> {
159:     let dir = Path::new(&path);
160:     if !dir.is_dir() {
161:         return Err(format!("not a folder: {path}"));
162:     }
163:     app.asset_protocol_scope()
164:         .allow_directory(dir, true)
165:         .map_err(|error| error.to_string())?;
166:     if let Some(scope) = app.try_fs_scope() {
167:         scope
168:             .allow_directory(dir, true)
169:             .map_err(|error| error.to_string())?;
170:     }
171:     Ok(())
172: }
173: 
174: /// The webview never leaves our own origin (spec �14); links open in the
175: /// system browser instead.
176: fn navigation_guard<R: Runtime>() -> TauriPlugin<R> {
177:     PluginBuilder::new("plain-navigation")
178:         .on_navigation(|_webview, url: &Url| match url.scheme() {
179:             "tauri" => url.host_str() == Some("localhost"),
180:             "http" => {
181:                 url.host_str() == Some("tauri.localhost")
182:                     || (cfg!(dev) && url.host_str() == Some("localhost"))
183:             }
184:             _ => false,
185:         })
186:         .build()
187: }
188: 
189: pub fn run() {
190:     tauri::Builder::default()
191:         // Order matters: single-instance first (spec �13), and persisted-scope
192:         // reads the fs scope during its own setup, so fs has to come before it.
193:         .plugin(tauri_plugin_single_instance::init(|app, argv, cwd| {
194:             if let Some(window) = app.get_webview_window("main") {
195:                 let _ = window.unminimize();
196:                 let _ = window.show();
197:                 let _ = window.set_focus();
198:             }
199:             let cwd = Path::new(&cwd);
200:             let paths = path_args(argv.clone(), cwd);
201:             let folders = folder_args(argv, cwd);
202:             if !paths.is_empty() || !folders.is_empty() {
203:                 queue_paths(app, paths);
204:                 queue_folders(app, folders);
205:                 // No payload: the event only says "there is something to take".
206:                 let _ = app.emit("open-path", ());
207:             }
208:         }))
209:         .plugin(tauri_plugin_fs::init())
210:         .plugin(tauri_plugin_persisted_scope::init())
211:         .plugin(tauri_plugin_dialog::init())
212:         .plugin(tauri_plugin_opener::init())
213:         .plugin(tauri_plugin_window_state::Builder::default().build())
214:         .plugin(navigation_guard())
215:         .manage(PendingPaths::default())
216:         .manage(PendingFolders::default())
217:         .manage(watch::Watcher::default())
218:         .setup(|app| {
219:             // Idempotent, and only for a real build: a `tauri dev` run would
220:             // otherwise point every `.md` at target/debug (spec �13).
221:             #[cfg(not(debug_assertions))]
222:             assoc::register_quietly();
223:             let cwd = std::env::current_dir().unwrap_or_default();
224:             let argv: Vec<String> = std::env::args().collect();
225:             queue_paths(app.handle(), path_args(argv.clone(), &cwd));
226:             queue_folders(app.handle(), folder_args(argv, &cwd));
227:             // Our own folder is ours to read and write, wherever it is. The
228:             // capability grants %APPDATA%, so portable mode would otherwise
229:             // leave the front end able to write drafts through Rust but not
230:             // to list or delete them through plugin-fs (spec �2a).
231:             if let Some(scope) = app.handle().try_fs_scope() {
232:                 let _ = scope.allow_directory(data_dir(app.handle()), true);
233:             }
234:             // Snapshots older than thirty days go at startup (spec �2a); the
235:             // front end never has to think about it.
236:             let handle = app.handle().clone();
237:             std::thread::spawn(move || history::clean(&handle));
238:             Ok(())
239:         })
240:         .invoke_handler(tauri::generate_handler![
241:             take_pending_paths,
242:             take_pending_folders,
243:             allow_asset_dir,
244:             assoc::register_file_association,
245:             assoc::unregister_file_association,
246:             assoc::file_association_registered,
247:             data_path,
248:             fs::read_file,
249:             fs::canonical_path,
250:             fs::write_file_atomic,
251:             fs::write_text_atomic,
252:             fs::hash_file,
253:             fs::trash_path,
254:             watch::watch,
255:             watch::unwatch,
256:             tree::read_tree,
257:             tree::path_kind,
258:             tree::rename_path,
259:             tree::create_file,
260:             tree::create_dir,
261:             search::search_folder,
262:             search::collect_tags,
263:             search::backlinks,
264:             history::list_snapshots,
265:             history::snapshot_text,
266:             history::delete_snapshot
267:         ])
268:         .build(tauri::generate_context!())
269:         .expect("error while building Plain")
270:         .run(|_app, _event| {
271:             // Finder does not pass a path in argv: it sends the app an Apple
272:             // Event, which Tauri turns into this (spec �13a). Same queue the
273:             // command line uses, same nudge to the front end.
274:             #[cfg(target_os = "macos")]
275:             if let tauri::RunEvent::Opened { urls } = _event {
276:                 let paths: Vec<String> = urls
277:                     .iter()
278:                     .filter_map(|url| url.to_file_path().ok())
279:                     .filter(|path| path.is_file())
280:                     .map(|path| path.to_string_lossy().into_owned())
281:                     .collect();
282:                 let folders: Vec<String> = urls
283:                     .iter()
284:                     .filter_map(|url| url.to_file_path().ok())
285:                     .filter(|path| path.is_dir())
286:                     .map(|path| path.to_string_lossy().into_owned())
287:                     .collect();
288:                 if !paths.is_empty() || !folders.is_empty() {
289:                     queue_paths(_app, paths);
290:                     queue_folders(_app, folders);
291:                     let _ = _app.emit("open-path", ());
292:                 }
293:             }
294:         });
295: }
296: 
297: #[cfg(test)]
298: mod tests {
299:     use super::{folder_args, path_args, pick_data_dir};
300:     use std::path::Path;
301: 
302:     /// �2a: a `data` folder next to the exe takes over from %APPDATA%, and
303:     /// nothing else does.
304:     #[test]
305:     fn a_data_folder_next_to_the_exe_wins() {
306:         let dir = tempfile::tempdir().unwrap();
307:         let app_data = dir.path().join("appdata");
308:         let portable = dir.path().join("beside-exe").join("data");
309: 
310:         // Not there: the ordinary place.
311:         assert_eq!(pick_data_dir(Some(&portable), &app_data), app_data);
312:         assert_eq!(pick_data_dir(None, &app_data), app_data);
313: 
314:         // A file called `data` is not a folder and does not count.
315:         std::fs::create_dir_all(portable.parent().unwrap()).unwrap();
316:         std::fs::write(&portable, "not a folder").unwrap();
317:         assert_eq!(pick_data_dir(Some(&portable), &app_data), app_data);
318: 
319:         std::fs::remove_file(&portable).unwrap();
320:         std::fs::create_dir(&portable).unwrap();
321:         assert_eq!(pick_data_dir(Some(&portable), &app_data), portable);
322:     }
323: 
324:     #[test]
325:     fn without_an_exe_folder_it_is_still_the_ordinary_place() {
326:         assert_eq!(
327:             pick_data_dir(None, Path::new(r"C:\Users\me\AppData\Roaming\Plain")),
328:             Path::new(r"C:\Users\me\AppData\Roaming\Plain")
329:         );
330:     }
331: 
332:     /// `tauri dev` hands us `--color always`; only real files may survive.
333:     #[test]
334:     fn keeps_only_the_arguments_that_are_files() {
335:         let dir = tempfile::tempdir().unwrap();
336:         let file = dir.path().join("a.md");
337:         std::fs::write(&file, "x").unwrap();
338:         let argv = vec![
339:             "plain.exe".to_string(),
340:             "--color".to_string(),
341:             "always".to_string(),
342:             file.to_string_lossy().into_owned(),
343:         ];
344:         assert_eq!(
345:             path_args(argv, dir.path()),
346:             vec![file.to_string_lossy().into_owned()]
347:         );
348:     }
349: 
350:     #[cfg(windows)] // C:\-shaped paths
351:     #[test]
352:     fn resolves_relatives_against_the_given_cwd() {
353:         let dir = tempfile::tempdir().unwrap();
354:         std::fs::create_dir(dir.path().join("notes")).unwrap();
355:         let file = dir.path().join("notes").join("b.md");
356:         std::fs::write(&file, "x").unwrap();
357: 
358:         let argv = vec!["plain.exe".to_string(), r".\notes\b.md".to_string()];
359:         assert_eq!(
360:             path_args(argv, dir.path()),
361:             vec![file.to_string_lossy().into_owned()]
362:         );
363:     }
364: 
365:     /// A folder argument is a library, and never a document (spec �6).
366:     #[test]
367:     fn a_folder_argument_goes_to_the_other_queue() {
368:         let dir = tempfile::tempdir().unwrap();
369:         let argv = vec![
370:             "plain.exe".to_string(),
371:             dir.path().to_string_lossy().into_owned(),
372:         ];
373:         assert!(path_args(argv.clone(), dir.path()).is_empty());
374:         assert_eq!(
375:             folder_args(argv, dir.path()),
376:             vec![dir.path().to_string_lossy().into_owned()]
377:         );
378:     }
379: }
FILE src/app/platform.ts
1: // The one place that knows which platform this is, and the few browser
2: // capabilities that differ between WebView2 and WKWebView (spec �13a).
3: // Everything else asks here rather than sniffing on its own.
4: 
5: let forced: boolean | null = null;
6: 
7: /**
8:  * Tests drive both platforms through this; `null` hands detection back to
9:  * the real navigator.
10:  */
11: export function setMacForTests(value: boolean | null): void {
12:   forced = value;
13: }
14: 
15: function detect(): boolean {
16:   if (typeof navigator === "undefined") return false;
17:   const data = (navigator as { userAgentData?: { platform?: string } }).userAgentData;
18:   // userAgentData.platform is the modern one; navigator.platform still works
19:   // in WKWebView and is what Safari reports.
20:   return /mac/i.test(data?.platform ?? navigator.platform ?? "");
21: }
22: 
23: export function isMac(): boolean {
24:   return forced ?? detect();
25: }
26: 
27: /**
28:  * What the primary modifier is called here. Chords are written with `Ctrl`
29:  * everywhere (spec �13a); on macOS that key is �??.
30:  */
31: export function primaryModifierLabel(): string {
32:   return isMac() ? "�??" : "ctrl";
33: }
34: 
35: /* ------------------------------------------------------------ capability */
36: 
37: interface IdleWindow {
38:   requestIdleCallback?: (fn: () => void, options?: { timeout: number }) => number;
39:   cancelIdleCallback?: (handle: number) => void;
40: }
41: 
42: /**
43:  * `requestIdleCallback` where there is one, a timeout where there is not �?"
44:  * Safari has never shipped it (spec �13a). Returns its own canceller so no
45:  * caller has to know which of the two it got.
46:  */
47: export function idle(fn: () => void, timeout = 500): () => void {
48:   const host = window as unknown as IdleWindow;
49:   if (host.requestIdleCallback && host.cancelIdleCallback) {
50:     const handle = host.requestIdleCallback(fn, { timeout });
51:     return () => host.cancelIdleCallback?.(handle);
52:   }
53:   // Sooner than the timeout: this is the fallback, not the deadline.
54:   const handle = window.setTimeout(fn, Math.min(200, timeout));
55:   return () => window.clearTimeout(handle);
56: }
57: 
58: /**
59:  * The CSS Custom Highlight API, which read's find bar paints with. Safari
60:  * only has it from 17.2, so find degrades to "no highlight" without it.
61:  */
62: export function hasHighlights(): boolean {
63:   return (
64:     typeof CSS !== "undefined" &&
65:     (CSS as unknown as { highlights?: unknown }).highlights !== undefined &&
66:     typeof (globalThis as { Highlight?: unknown }).Highlight === "function"
67:   );
68: }
FILE src/app/chords.ts
1: // Shortcuts are bound by KeyboardEvent.code so they keep working in a
2: // Russian layout (spec �12). This turns readable chords like "Ctrl+Shift+F"
3: // into a physical-key description, and into a tinykeys pattern.
4: 
5: import { isMac } from "./platform";
6: 
7: export interface Chord {
8:   ctrl: boolean;
9:   shift: boolean;
10:   alt: boolean;
11:   meta: boolean;
12:   code: string;
13: }
14: 
15: export interface KeyLike {
16:   code: string;
17:   ctrlKey: boolean;
18:   shiftKey: boolean;
19:   altKey: boolean;
20:   metaKey: boolean;
21: }
22: 
23: const PUNCT: Record<string, string> = {
24:   "/": "Slash",
25:   "\\": "Backslash",
26:   "[": "BracketLeft",
27:   "]": "BracketRight",
28:   ",": "Comma",
29:   ".": "Period",
30:   ";": "Semicolon",
31:   "'": "Quote",
32:   "`": "Backquote",
33:   "-": "Minus",
34:   "=": "Equal",
35: };
36: 
37: const NAMED: Record<string, string> = {
38:   space: "Space",
39:   enter: "Enter",
40:   return: "Enter",
41:   esc: "Escape",
42:   escape: "Escape",
43:   tab: "Tab",
44:   backspace: "Backspace",
45:   delete: "Delete",
46:   del: "Delete",
47:   insert: "Insert",
48:   up: "ArrowUp",
49:   down: "ArrowDown",
50:   left: "ArrowLeft",
51:   right: "ArrowRight",
52:   home: "Home",
53:   end: "End",
54:   pageup: "PageUp",
55:   pagedown: "PageDown",
56: };
57: 
58: /** Maps one chord token to a KeyboardEvent.code. */
59: export function toCode(token: string): string {
60:   if (/^[a-z]$/i.test(token)) return "Key" + token.toUpperCase();
61:   if (/^[0-9]$/.test(token)) return "Digit" + token;
62:   if (/^f([1-9]|1[0-9]|2[0-4])$/i.test(token)) return "F" + token.slice(1);
63:   const punct = PUNCT[token];
64:   if (punct) return punct;
65:   const named = NAMED[token.toLowerCase()];
66:   if (named) return named;
67:   // Already a code ("Backquote", "NumpadAdd", ...).
68:   if (/^[A-Z][A-Za-z0-9]*$/.test(token)) return token;
69:   throw new Error(`unknown key token: ${token}`);
70: }
71: 
72: /**
73:  * Modifier vocabulary (spec �13a):
74:  *
75:  * - `Ctrl` �?" the platform's primary modifier: `Ctrl` on Windows, �?? on macOS.
76:  *   Almost every chord in the registry is written with it.
77:  * - `Cmd` / `Meta` / `Win` �?" literally the meta key, on both platforms.
78:  * - `Control` �?" literally the control key, on both. Only macOS needs it, for
79:  *   the handful of system chords that really are �?? (fullscreen).
80:  * - `Alt` / `Option` �?" �?? on macOS.
81:  */
82: export function parseChord(chord: string): Chord {
83:   const parts = chord.split("+").map((p) => p.trim());
84:   const last = parts.pop();
85:   if (!last) throw new Error(`empty chord: ${chord}`);
86: 
87:   const out: Chord = {
88:     ctrl: false,
89:     shift: false,
90:     alt: false,
91:     meta: false,
92:     code: toCode(last),
93:   };
94:   for (const part of parts) {
95:     switch (part.toLowerCase()) {
96:       case "ctrl":
97:       case "mod":
98:         if (isMac()) out.meta = true;
99:         else out.ctrl = true;
100:         break;
101:       case "control":
102:         out.ctrl = true;
103:         break;
104:       case "shift":
105:         out.shift = true;
106:         break;
107:       case "alt":
108:       case "option":
109:         out.alt = true;
110:         break;
111:       case "meta":
112:       case "cmd":
113:       case "win":
114:         out.meta = true;
115:         break;
116:       default:
117:         throw new Error(`unknown modifier: ${part}`);
118:     }
119:   }
120:   return out;
121: }
122: 
123: /* --------------------------------------------------------------- display */
124: 
125: /** `KeyB` -> `B`, `Comma` -> `,`, `ArrowLeft` -> `��?`, for the macOS glyphs. */
126: const KEY_GLYPHS: Record<string, string> = {
127:   ArrowLeft: "��?",
128:   ArrowRight: "��'",
129:   ArrowUp: "��'",
130:   ArrowDown: "��"",
131:   Enter: "��c",
132:   Tab: "��?",
133:   Escape: "�?<",
134:   Backspace: "�?<",
135:   Delete: "�?�",
136:   Space: "space",
137: };
138: 
139: function keyLabel(code: string): string {
140:   const glyph = KEY_GLYPHS[code];
141:   if (glyph) return glyph;
142:   if (code.startsWith("Key")) return code.slice(3);
143:   if (code.startsWith("Digit")) return code.slice(5);
144:   if (/^F\d+$/.test(code)) return code;
145:   for (const [punct, name] of Object.entries(PUNCT)) {
146:     if (name === code) return punct;
147:   }
148:   return code;
149: }
150: 
151: /**
152:  * How a chord is written in the menu, the palette and the shortcuts screen.
153:  * Windows spells it out in the app's lowercase microcopy (`ctrl shift s`);
154:  * macOS uses the glyphs in Apple's order, �??�??���??, with nothing between them.
155:  */
156: export function chordText(chord: string): string {
157:   if (!isMac()) {
158:     return chord
159:       .split("+")
160:       .map((part) => part.trim().toLowerCase())
161:       .join(" ");
162:   }
163:   const c = parseChord(chord);
164:   return (
165:     (c.ctrl ? "�??" : "") +
166:     (c.alt ? "�??" : "") +
167:     (c.shift ? "��" : "") +
168:     (c.meta ? "�??" : "") +
169:     keyLabel(c.code)
170:   );
171: }
172: 
173: /** Exact match, including "no other modifier is held". */
174: export function matchesChord(chord: string | Chord, event: KeyLike): boolean {
175:   const c = typeof chord === "string" ? parseChord(chord) : chord;
176:   return (
177:     event.code === c.code &&
178:     event.ctrlKey === c.ctrl &&
179:     event.shiftKey === c.shift &&
180:     event.altKey === c.alt &&
181:     event.metaKey === c.meta
182:   );
183: }
184: 
185: /**
186:  * tinykeys pattern for the same chord. tinykeys compares the last token
187:  * against event.code as-is, so passing "KeyF" binds the physical key.
188:  */
189: export function chordToPattern(chord: string): string {
190:   const c = parseChord(chord);
191:   const mods: string[] = [];
192:   if (c.ctrl) mods.push("Control");
193:   if (c.alt) mods.push("Alt");
194:   if (c.shift) mods.push("Shift");
195:   if (c.meta) mods.push("Meta");
196:   return [...mods, c.code].join("+");
197: }
FILE src/app/keys.ts
1: // One keyboard layer for the whole app. There are no chords here: every
2: // binding comes from the command registry (spec �12), so a shortcut and the
3: // menu entry that shows it can never drift apart. Tree keys (`F2`, `Delete`,
4: // the arrows) are local to the tree and live in the rail.
5: 
6: import { tinykeys } from "tinykeys";
7: import { chordToPattern, matchesChord } from "./chords";
8: import { commands } from "./registry";
9: 
10: /**
11:  * WebView2 keeps its own accelerators (spec �12 leaves them on for zoom), but
12:  * these must not reach it �?" they are ours, or they would reload or print.
13:  */
14: const SWALLOW = [
15:   "Ctrl+F",
16:   "Ctrl+P",
17:   "Ctrl+G",
18:   "F5",
19:   "Ctrl+S",
20:   "Ctrl+Shift+S",
21:   "Ctrl+N",
22:   "Ctrl+O",
23:   "Ctrl+H",
24:   "Ctrl+K",
25:   "F1",
26: ];
27: 
28: /** chord -> action, straight out of the registry. */
29: export function bindings(): Record<string, () => void> {
30:   const map: Record<string, () => void> = {};
31:   for (const command of commands) {
32:     for (const chord of [command.chord, ...(command.chords ?? [])]) {
33:       if (chord) map[chord] = () => void command.run();
34:     }
35:   }
36:   return map;
37: }
38: 
39: export function installKeys(): () => void {
40:   const swallow = (event: KeyboardEvent) => {
41:     if (SWALLOW.some((chord) => matchesChord(chord, event))) event.preventDefault();
42:   };
43:   window.addEventListener("keydown", swallow, { capture: true });
44: 
45:   // WebView2's own menu is suppressed everywhere except the editor, where it
46:   // is the only way to reach cut/copy/paste and the spelling suggestions
47:   // Windows offers. Read has its own menu, which stops this event itself.
48:   const noMenu = (event: MouseEvent) => {
49:     const target = event.target as HTMLElement | null;
50:     if (target?.closest(".cm-editor")) return;
51:     event.preventDefault();
52:   };
53:   window.addEventListener("contextmenu", noMenu);
54: 
55:   const map: Record<string, (event: KeyboardEvent) => void> = {};
56:   for (const [chord, run] of Object.entries(bindings())) {
57:     map[chordToPattern(chord)] = (event) => {
58:       event.preventDefault();
59:       run();
60:     };
61:   }
62: 
63:   const off = tinykeys(window, map, {
64:     // Shortcuts must keep working while the editor (contenteditable) has
65:     // focus, so only auto-repeat and IME composition are skipped.
66:     ignore: (event) => event.repeat || event.isComposing,
67:   });
68: 
69:   return () => {
70:     window.removeEventListener("keydown", swallow, { capture: true });
71:     window.removeEventListener("contextmenu", noMenu);
72:     off();
73:   };
74: }
FILE src/app/bootstrap.ts
1: // Startup: settings, then unsaved work, then either the paths this launch
2: // was asked to open or the session that was open last time (spec �6, �8).
3: 
4: import { listen } from "@tauri-apps/api/event";
5: import {
6:   drainPendingPaths,
7:   openLibraryPath,
8:   openPaths,
9:   pendingFolders,
10:   pendingPaths,
11: } from "./commands";
12: import { listDrafts } from "./drafts";
13: import { inTauri } from "./env";
14: import { pathKey } from "./paths";
15: import { loadSession, readingPosition, type SessionFile } from "./session";
16: import { restoreZoom } from "./zoom";
17: import { loadSettings } from "./settings";
18: import { useStore } from "./store";
19: import { emitGotoHeading } from "../read/events";
20: 
21: /** Held while the `unsaved work found` screen is up (spec �8). */
22: let afterRecovery: (() => Promise<void>) | null = null;
23: 
24: export function recoveryDone(): void {
25:   const next = afterRecovery;
26:   afterRecovery = null;
27:   useStore.getState().setRecovery(null);
28:   void next?.();
29: }
30: 
31: async function restore(session: SessionFile): Promise<void> {
32:   const store = useStore.getState();
33:   await openPaths(session.files.map((f) => f.path));
34:   for (const file of session.files) {
35:     const id = pathKey(file.path);
36:     if (!useStore.getState().docs.some((d) => d.id === id)) continue;
37:     store.updateDoc(id, { mode: file.mode, caret: file.caret });
38:   }
39:   const active = session.active ? pathKey(session.active) : null;
40:   if (active && useStore.getState().docs.some((d) => d.id === active)) {
41:     store.activate(active);
42:     const heading = readingPosition(session.active as string);
43:     // After the read view has mounted and built the document.
44:     if (heading) setTimeout(() => emitGotoHeading(heading), 0);
45:   }
46: }
47: 
48: export async function bootstrap(): Promise<void> {
49:   const { settings, invalid } = await loadSettings();
50:   const store = useStore.getState();
51:   store.applySettings(settings);
52:   if (invalid) {
53:     store.showBanner({
54:       id: "settings",
55:       text: "settings.json is invalid",
56:       actions: [{ label: "dismiss", run: () => useStore.getState().dismissBanner("settings") }],
57:     });
58:   }
59: 
60:   if (!inTauri) return;
61: 
62:   // Listener first, then drain: a second launch can signal at any moment, and
63:   // the event is only a nudge to read the queue �?" the paths live in Rust.
64:   await listen("open-path", () => void drainPendingPaths());
65:   const args = await pendingPaths();
66:   const folders = await pendingFolders();
67: 
68:   // The library, the recent list and the reading positions come back either
69:   // way; only the list of open files depends on the arguments (spec �6).
70:   const session = await loadSession();
71:   if (session) {
72:     if (session.library) await openLibraryPath(session.library, false);
73:     store.setCollapsed(session.collapsed);
74:     store.setRecent(session.recent);
75:     store.setRecentLibraries(session.recentLibraries);
76:     restoreZoom(session.zoom);
77:     store.setRailView(session.rail.view);
78:     store.setRailCollapsed(session.rail.collapsed);
79:     store.setSplitRatio(session.split);
80:   }
81: 
82:   const open = async () => {
83:     // A folder argument is the library, and it replaces the last session's
84:     // open files �?" you asked for that folder, not for yesterday (spec �6).
85:     const folder = folders[0];
86:     if (folder) await openLibraryPath(folder);
87:     if (args.length > 0) {
88:       if ((await openPaths(args)) && !folder) useStore.getState().setRailCollapsed(true);
89:       return;
90:     }
91:     if (folder) return;
92:     if (session && session.files.length > 0) await restore(session);
93:   };
94: 
95:   const drafts = await listDrafts();
96:   if (drafts.length > 0) {
97:     afterRecovery = open;
98:     store.setRecovery(drafts);
99:     return;
100:   }
101:   await open();
102: }
FILE src/app/registry.ts
1: // Every command the app has, in one table. The keyboard layer (keys.ts), the
2: // quick-search palette (`>`), and the menu bar all read this list, so a chord
3: // or a title is written exactly once (spec �4, �12).
4: 
5: import {
6:   copyPath,
7:   copyPlainText,
8:   exitApp,
9:   exportHtmlFile,
10:   exportPdf,
11:   exportPlainText,
12:   newDoc,
13:   openFile,
14:   openFolderSearch,
15:   openLibrary,
16:   openQuickSearch,
17:   openSettingsFile,
18:   refresh,
19:   revealInExplorer,
20:   showAbout,
21:   toggleAlwaysOnTop,
22:   toggleFullscreen,
23: } from "./commands";
24: import { closeActive } from "./close";
25: import { zoomIn, zoomOut, zoomReset } from "./zoom";
26: import { runEditorCommand } from "../editor";
27: import { redo, undo } from "@codemirror/commands";
28: import {
29:   clearHeading,
30:   cycleList,
31:   insertLink,
32:   toggleBold,
33:   toggleCheckbox,
34:   toggleCodeBlock,
35:   toggleHeading,
36:   toggleInlineCode,
37:   toggleItalic,
38:   toggleQuote,
39: } from "../editor/commands";
40: import { reopenAs, saveActive, saveAs } from "./save";
41: import { toggleLineNumbers } from "../editor/setup";
42: import { goBack, goForward } from "../read/history";
43: import {
44:   emitFind,
45:   emitFindStep,
46:   emitGotoLinePrompt,
47:   emitReplace,
48: } from "../read/events";
49: import { isMac } from "./platform";
50: import { activeDoc, useStore } from "./store";
51: 
52: export interface Command {
53:   /** Stable id; the menu and the palette address commands by it. */
54:   id: string;
55:   /** Microcopy: lowercase latin (spec �1.4). */
56:   title: string;
57:   /** Spelled the way the spec spells it; parsed by chords.ts. */
58:   chord?: string;
59:   /**
60:    * Extra chords that do the same thing. `F8` is taken by a global hotkey on
61:    * some machines, so focus mode answers to two.
62:    */
63:   chords?: string[];
64:   /**
65:    * A chord somebody else already owns �?" the editor's keymap (spec �5.2) or
66:    * WebView2 (�12). The menu shows it; binding it here would run it twice.
67:    */
68:   hint?: string;
69:   run: () => void | Promise<void>;
70:   /** False hides the command from the palette; chords still fire. */
71:   when?: () => boolean;
72: }
73: 
74: const store = () => useStore.getState();
75: const hasDoc = () => store().activeId !== null;
76: const hasFile = () => activeDoc(store())?.path != null;
77: const hasLibrary = () => store().libraryPath !== null;
78: // Rich is edit with the markers hidden: every editing command works there
79: // too (spec �5.3).
80: const inEdit = () => {
81:   const mode = activeDoc(store())?.mode;
82:   return mode === "edit" || mode === "rich";
83: };
84: 
85: export const commands: Command[] = [
86:   /* ------------------------------------------------------------- file */
87:   { id: "file.new", title: "new file", chord: "Ctrl+N", run: () => newDoc() },
88:   { id: "file.open", title: "open file�?�", chord: "Ctrl+O", run: () => void openFile() },
89:   {
90:     id: "file.openLibrary",
91:     title: "open library�?�",
92:     chord: "Ctrl+Alt+O",
93:     run: () => void openLibrary(),
94:   },
95:   { id: "file.save", title: "save", chord: "Ctrl+S", run: () => void saveActive(), when: hasDoc },
96:   {
97:     id: "file.saveAs",
98:     title: "save as�?�",
99:     chord: "Ctrl+Shift+S",
100:     run: () => {
101:       const id = store().activeId;
102:       if (id) void saveAs(id);
103:     },
104:     when: hasDoc,
105:   },
106:   {
107:     id: "file.history",
108:     title: "version history�?�",
109:     run: () => {
110:       const id = store().activeId;
111:       if (id) useStore.getState().setHistory(id);
112:     },
113:     when: hasDoc,
114:   },
115:   {
116:     id: "file.copyPlain",
117:     title: "copy as plain text",
118:     chord: "Ctrl+Shift+Alt+C",
119:     run: () => void copyPlainText(),
120:     when: hasDoc,
121:   },
122:   {
123:     id: "file.exportText",
124:     title: "export as text�?�",
125:     run: () => void exportPlainText(),
126:     when: hasDoc,
127:   },
128:   {
129:     id: "file.exportHtml",
130:     title: "export as html�?�",
131:     run: () => void exportHtmlFile(),
132:     when: hasDoc,
133:   },
134:   {
135:     id: "file.exportPdf",
136:     title: "print / export as pdf�?�",
137:     chord: "Ctrl+P",
138:     run: () => void exportPdf(),
139:     when: hasDoc,
140:   },
141:   { id: "file.close", title: "close", chord: "Ctrl+W", run: () => closeActive(), when: hasDoc },
142:   {
143:     id: "file.reload",
144:     title: "reload from disk",
145:     chord: "F5",
146:     run: () => refresh(),
147:     when: hasDoc,
148:   },
149:   {
150:     id: "file.reopenUtf8",
151:     title: "reopen as utf-8",
152:     run: () => reopenAs("utf-8"),
153:     when: hasFile,
154:   },
155:   {
156:     id: "file.reopenCp1251",
157:     title: "reopen as cp1251",
158:     run: () => reopenAs("windows-1251"),
159:     when: hasFile,
160:   },
161:   {
162:     id: "file.reopenUtf16",
163:     title: "reopen as utf-16",
164:     run: () => reopenAs("utf-16le"),
165:     when: hasFile,
166:   },
167:   {
168:     id: "file.reveal",
169:     title: "reveal in explorer",
170:     chord: "Ctrl+Shift+E",
171:     run: () => void revealInExplorer(),
172:     when: () => hasFile() || store().treeSelected !== null,
173:   },
174:   {
175:     id: "file.copyPath",
176:     title: "copy path",
177:     chord: "Ctrl+Shift+C",
178:     run: () => void copyPath(),
179:     when: () => hasFile() || store().treeSelected !== null,
180:   },
181:   { id: "file.exit", title: "exit", run: () => void exitApp() },
182: 
183:   /* ------------------------------------------------------------- view */
184:   { id: "view.toggleMode", title: "toggle read/edit", chord: "Ctrl+/", run: () => store().toggleMode(), when: hasDoc },
185:   { id: "view.read", title: "read", chord: "Ctrl+Alt+1", run: () => store().setMode("read"), when: hasDoc },
186:   { id: "view.edit", title: "edit", chord: "Ctrl+Alt+2", run: () => store().setMode("edit"), when: hasDoc },
187:   { id: "view.rich", title: "rich", chord: "Ctrl+Alt+3", run: () => store().setMode("rich"), when: hasDoc },
188:   {
189:     id: "view.split",
190:     title: "split",
191:     chord: "Ctrl+Shift+\\",
192:     run: () => store().setMode("split"),
193:     when: hasDoc,
194:   },
195:   { id: "view.toggleRail", title: "toggle rail", chord: "Ctrl+\\", run: () => store().toggleRail() },
196:   {
197:     id: "view.library",
198:     title: "library",
199:     chord: "Ctrl+Alt+L",
200:     run: () => store().setLibraryOpen(!store().libraryOpen),
201:     when: () => store().libraryPath !== null,
202:   },
203:   { id: "view.files", title: "files", run: () => store().setRailView("files") },
204:   { id: "view.outline", title: "outline", run: () => store().setRailView("outline") },
205:   { id: "view.toggleTheme", title: "toggle theme", chord: "Ctrl+Shift+D", run: () => store().toggleTheme() },
206:   { id: "view.themeLight", title: "light theme", run: () => store().setTheme("light") },
207:   { id: "view.themeDark", title: "dark theme", run: () => store().setTheme("dark") },
208:   { id: "view.themeSystem", title: "system theme", run: () => store().setTheme("system") },
209:   { id: "view.lineNumbers", title: "line numbers", chord: "Ctrl+Shift+9", run: () => toggleLineNumbers() },
210:   { id: "view.alwaysOnTop", title: "always on top", chord: "Ctrl+Shift+A", run: () => void toggleAlwaysOnTop() },
211:   { id: "view.fullscreen", title: "fullscreen", chord: "F11", run: () => void toggleFullscreen() },
212:   {
213:     id: "view.focus",
214:     title: "focus",
215:     chord: "F8",
216:     chords: ["Ctrl+Alt+F"],
217:     run: () => store().setFocus(!store().focus),
218:   },
219:   { id: "view.zoomIn", title: "zoom in", hint: "Ctrl+=", run: () => zoomIn() },
220:   { id: "view.zoomOut", title: "zoom out", hint: "Ctrl+-", run: () => zoomOut() },
221:   { id: "view.zoomReset", title: "reset zoom", hint: "Ctrl+0", run: () => zoomReset() },
222: 
223:   /* ------------------------------------------------------------- edit */
224:   // The chords below belong to the editor's keymap (keymap.ts); they are
225:   // `hint`s so the menu can show them without the app binding them twice.
226:   { id: "edit.undo", title: "undo", hint: "Ctrl+Z", run: () => void runEditorCommand(undo), when: inEdit },
227:   { id: "edit.redo", title: "redo", hint: "Ctrl+Shift+Z", run: () => void runEditorCommand(redo), when: inEdit },
228:   { id: "edit.bold", title: "bold", hint: "Ctrl+B", run: () => void runEditorCommand(toggleBold), when: inEdit },
229:   { id: "edit.italic", title: "italic", hint: "Ctrl+I", run: () => void runEditorCommand(toggleItalic), when: inEdit },
230:   {
231:     id: "edit.code",
232:     title: "inline code",
233:     hint: "Ctrl+E",
234:     run: () => void runEditorCommand(toggleInlineCode),
235:     when: inEdit,
236:   },
237:   {
238:     id: "edit.codeBlock",
239:     title: "code block",
240:     hint: "Ctrl+Shift+`",
241:     run: () => void runEditorCommand(toggleCodeBlock),
242:     when: inEdit,
243:   },
244:   {
245:     id: "edit.link",
246:     title: "link�?�",
247:     hint: "Ctrl+Shift+K",
248:     run: () => void runEditorCommand(insertLink),
249:     when: inEdit,
250:   },
251:   {
252:     id: "edit.quote",
253:     title: "quote",
254:     hint: "Ctrl+Shift+Q",
255:     run: () => void runEditorCommand(toggleQuote),
256:     when: inEdit,
257:   },
258:   {
259:     id: "edit.list",
260:     title: "list",
261:     hint: "Ctrl+Shift+L",
262:     run: () => void runEditorCommand(cycleList),
263:     when: inEdit,
264:   },
265:   {
266:     id: "edit.checkbox",
267:     title: "checkbox",
268:     hint: "Ctrl+Enter",
269:     run: () => void runEditorCommand(toggleCheckbox),
270:     when: inEdit,
271:   },
272:   ...[1, 2, 3, 4, 5, 6].map((level) => ({
273:     id: `edit.heading${level}`,
274:     title: `heading ${level}`,
275:     hint: `Ctrl+${level}`,
276:     run: () => void runEditorCommand(toggleHeading(level)),
277:     when: inEdit,
278:   })),
279:   { id: "edit.headingOff", title: "no heading", run: () => void runEditorCommand(clearHeading), when: inEdit },
280: 
281:   /* ------------------------------------------------------- navigation */
282:   { id: "nav.quickSearch", title: "quick search", chord: "Ctrl+K", run: () => openQuickSearch() },
283:   { id: "nav.palette", title: "command palette", chord: "Ctrl+Shift+P", run: () => openQuickSearch(">") },
284:   { id: "nav.find", title: "find", chord: "Ctrl+F", run: () => emitFind(), when: hasDoc },
285:   { id: "nav.replace", title: "replace", chord: "Ctrl+H", run: () => emitReplace(), when: hasDoc },
286:   { id: "nav.findNext", title: "find next", chord: "F3", run: () => emitFindStep(1), when: hasDoc },
287:   { id: "nav.findPrevious", title: "find previous", chord: "Shift+F3", run: () => emitFindStep(-1), when: hasDoc },
288:   { id: "nav.gotoLine", title: "go to line", chord: "Ctrl+G", run: () => emitGotoLinePrompt(), when: inEdit },
289:   {
290:     id: "nav.searchInFolder",
291:     title: "search in folder",
292:     chord: "Ctrl+Shift+F",
293:     run: () => openFolderSearch(),
294:     when: hasLibrary,
295:   },
296:   { id: "nav.nextDoc", title: "next file", chord: "Ctrl+Tab", run: () => store().cycleDoc(1), when: hasDoc },
297:   {
298:     id: "nav.previousDoc",
299:     title: "previous file",
300:     chord: "Ctrl+Shift+Tab",
301:     run: () => store().cycleDoc(-1),
302:     when: hasDoc,
303:   },
304:   { id: "nav.back", title: "back", chord: "Alt+Left", run: () => goBack(), when: hasDoc },
305:   { id: "nav.forward", title: "forward", chord: "Alt+Right", run: () => goForward(), when: hasDoc },
306: 
307:   /* ------------------------------------------------------------- help */
308:   {
309:     id: "app.settings",
310:     title: "settings�?�",
311:     chord: "Ctrl+,",
312:     run: () => store().setSettingsOpen(true),
313:   },
314:   {
315:     id: "app.settingsFile",
316:     title: "open settings.json",
317:     run: () => void openSettingsFile(),
318:   },
319:   {
320:     id: "app.shortcuts",
321:     title: "shortcuts",
322:     chord: "F1",
323:     run: () => store().setShortcutsOpen(true),
324:   },
325:   { id: "app.about", title: "about", run: () => void showAbout() },
326: ];
327: 
328: /**
329:  * The only chords macOS spells differently (spec �13a). Everything else is
330:  * the same string, because `Ctrl` already means �?? there (chords.ts).
331:  *
332:  * | id                | windows       | macOS   | why                        |
333:  * |-------------------|---------------|---------|----------------------------|
334:  * | nav.back          | Alt+Left      | �??�??��?     | �??��?/��' move by word on macOS |
335:  * | nav.forward       | Alt+Right     | �??�??��'     | and �??[/�??] is the editor's  |
336:  * | view.fullscreen   | F11           | �??�??F     | the system chord           |
337:  *
338:  * �??[ / �??] stay with the editor'''s indent, the way VS Code has them: indenting
339:  * is a per-keystroke thing and history is not. Both would also collide �?"
340:  * the editor keymap and the app layer would each fire once.
341:  */
342: export const macOverrides: Record<string, string> = {
343:   "nav.back": "Ctrl+Alt+Left",
344:   "nav.forward": "Ctrl+Alt+Right",
345:   "view.fullscreen": "Control+Cmd+F",
346: };
347: 
348: /** Exported so the table can be tested without re-importing this module. */
349: export function applyMacChords(list: Command[]): void {
350:   for (const command of list) {
351:     const override = macOverrides[command.id];
352:     if (override) command.chord = override;
353:   }
354: }
355: 
356: if (isMac()) applyMacChords(commands);
357: 
358: const byId = new Map(commands.map((command) => [command.id, command]));
359: 
360: export function command(id: string): Command | undefined {
361:   return byId.get(id);
362: }
363: 
364: /** What the menu and the palette show next to a title. */
365: export function chordOf(id: string): string | undefined {
366:   return byId.get(id)?.chord;
367: }
368: 
369: export function runCommand(id: string): void {
370:   void byId.get(id)?.run();
371: }
372: 
373: /** The palette shows what makes sense right now (spec �4). */
374: export function availableCommands(): Command[] {
375:   return commands.filter((c) => (c.when ? c.when() : true));
376: }

codex
Уже видны пропуски в клавишах: `Ctrl+Tab` превращается в системный `⌘Tab`, zoom остаётся только подсказкой, а Quick Search проверяет физический Control. Проверю native menu и путь сохранения, чтобы отделить подтверждённые дефекты от рисков, требующих запуска на Mac.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$files = @('"'src/app/chords.ts','src/app/keys.ts','src/editor/setup.ts','src/editor/keymap.ts','src/app/paths.ts','src/app/close.ts','src-tauri/tauri.conf.json'); foreach ("'$f in $files) { Write-Output "FILE $f"; $n=0; Get-Content -Encoding utf8 $f | ForEach-Object { $n++; "${n}: $_" } }' in G:\Claude Projects\md reader
 succeeded in 201ms:
FILE src/app/chords.ts
1: // Shortcuts are bound by KeyboardEvent.code so they keep working in a
2: // Russian layout (spec 12). This turns readable chords like "Ctrl+Shift+F"
3: // into a physical-key description, and into a tinykeys pattern.
4: 
5: import { isMac } from "./platform";
6: 
7: export interface Chord {
8:   ctrl: boolean;
9:   shift: boolean;
10:   alt: boolean;
11:   meta: boolean;
12:   code: string;
13: }
14: 
15: export interface KeyLike {
16:   code: string;
17:   ctrlKey: boolean;
18:   shiftKey: boolean;
19:   altKey: boolean;
20:   metaKey: boolean;
21: }
22: 
23: const PUNCT: Record<string, string> = {
24:   "/": "Slash",
25:   "\\": "Backslash",
26:   "[": "BracketLeft",
27:   "]": "BracketRight",
28:   ",": "Comma",
29:   ".": "Period",
30:   ";": "Semicolon",
31:   "'": "Quote",
32:   "`": "Backquote",
33:   "-": "Minus",
34:   "=": "Equal",
35: };
36: 
37: const NAMED: Record<string, string> = {
38:   space: "Space",
39:   enter: "Enter",
40:   return: "Enter",
41:   esc: "Escape",
42:   escape: "Escape",
43:   tab: "Tab",
44:   backspace: "Backspace",
45:   delete: "Delete",
46:   del: "Delete",
47:   insert: "Insert",
48:   up: "ArrowUp",
49:   down: "ArrowDown",
50:   left: "ArrowLeft",
51:   right: "ArrowRight",
52:   home: "Home",
53:   end: "End",
54:   pageup: "PageUp",
55:   pagedown: "PageDown",
56: };
57: 
58: /** Maps one chord token to a KeyboardEvent.code. */
59: export function toCode(token: string): string {
60:   if (/^[a-z]$/i.test(token)) return "Key" + token.toUpperCase();
61:   if (/^[0-9]$/.test(token)) return "Digit" + token;
62:   if (/^f([1-9]|1[0-9]|2[0-4])$/i.test(token)) return "F" + token.slice(1);
63:   const punct = PUNCT[token];
64:   if (punct) return punct;
65:   const named = NAMED[token.toLowerCase()];
66:   if (named) return named;
67:   // Already a code ("Backquote", "NumpadAdd", ...).
68:   if (/^[A-Z][A-Za-z0-9]*$/.test(token)) return token;
69:   throw new Error(`unknown key token: ${token}`);
70: }
71: 
72: /**
73:  * Modifier vocabulary (spec 13a):
74:  *
75:  * - `Ctrl` - the platform's primary modifier: `Ctrl` on Windows, ? on macOS.
76:  *   Almost every chord in the registry is written with it.
77:  * - `Cmd` / `Meta` / `Win` - literally the meta key, on both platforms.
78:  * - `Control` - literally the control key, on both. Only macOS needs it, for
79:  *   the handful of system chords that really are ? (fullscreen).
80:  * - `Alt` / `Option` - ? on macOS.
81:  */
82: export function parseChord(chord: string): Chord {
83:   const parts = chord.split("+").map((p) => p.trim());
84:   const last = parts.pop();
85:   if (!last) throw new Error(`empty chord: ${chord}`);
86: 
87:   const out: Chord = {
88:     ctrl: false,
89:     shift: false,
90:     alt: false,
91:     meta: false,
92:     code: toCode(last),
93:   };
94:   for (const part of parts) {
95:     switch (part.toLowerCase()) {
96:       case "ctrl":
97:       case "mod":
98:         if (isMac()) out.meta = true;
99:         else out.ctrl = true;
100:         break;
101:       case "control":
102:         out.ctrl = true;
103:         break;
104:       case "shift":
105:         out.shift = true;
106:         break;
107:       case "alt":
108:       case "option":
109:         out.alt = true;
110:         break;
111:       case "meta":
112:       case "cmd":
113:       case "win":
114:         out.meta = true;
115:         break;
116:       default:
117:         throw new Error(`unknown modifier: ${part}`);
118:     }
119:   }
120:   return out;
121: }
122: 
123: /* --------------------------------------------------------------- display */
124: 
125: /** `KeyB` -> `B`, `Comma` -> `,`, `ArrowLeft` -> ``, for the macOS glyphs. */
126: const KEY_GLYPHS: Record<string, string> = {
127:   ArrowLeft: "",
128:   ArrowRight: "",
129:   ArrowUp: "",
130:   ArrowDown: "",
131:   Enter: "?",
132:   Tab: "?",
133:   Escape: "?",
134:   Backspace: "?",
135:   Delete: "?",
136:   Space: "space",
137: };
138: 
139: function keyLabel(code: string): string {
140:   const glyph = KEY_GLYPHS[code];
141:   if (glyph) return glyph;
142:   if (code.startsWith("Key")) return code.slice(3);
143:   if (code.startsWith("Digit")) return code.slice(5);
144:   if (/^F\d+$/.test(code)) return code;
145:   for (const [punct, name] of Object.entries(PUNCT)) {
146:     if (name === code) return punct;
147:   }
148:   return code;
149: }
150: 
151: /**
152:  * How a chord is written in the menu, the palette and the shortcuts screen.
153:  * Windows spells it out in the app's lowercase microcopy (`ctrl shift s`);
154:  * macOS uses the glyphs in Apple's order, ????, with nothing between them.
155:  */
156: export function chordText(chord: string): string {
157:   if (!isMac()) {
158:     return chord
159:       .split("+")
160:       .map((part) => part.trim().toLowerCase())
161:       .join(" ");
162:   }
163:   const c = parseChord(chord);
164:   return (
165:     (c.ctrl ? "?" : "") +
166:     (c.alt ? "?" : "") +
167:     (c.shift ? "?" : "") +
168:     (c.meta ? "?" : "") +
169:     keyLabel(c.code)
170:   );
171: }
172: 
173: /** Exact match, including "no other modifier is held". */
174: export function matchesChord(chord: string | Chord, event: KeyLike): boolean {
175:   const c = typeof chord === "string" ? parseChord(chord) : chord;
176:   return (
177:     event.code === c.code &&
178:     event.ctrlKey === c.ctrl &&
179:     event.shiftKey === c.shift &&
180:     event.altKey === c.alt &&
181:     event.metaKey === c.meta
182:   );
183: }
184: 
185: /**
186:  * tinykeys pattern for the same chord. tinykeys compares the last token
187:  * against event.code as-is, so passing "KeyF" binds the physical key.
188:  */
189: export function chordToPattern(chord: string): string {
190:   const c = parseChord(chord);
191:   const mods: string[] = [];
192:   if (c.ctrl) mods.push("Control");
193:   if (c.alt) mods.push("Alt");
194:   if (c.shift) mods.push("Shift");
195:   if (c.meta) mods.push("Meta");
196:   return [...mods, c.code].join("+");
197: }
FILE src/app/keys.ts
1: // One keyboard layer for the whole app. There are no chords here: every
2: // binding comes from the command registry (spec 12), so a shortcut and the
3: // menu entry that shows it can never drift apart. Tree keys (`F2`, `Delete`,
4: // the arrows) are local to the tree and live in the rail.
5: 
6: import { tinykeys } from "tinykeys";
7: import { chordToPattern, matchesChord } from "./chords";
8: import { commands } from "./registry";
9: 
10: /**
11:  * WebView2 keeps its own accelerators (spec 12 leaves them on for zoom), but
12:  * these must not reach it - they are ours, or they would reload or print.
13:  */
14: const SWALLOW = [
15:   "Ctrl+F",
16:   "Ctrl+P",
17:   "Ctrl+G",
18:   "F5",
19:   "Ctrl+S",
20:   "Ctrl+Shift+S",
21:   "Ctrl+N",
22:   "Ctrl+O",
23:   "Ctrl+H",
24:   "Ctrl+K",
25:   "F1",
26: ];
27: 
28: /** chord -> action, straight out of the registry. */
29: export function bindings(): Record<string, () => void> {
30:   const map: Record<string, () => void> = {};
31:   for (const command of commands) {
32:     for (const chord of [command.chord, ...(command.chords ?? [])]) {
33:       if (chord) map[chord] = () => void command.run();
34:     }
35:   }
36:   return map;
37: }
38: 
39: export function installKeys(): () => void {
40:   const swallow = (event: KeyboardEvent) => {
41:     if (SWALLOW.some((chord) => matchesChord(chord, event))) event.preventDefault();
42:   };
43:   window.addEventListener("keydown", swallow, { capture: true });
44: 
45:   // WebView2's own menu is suppressed everywhere except the editor, where it
46:   // is the only way to reach cut/copy/paste and the spelling suggestions
47:   // Windows offers. Read has its own menu, which stops this event itself.
48:   const noMenu = (event: MouseEvent) => {
49:     const target = event.target as HTMLElement | null;
50:     if (target?.closest(".cm-editor")) return;
51:     event.preventDefault();
52:   };
53:   window.addEventListener("contextmenu", noMenu);
54: 
55:   const map: Record<string, (event: KeyboardEvent) => void> = {};
56:   for (const [chord, run] of Object.entries(bindings())) {
57:     map[chordToPattern(chord)] = (event) => {
58:       event.preventDefault();
59:       run();
60:     };
61:   }
62: 
63:   const off = tinykeys(window, map, {
64:     // Shortcuts must keep working while the editor (contenteditable) has
65:     // focus, so only auto-repeat and IME composition are skipped.
66:     ignore: (event) => event.repeat || event.isComposing,
67:   });
68: 
69:   return () => {
70:     window.removeEventListener("keydown", swallow, { capture: true });
71:     window.removeEventListener("contextmenu", noMenu);
72:     off();
73:   };
74: }
FILE src/editor/setup.ts
1: // One place that assembles the CodeMirror extensions. The only compartment
2: // is the line-number gutter, which Ctrl+Shift+9 flips while the app runs.
3: 
4: import { closeBrackets, closeBracketsKeymap } from "@codemirror/autocomplete";
5: import { defaultKeymap, history, historyKeymap } from "@codemirror/commands";
6: import {
7:   deleteMarkupBackward,
8:   insertNewlineContinueMarkupCommand,
9:   markdown,
10:   markdownLanguage,
11: } from "@codemirror/lang-markdown";
12: import { languages } from "@codemirror/language-data";
13: import { codeFolding, indentUnit, syntaxHighlighting, syntaxTree } from "@codemirror/language";
14: import type { SyntaxNode } from "@lezer/common";
15: import { search, searchKeymap } from "@codemirror/search";
16: import { commands } from "../app/registry";
17: import { parseChord, toCode, type Chord } from "../app/chords";
18: import { isMac } from "../app/platform";
19: import {
20:   Compartment,
21:   EditorState,
22:   Prec,
23:   RangeSetBuilder,
24:   countColumn,
25:   type Extension,
26: } from "@codemirror/state";
27: import {
28:   Decoration,
29:   EditorView,
30:   ViewPlugin,
31:   drawSelection,
32:   dropCursor,
33:   keymap,
34:   lineNumbers,
35:   type DecorationSet,
36:   type KeyBinding,
37:   type ViewUpdate,
38: } from "@codemirror/view";
39: import type { Settings } from "../app/settings";
40: import type { Doc } from "../app/store";
41: import { activeDoc, useStore } from "../app/store";
42: import { detectIndent, urlPaste } from "./commands";
43: import { pasteImage } from "./images";
44: import { editorKeymap } from "./keymap";
45: import { findPanel } from "./findPanel";
46: import { remoteCommands } from "./remote";
47: import { richConf } from "./rich";
48: import { storeSync } from "./sync";
49: import { formatToolbar } from "./toolbar";
50: import { editorTheme, highlightStyle } from "./theme";
51: 
52: export const gutterConf = new Compartment();
53: /** Save As turns a read-only copy into an editable file (review #5). */
54: export const readOnlyConf = new Compartment();
55: export const spellConf = new Compartment();
56: 
57: /**
58:  * Windows own dictionaries do the checking; no `lang` is set, so WebView2
59:  * picks the one the text looks like (spec 2a).
60:  */
61: export function spellExtension(on: boolean): Extension {
62:   return EditorView.contentAttributes.of({ spellcheck: on ? "true" : "false" });
63: }
64: 
65: /* ------------------------------------------------- soft wrap continuation */
66: 
67: // Leading blockquote/list markup, so a wrapped line lines up with its text.
68: const MARKUP = /^(?:[ \t]*(?:>[ \t]?|[-*+][ \t]+|\d+[.)][ \t]+))*[ \t]*/;
69: 
70: /** Last line of a leading `---` block; 0 when the file has no front matter. */
71: function frontMatterEnd(state: EditorState): number {
72:   if (state.doc.lines < 2 || !/^---\s*$/.test(state.doc.line(1).text)) return 0;
73:   const limit = Math.min(state.doc.lines, 200);
74:   for (let n = 2; n <= limit; n += 1) {
75:     if (/^(---|\.\.\.)\s*$/.test(state.doc.line(n).text)) return n;
76:   }
77:   return 0;
78: }
79: 
80: /** Fenced code, so the whole block gets one background like read does. */
81: function inCode(state: EditorState, pos: number): boolean {
82:   for (let node: SyntaxNode | null = syntaxTree(state).resolveInner(pos, 1); node; node = node.parent) {
83:     if (node.name === "FencedCode" || node.name === "CodeBlock") return true;
84:   }
85:   return false;
86: }
87: 
88: function lineDecorations(view: EditorView): DecorationSet {
89:   const builder = new RangeSetBuilder<Decoration>();
90:   const { state } = view;
91:   const frontMatter = frontMatterEnd(state);
92:   for (const { from, to } of view.visibleRanges) {
93:     for (let pos = from; pos <= to; ) {
94:       const line = state.doc.lineAt(pos);
95:       const markup = MARKUP.exec(line.text)?.[0] ?? "";
96:       const width = markup ? Math.min(countColumn(markup, state.tabSize), 24) : 0;
97:       const cls =
98:         line.number <= frontMatter
99:           ? "cm-frontmatter"
100:           : inCode(state, line.from)
101:             ? "cm-code"
102:             : "";
103:       if (width > 0 || cls) {
104:         builder.add(
105:           line.from,
106:           line.from,
107:           Decoration.line({
108:             class: cls || undefined,
109:             attributes:
110:               width > 0
111:                 ? { style: `text-indent:-${width}ch;padding-left:${width}ch` }
112:                 : undefined,
113:           }),
114:         );
115:       }
116:       pos = line.to + 1;
117:     }
118:   }
119:   return builder.finish();
120: }
121: 
122: const lineLook = ViewPlugin.fromClass(
123:   class {
124:     decorations: DecorationSet;
125:     constructor(view: EditorView) {
126:       this.decorations = lineDecorations(view);
127:     }
128:     update(update: ViewUpdate) {
129:       if (update.docChanged || update.viewportChanged) {
130:         this.decorations = lineDecorations(update.view);
131:       }
132:     }
133:   },
134:   { decorations: (v) => v.decorations },
135: );
136: 
137: /* ----------------------------------------------------- line numbers (12) */
138: 
139: // Ctrl+Shift+9 flips them for the session; settings.json is the starting point.
140: let numbersOverride: boolean | null = null;
141: const numbersListeners = new Set<() => void>();
142: 
143: export function lineNumbersOn(): boolean {
144:   return numbersOverride ?? useStore.getState().settings.edit.lineNumbers;
145: }
146: 
147: export function toggleLineNumbers(): void {
148:   numbersOverride = !lineNumbersOn();
149:   for (const listener of numbersListeners) listener();
150: }
151: 
152: export function onLineNumbers(listener: () => void): () => void {
153:   numbersListeners.add(listener);
154:   return () => numbersListeners.delete(listener);
155: }
156: 
157: /**
158:  * The settings screen moved the base, so the session override is stale - the
159:  * setting is what the user just looked at (spec 10).
160:  */
161: export function syncLineNumbers(): void {
162:   numbersOverride = null;
163:   for (const listener of numbersListeners) listener();
164: }
165: 
166: export function gutterExtension(on: boolean): Extension {
167:   return on ? lineNumbers() : [];
168: }
169: 
170: /* ------------------------------------------------------------- assembling */
171: 
172: const reducedMotion =
173:   typeof window !== "undefined" &&
174:   window.matchMedia("(prefers-reduced-motion: reduce)").matches;
175: 
176: // Enter continues the list, the quote or the checkbox; on an empty item it
177: // takes a level off instead of loosening the list (spec 5.2).
178: const markupKeymap = Prec.high(
179:   keymap.of([
180:     { key: "Enter", run: insertNewlineContinueMarkupCommand({ nonTightLists: false }) },
181:     { key: "Backspace", run: deleteMarkupBackward },
182:   ]),
183: );
184: 
185: /* ------------------------------------------------------------- key sharing */
186: 
187: /**
188:  * Whatever the command registry binds is the registry's (spec 12): leaving
189:  * the same chord in CodeMirror's own keymaps runs it twice, once through
190:  * tinykeys and once here (review #12, #16, #8). The list is taken from the
191:  * registry itself, so a new chord there cannot go on to collide quietly.
192:  */
193: function chordKey(chord: Chord): string {
194:   return `${chord.ctrl ? 1 : 0}${chord.shift ? 1 : 0}${chord.alt ? 1 : 0}${chord.meta ? 1 : 0}:${chord.code}`;
195: }
196: 
197: /** CodeMirror spells them `Mod-f`, `Shift-Alt-ArrowUp`, `F3`. */
198: function cmChord(key: string): Chord | null {
199:   const parts = key.split("-");
200:   const last = parts.pop() || (parts.length > 0 ? "-" : "");
201:   if (!last) return null;
202:   const chord: Chord = { ctrl: false, shift: false, alt: false, meta: false, code: "" };
203:   for (const part of parts) {
204:     // CodeMirror's `Mod` is ? on macOS and Ctrl everywhere else - the same
205:     // rule the registry's `Ctrl` follows (spec 13a).
206:     if (part === "Mod") {
207:       if (isMac()) chord.meta = true;
208:       else chord.ctrl = true;
209:     } else if (part === "Ctrl" || part === "Control") chord.ctrl = true;
210:     else if (part === "Shift") chord.shift = true;
211:     else if (part === "Alt") chord.alt = true;
212:     else if (part === "Meta" || part === "Cmd") chord.meta = true;
213:     else if (part !== "") return null;
214:   }
215:   try {
216:     chord.code = toCode(last);
217:   } catch {
218:     return null;
219:   }
220:   return chord;
221: }
222: 
223: let shared: readonly KeyBinding[] | null = null;
224: 
225: function appKeymap(): readonly KeyBinding[] {
226:   if (shared) return shared;
227:   const owned = new Set<string>();
228:   for (const command of commands) {
229:     if (!command.chord) continue;
230:     try {
231:       for (const chord of [command.chord, ...(command.chords ?? [])]) {
232:         if (chord) owned.add(chordKey(parseChord(chord)));
233:       }
234:     } catch {
235:       // A chord the app cannot parse cannot collide either.
236:     }
237:   }
238:   const taken = (key: string | undefined, shift: boolean): boolean => {
239:     if (!key) return false;
240:     const chord = cmChord(key);
241:     if (!chord) return false;
242:     return owned.has(chordKey(chord)) || (shift && owned.has(chordKey({ ...chord, shift: true })));
243:   };
244:   shared = [
245:     ...closeBracketsKeymap,
246:     ...historyKeymap,
247:     ...searchKeymap,
248:     ...defaultKeymap,
249:   ].filter(
250:     (binding) =>
251:       !taken(binding.key, binding.shift !== undefined) &&
252:       // Each platform has its own override field in a CM6 binding.
253:       !taken(isMac() ? binding.mac : binding.win, binding.shift !== undefined),
254:   );
255:   return shared;
256: }
257: 
258: /** The first image on the clipboard, if that is what was copied (2a). */
259: function clipboardImage(data: DataTransfer | null): File | null {
260:   if (!data) return null;
261:   for (const item of data.items) {
262:     if (item.kind === "file" && item.type.startsWith("image/")) {
263:       const file = item.getAsFile();
264:       if (file) return file;
265:     }
266:   }
267:   return null;
268: }
269: 
270: /**
271:  * Paste: an image goes into `assets/` and leaves a link behind (spec 2a);
272:  * a URL over selected text becomes a link (5.2). This replaces the language
273:  * pack's own handler so both also work in a file too big for highlighting.
274:  */
275: const pasteLink = EditorView.domEventHandlers({
276:   paste(event, view) {
277:     const image = clipboardImage(event.clipboardData);
278:     if (image) {
279:       const doc = activeDoc(useStore.getState());
280:       if (doc) {
281:         event.preventDefault();
282:         void pasteImage(view, doc, image);
283:         return true;
284:       }
285:     }
286:     const pasted = event.clipboardData?.getData("text/plain");
287:     if (!pasted) return false;
288:     const spec = urlPaste(view.state, pasted);
289:     if (!spec) return false;
290:     event.preventDefault();
291:     view.dispatch(spec);
292:     return true;
293:   },
294: });
295: 
296: /** No language on big files: no parse, no highlighting (spec 8). */
297: function languageExtension(large: boolean): Extension {
298:   if (large) return [];
299:   return [
300:     markdown({
301:       base: markdownLanguage,
302:       codeLanguages: languages,
303:       addKeymap: false,
304:       pasteURLAsLink: false,
305:     }),
306:     markupKeymap,
307:   ];
308: }
309: 
310: export function editorExtensions(doc: Doc, settings: Settings): Extension {
311:   const unit = settings.edit.indentUnit === "tab" ? "\t" : " ".repeat(settings.edit.indentUnit);
312:   return [
313:     editorTheme,
314:     syntaxHighlighting(highlightStyle),
315:     history(),
316:     drawSelection({ cursorBlinkRate: reducedMotion ? 0 : 1100 }),
317:     dropCursor(),
318:     EditorState.allowMultipleSelections.of(true),
319:     EditorView.clickAddsSelectionRange.of((event) => event.altKey),
320:     readOnlyConf.of(EditorState.readOnly.of(doc.readOnly)),
321:     // Microcopy is lowercase latin (spec 1.4); this is CodeMirror's own.
322:     EditorState.phrases.of({ "Go to line": "go to line", go: "go", close: "?" }),
323:     EditorView.lineWrapping,
324:     lineLook,
325:     codeFolding(),
326:     closeBrackets(),
327:     search({ top: true, createPanel: findPanel }),
328:     gutterConf.of(gutterExtension(lineNumbersOn())),
329:     spellConf.of(spellExtension(settings.edit.spellcheck)),
330:     // Rich is the same buffer with the markers hidden; the mode switch just
331:     // reconfigures this (spec 5.3).
332:     richConf.of([]),
333:     indentUnit.of(detectIndent(doc.text, unit)),
334:     languageExtension(doc.large),
335:     pasteLink,
336:     storeSync,
337:     remoteCommands,
338:     formatToolbar,
339:     editorKeymap(),
340:     keymap.of([...appKeymap()]),
341:   ];
342: }
FILE src/editor/keymap.ts
1: // CodeMirror matches key bindings by `key`, which breaks every letter chord
2: // in a Russian layout (spec 12). Our commands go through this thin layer
3: // instead: same chord spelling as the app, matched by KeyboardEvent.code.
4: 
5: import {
6:   deleteLine,
7:   indentLess,
8:   indentMore,
9:   redo,
10:   selectAll,
11:   undo,
12: } from "@codemirror/commands";
13: import { foldCode, unfoldCode } from "@codemirror/language";
14: import { selectNextOccurrence } from "@codemirror/search";
15: import type { Extension } from "@codemirror/state";
16: import { ViewPlugin, type Command } from "@codemirror/view";
17: import { matchesChord, parseChord } from "../app/chords";
18: import {
19:   cycleList,
20:   insertLink,
21:   tabIndent,
22:   toggleBold,
23:   toggleCheckbox,
24:   toggleCodeBlock,
25:   toggleHeading,
26:   toggleInlineCode,
27:   toggleItalic,
28:   toggleQuote,
29: } from "./commands";
30: 
31: export interface CodeBinding {
32:   chord: string;
33:   run: Command;
34: }
35: 
36: /**
37:  * Runs the first binding whose physical chord matches. This listens on the
38:  * editor in the capture phase rather than through CodeMirror's own keydown
39:  * handling, because the app swallows Ctrl+F/Ctrl+G with preventDefault on
40:  * window (spec 12) and CodeMirror skips its handlers once that is set.
41:  */
42: export function codeKeymap(bindings: CodeBinding[]): Extension {
43:   const compiled = bindings.map((b) => ({ chord: parseChord(b.chord), run: b.run }));
44:   return ViewPlugin.define((view) => {
45:     const onKeydown = (event: KeyboardEvent) => {
46:       if (event.isComposing) return;
47:       // The find panel has its own keys.
48:       if ((event.target as HTMLElement | null)?.closest(".cm-panels")) return;
49:       for (const binding of compiled) {
50:         if (!matchesChord(binding.chord, event)) continue;
51:         if (binding.run(view)) event.preventDefault();
52:         return;
53:       }
54:     };
55:     view.dom.addEventListener("keydown", onKeydown, true);
56:     return { destroy: () => view.dom.removeEventListener("keydown", onKeydown, true) };
57:   });
58: }
59: 
60: /** Chords the app layer owns; CodeMirror must keep its hands off them. */
61: const yieldToApp: Command = () => true;
62: 
63: export function editorKeymap(): Extension {
64:   return codeKeymap([
65:     // formatting (5.2)
66:     { chord: "Ctrl+B", run: toggleBold },
67:     { chord: "Ctrl+I", run: toggleItalic },
68:     { chord: "Ctrl+E", run: toggleInlineCode },
69:     { chord: "Ctrl+Shift+`", run: toggleCodeBlock },
70:     { chord: "Ctrl+Shift+K", run: insertLink },
71:     { chord: "Ctrl+Shift+Q", run: toggleQuote },
72:     { chord: "Ctrl+Shift+L", run: cycleList },
73:     { chord: "Ctrl+Enter", run: toggleCheckbox },
74:     { chord: "Ctrl+1", run: toggleHeading(1) },
75:     { chord: "Ctrl+2", run: toggleHeading(2) },
76:     { chord: "Ctrl+3", run: toggleHeading(3) },
77:     { chord: "Ctrl+4", run: toggleHeading(4) },
78:     { chord: "Ctrl+5", run: toggleHeading(5) },
79:     { chord: "Ctrl+6", run: toggleHeading(6) },
80:     { chord: "Ctrl+]", run: indentMore },
81:     { chord: "Ctrl+[", run: indentLess },
82:     { chord: "Tab", run: tabIndent },
83:     { chord: "Shift+Tab", run: indentLess },
84: 
85:     // editing (12)
86:     { chord: "Ctrl+Z", run: undo },
87:     { chord: "Ctrl+Shift+Z", run: redo },
88:     { chord: "Ctrl+Y", run: redo },
89:     { chord: "Ctrl+A", run: selectAll },
90:     { chord: "Ctrl+D", run: selectNextOccurrence },
91:     { chord: "Ctrl+Shift+X", run: deleteLine },
92:     { chord: "Ctrl+Shift+[", run: foldCode },
93:     { chord: "Ctrl+Shift+]", run: unfoldCode },
94: 
95:     // Find, replace and go-to-line are app commands now: the registry owns
96:     // their chords and remote.ts answers the events (spec 12).
97: 
98:     // Ctrl+/ is the app's read/edit switch, not CodeMirror's comment toggle.
99:     { chord: "Ctrl+/", run: yieldToApp },
100:   ]);
101: }
FILE src/app/paths.ts
1: // Windows paths, but separators are normalised both ways: dialogs return
2: // backslashes, hand-written config may not.
3: 
4: import type { Doc } from "./store";
5: 
6: export function basename(path: string): string {
7:   const parts = path.split(/[\\/]/).filter(Boolean);
8:   return parts[parts.length - 1] ?? path;
9: }
10: 
11: export function dirname(path: string): string {
12:   const index = Math.max(path.lastIndexOf("\\"), path.lastIndexOf("/"));
13:   return index > 0 ? path.slice(0, index) : "";
14: }
15: 
16: /**
17:  * Identity of a file, used to keep one buffer per file. Feed it the path
18:  * Rust canonicalized (`read_file` returns one, `canonical_path` makes one):
19:  * that is what expands an 8.3 name like `KOTENO~1` and settles the case the
20:  * disk really uses. This only finishes the job - separator and case - so a
21:  * raw path from a dialog or a link still keys consistently with itself.
22:  */
23: export function pathKey(path: string): string {
24:   return path.replace(/\\/g, "/").replace(/\/+$/, "").toLowerCase();
25: }
26: 
27: function isUnder(path: string, root: string): boolean {
28:   const a = path.replace(/\\/g, "/").toLowerCase();
29:   const b = root.replace(/\\/g, "/").replace(/\/$/, "").toLowerCase();
30:   return a.startsWith(b + "/");
31: }
32: 
33: /** `folder / file.md` - first crumb is the root and is drawn bolder. */
34: export function breadcrumbs(libraryPath: string | null, doc: Doc | null): string[] {
35:   if (!doc) return libraryPath ? [basename(libraryPath)] : [];
36:   if (!doc.path) return [doc.title];
37:   if (libraryPath && isUnder(doc.path, libraryPath)) {
38:     const rel = doc.path.slice(libraryPath.length).replace(/^[\\/]+/, "");
39:     return [basename(libraryPath), ...rel.split(/[\\/]/).filter(Boolean)];
40:   }
41:   const parts = doc.path.split(/[\\/]/).filter(Boolean);
42:   return parts.slice(-2);
43: }
44: 
45: /**
46:  * The same crumbs, split into the three parts the mode bar lays out: the
47:  * root and the file name keep their width, and everything between them is
48:  * one cell that gives its width up first when the window is narrow.
49:  */
50: export interface Crumbs {
51:   root: string;
52:   /** Folders between the root and the file; often empty. */
53:   middle: string[];
54:   /** The file; null when only the library is open. */
55:   leaf: string | null;
56: }
57: 
58: export function crumbParts(libraryPath: string | null, doc: Doc | null): Crumbs | null {
59:   const parts = breadcrumbs(libraryPath, doc);
60:   const root = parts[0];
61:   if (root === undefined) return null;
62:   if (parts.length === 1) return { root, middle: [], leaf: null };
63:   return { root, middle: parts.slice(1, -1), leaf: parts[parts.length - 1] ?? null };
64: }
FILE src/app/close.ts
1: // Closing a document or the window. Unsaved work is never dropped without
2: // one explicit answer, and the answer has to still be true when the buffer
3: // is actually thrown away (spec 8).
4: 
5: import { getCurrentWindow } from "@tauri-apps/api/window";
6: import { dropBuffer, flushActiveEditor, isDirty } from "../editor";
7: import { draftsSettled, dropDraft } from "./drafts";
8: import { inTauri } from "./env";
9: import { save } from "./save";
10: import { toSession, writeSession } from "./session";
11: import { flushSettings } from "./settings";
12: import { useStore, type Doc } from "./store";
13: 
14: /** Always measured on live editor text, never on a stale `Doc`. */
15: function unsaved(ids: string[]): Doc[] {
16:   flushActiveEditor();
17:   return useStore
18:     .getState()
19:     .docs.filter((d) => ids.includes(d.id) && isDirty(d.id, d.text));
20: }
21: 
22: /**
23:  * Drops the buffers and their drafts. Resolves once the drafts are really
24:  * gone: the window may not close while a delete is still in flight, or the
25:  * next start offers back work the user just discarded.
26:  */
27: async function forget(ids: string[]): Promise<void> {
28:   const store = useStore.getState();
29:   const going = ids
30:     .map((id) => store.docs.find((d) => d.id === id))
31:     .filter((d): d is Doc => d !== undefined);
32: 
33:   for (const id of ids) {
34:     store.closeDoc(id);
35:     dropBuffer(id);
36:   }
37:   // There is one banner slot and it belonged to a document that is gone.
38:   store.dismissBanner("conflict");
39:   store.dismissBanner("save-failed");
40: 
41:   await Promise.all(going.map((d) => dropDraft(d)));
42:   await draftsSettled();
43: }
44: 
45: interface CloseOptions {
46:   /** After everything is saved and agreed, before the documents go. */
47:   ready?: () => void;
48:   /** Once they are really gone and their drafts with them. */
49:   done?: () => void;
50: }
51: 
52: /**
53:  * Closes the documents, asking once for all of them if any is unsaved.
54:  * `done` runs only when everything really closed - it is how the window
55:  * close request knows it may go through.
56:  */
57: export function closeDocs(ids: string[], options: CloseOptions | (() => void) = {}): void {
58:   // A bare callback is the common case (the tree's delete, the rail's ?).
59:   const hooks: CloseOptions = typeof options === "function" ? { done: options } : options;
60:   const finish = (targets: string[]) => {
61:     hooks.ready?.();
62:     void forget(targets).then(() => hooks.done?.());
63:   };
64: 
65:   const store = useStore.getState();
66:   const dirty = unsaved(ids);
67:   if (dirty.length === 0) {
68:     finish(ids);
69:     return;
70:   }
71: 
72:   store.setDialog({
73:     title: dirty.length === 1 ? "unsaved changes" : `${dirty.length} files have unsaved changes`,
74:     lines: dirty.map((d) => d.path ?? d.title),
75:     actions: [
76:       {
77:         label: "save",
78:         run: () => {
79:           store.setDialog(null);
80:           void (async () => {
81:             const before = new Set(useStore.getState().docs.map((d) => d.id));
82:             for (const item of dirty) {
83:               // A failed save leaves its own banner up and stops the close.
84:               if (!(await save(item.id))) return;
85:             }
86:             // Save As gave a nameless buffer a new id; it is the same
87:             // document, so it is still one of the ones being closed.
88:             const renamed = useStore
89:               .getState()
90:               .docs.filter((d) => !before.has(d.id))
91:               .map((d) => d.id);
92:             const targets = [...new Set([...ids, ...renamed])];
93:             // The saves took time and the editor stayed usable. Nothing typed
94:             // in the meantime was ever agreed to, so it is asked about again
95:             // rather than thrown away (spec 8).
96:             if (unsaved(targets).length > 0) {
97:               closeDocs(targets, hooks);
98:               return;
99:             }
100:             finish(targets);
101:           })();
102:         },
103:       },
104:       {
105:         label: "don't save",
106:         run: () => {
107:           store.setDialog(null);
108:           finish(ids);
109:         },
110:       },
111:       { label: "cancel", run: () => store.setDialog(null) },
112:     ],
113:     cancel: () => store.setDialog(null),
114:   });
115: }
116: 
117: export function closeActive(): void {
118:   const { activeId } = useStore.getState();
119:   if (activeId) closeDocs([activeId]);
120: }
121: 
122: /** The window's X and Alt+F4 go through the same one question. */
123: export function installCloseGuard(): () => void {
124:   if (!inTauri) return () => undefined;
125:   let leaving = false;
126:   const window = getCurrentWindow();
127:   const unlisten = window.onCloseRequested((event) => {
128:     if (leaving) return;
129:     event.preventDefault();
130:     const ids = useStore.getState().docs.map((d) => d.id);
131:     let session = toSession();
132:     closeDocs(ids, {
133:       // Taken after the saves - a Save As at the door belongs in the next
134:       // session - and before the documents go, since closing them is what
135:       // empties the open list (spec 6, 8).
136:       ready: () => {
137:         flushActiveEditor();
138:         session = toSession();
139:       },
140:       done: () => {
141:         leaving = true;
142:         void Promise.all([writeSession(session), flushSettings()]).then(() => window.destroy());
143:       },
144:     });
145:   });
146:   return () => void unlisten.then((off) => off());
147: }
FILE src-tauri/tauri.conf.json
1: {
2:   "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
3:   "productName": "Plain",
4:   "version": "0.2.0",
5:   "identifier": "Plain",
6:   "build": {
7:     "beforeDevCommand": "npm run dev",
8:     "devUrl": "http://localhost:1420",
9:     "beforeBuildCommand": "npm run build",
10:     "frontendDist": "../dist"
11:   },
12:   "app": {
13:     "windows": [
14:       {
15:         "label": "main",
16:         "title": "Plain",
17:         "width": 1100,
18:         "height": 720,
19:         "minWidth": 480,
20:         "minHeight": 400,
21:         "decorations": true,
22:         "resizable": true,
23:         "dragDropEnabled": true
24:       }
25:     ],
26:     "security": {
27:       "csp": "default-src 'self'; img-src 'self' asset: http://asset.localhost https: data: blob:; style-src 'self' 'unsafe-inline'; font-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost ws://localhost:1420",
28:       "assetProtocol": {
29:         "enable": true,
30:         "scope": []
31:       }
32:     }
33:   },
34:   "bundle": {
35:     "active": true,
36:     "targets": [],
37:     "icon": ["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.ico", "icons/icon.png"],
38:     "fileAssociations": [
39:       {
40:         "ext": ["md", "markdown"],
41:         "name": "Markdown",
42:         "description": "Markdown document",
43:         "role": "Editor"
44:       }
45:     ],
46:     "macOS": {
47:       "minimumSystemVersion": "13.0"
48:     }
49:   }
50: }

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$f='"'src-tauri/src/fs.rs'; "'$n=0; Get-Content -Encoding utf8 $f | ForEach-Object { $n++; if ($n -ge 180 -and $n -le 590) { "${n}: $_" } }; Get-Content -Encoding utf8 ТЗ-Plain.md | Select-Object -Skip 125 -First 28; rg -n '"'print|commit\\(|stage\\(|readonly|to_lowercase' src-tauri/src/history.rs src/app/commands.ts src-tauri/src/fs.rs; git -c safe.directory='G:/Claude Projects/md reader' status --short; git -c safe.directory='G:/Claude Projects/md reader' rev-parse HEAD" in G:\Claude Projects\md reader
 succeeded in 268ms:
180:     (eol, dominant)
181: }
182: 
183: /* ------------------------------------------------------ one path per file */
184: 
185: /// Elsewhere `canonicalize` already hands back an ordinary path, and a
186: /// backslash is a legal character in a POSIX file name - so nothing is
187: /// stripped there (spec 13a).
188: #[cfg(not(windows))]
189: fn plain_path(path: PathBuf) -> PathBuf {
190:     path
191: }
192: 
193: /// `fs::canonicalize` hands back a verbatim path (`\\?\C:\:`) that no user
194: /// ever types and half of Windows will not take. This puts it back into the
195: /// ordinary form without giving up what canonicalizing found.
196: #[cfg(windows)]
197: fn plain_path(path: PathBuf) -> PathBuf {
198:     let text = path.as_os_str().to_string_lossy();
199:     if let Some(rest) = text.strip_prefix(r"\\?\UNC\") {
200:         return PathBuf::from(format!(r"\\{rest}"));
201:     }
202:     if let Some(rest) = text.strip_prefix(r"\\?\") {
203:         // A drive letter is safe to unwrap; a device path is left alone.
204:         let mut start = rest.chars();
205:         if matches!((start.next(), start.next()), (Some(letter), Some(':')) if letter.is_ascii_alphabetic())
206:         {
207:             return PathBuf::from(rest);
208:         }
209:     }
210:     path
211: }
212: 
213: /// The spelling the disk itself uses: 8.3 aliases expanded, links followed,
214: /// the real case. One file must have one identity or it gets two buffers
215: /// (spec 8). A file that does not exist yet - Save As onto a new name - is
216: /// resolved through its folder instead.
217: pub fn canonical(path: &Path) -> PathBuf {
218:     if let Ok(found) = fs::canonicalize(path) {
219:         return plain_path(found);
220:     }
221:     let (Some(parent), Some(name)) = (path.parent(), path.file_name()) else {
222:         return path.to_path_buf();
223:     };
224:     if parent.as_os_str().is_empty() {
225:         return path.to_path_buf();
226:     }
227:     match fs::canonicalize(parent) {
228:         Ok(folder) => plain_path(folder).join(name),
229:         Err(_) => path.to_path_buf(),
230:     }
231: }
232: 
233: /// For the paths that do not come back through `read_file`: the Save As
234: /// dialog, and anything else that hands us a name the user typed.
235: #[tauri::command]
236: pub fn canonical_path(path: String) -> String {
237:     canonical(Path::new(&path)).to_string_lossy().into_owned()
238: }
239: 
240: fn mtime_ms(meta: &fs::Metadata) -> f64 {
241:     meta.modified()
242:         .ok()
243:         .and_then(|time| time.duration_since(UNIX_EPOCH).ok())
244:         .map(|since| since.as_millis() as f64)
245:         .unwrap_or(0.0)
246: }
247: 
248: /// The text comes back exactly as it is on disk, CRLF and all: CodeMirror
249: /// normalizes it, and the front end keeps `eol` to put it back.
250: pub fn read_info(path: &Path, forced: Option<&str>) -> FsResult<FileInfo> {
251:     let bytes = fs::read(path)?;
252:     let meta = fs::metadata(path)?;
253:     let (encoding, skip) = detect(&bytes, forced);
254:     let (text, decode_errors) = encoding.decode_without_bom_handling(&bytes[skip..]);
255:     let (eol, dominant) = line_endings(&text);
256:     Ok(FileInfo {
257:         path: canonical(path).to_string_lossy().into_owned(),
258:         decode_errors,
259:         final_newline: text.ends_with('\n') || text.ends_with('\r'),
260:         eol: eol.into(),
261:         dominant_eol: dominant.into(),
262:         encoding: encoding.name().to_ascii_lowercase(),
263:         bom: skip > 0,
264:         hash: blake3::hash(&bytes).to_hex().to_string(),
265:         mtime_ms: mtime_ms(&meta),
266:         size: meta.len(),
267:         read_only: meta.permissions().readonly(),
268:         text: text.into_owned(),
269:     })
270: }
271: 
272: #[tauri::command]
273: pub fn read_file(path: String, encoding: Option<String>) -> FsResult<FileInfo> {
274:     read_info(Path::new(&path), encoding.as_deref())
275: }
276: 
277: /// `None` means the file is not there. Access denied or a network hiccup is
278: /// an error, not a deletion - the front end must not say `deleted on disk`
279: /// because a read failed (spec 8).
280: #[tauri::command]
281: pub fn hash_file(path: String) -> FsResult<Option<String>> {
282:     match fs::read(&path) {
283:         Ok(bytes) => Ok(Some(blake3::hash(&bytes).to_hex().to_string())),
284:         Err(error) if error.kind() == std::io::ErrorKind::NotFound => Ok(None),
285:         Err(error) => Err(FsError::io(error.to_string())),
286:     }
287: }
288: 
289: /* --------------------------------------------------------------- writing */
290: 
291: /// encoding_rs turns the two UTF-16 encodings into UTF-8 on the way out, so
292: /// they are done by hand. A legacy encoding that cannot hold the text is a
293: /// refusal: writing `&#1234;` in its place would corrupt the file (spec 8).
294: fn encode(text: &str, label: &str) -> FsResult<Vec<u8>> {
295:     match label {
296:         "utf-16le" => Ok(text.encode_utf16().flat_map(u16::to_le_bytes).collect()),
297:         "utf-16be" => Ok(text.encode_utf16().flat_map(u16::to_be_bytes).collect()),
298:         _ => {
299:             let encoding = Encoding::for_label(label.as_bytes()).unwrap_or(UTF_8);
300:             let (bytes, _, had_errors) = encoding.encode(text);
301:             if had_errors {
302:                 return Err(FsError::io(format!(
303:                     "can't encode in {} - convert to utf-8",
304:                     encoding.name().to_ascii_lowercase()
305:                 )));
306:             }
307:             Ok(bytes.into_owned())
308:         }
309:     }
310: }
311: 
312: fn bom_bytes(label: &str) -> &'static [u8] {
313:     match label {
314:         "utf-16le" => &[0xFF, 0xFE],
315:         "utf-16be" => &[0xFE, 0xFF],
316:         _ => &[0xEF, 0xBB, 0xBF],
317:     }
318: }
319: 
320: /// The front end already did this; doing it again keeps a pasted `\n` out of
321: /// a CRLF file. `keep` is for our own files in %APPDATA%.
322: fn apply_eol(text: &str, eol: &str) -> String {
323:     if eol == "keep" {
324:         return text.to_string();
325:     }
326:     let lf = if text.contains('\r') {
327:         text.replace("\r\n", "\n").replace('\r', "\n")
328:     } else {
329:         text.to_string()
330:     };
331:     match eol {
332:         "crlf" => lf.replace('\n', "\r\n"),
333:         "cr" => lf.replace('\n', "\r"),
334:         _ => lf,
335:     }
336: }
337: 
338: #[cfg(windows)]
339: fn wide(path: &Path) -> Vec<u16> {
340:     path.as_os_str().encode_wide().chain(Some(0)).collect()
341: }
342: 
343: /// Writes the bytes into a temp file next to the target and flushes them to
344: /// the platter (`sync_all` is FlushFileBuffers). Creates the folder, so it is
345: /// only ever called once the write is known to be allowed.
346: pub fn stage(dir: &Path, bytes: &[u8]) -> FsResult<PathBuf> {
347:     fs::create_dir_all(dir)?;
348:     let mut temp = tempfile::NamedTempFile::new_in(dir)?;
349:     temp.write_all(bytes)?;
350:     temp.flush()?;
351:     temp.as_file().sync_all()?;
352:     let (file, path) = temp.keep().map_err(|error| FsError::io(error.to_string()))?;
353:     drop(file);
354:     Ok(path)
355: }
356: 
357: #[cfg(windows)]
358: /// `ReplaceFileW` can fail after it has already moved the original aside
359: /// (ERROR_UNABLE_TO_MOVE_REPLACEMENT). If the target is gone, the staged file
360: /// is the only copy of the text left and it goes back under the target name;
361: /// only when the original is still there is the staged file litter.
362: fn recover_replacement(target: &Path, temp: &Path) -> std::io::Result<()> {
363:     if target.exists() {
364:         return match fs::remove_file(temp) {
365:             Err(error) if error.kind() == std::io::ErrorKind::NotFound => Ok(()),
366:             other => other,
367:         };
368:     }
369:     if temp.exists() {
370:         return fs::rename(temp, target);
371:     }
372:     Ok(())
373: }
374: 
375: /// POSIX `rename` is atomic and replaces whatever is there, so one call
376: /// covers both cases. The mode has to be carried over by hand: a temp file is
377: /// created 0600 and the file being replaced may be more permissive (13a).
378: ///
379: /// Compiled on every platform, not only the one that uses it, so a Windows
380: /// build still type-checks it and a Windows test can still run it - there is
381: /// no Mac here to find out on.
382: fn commit_by_rename(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
383:     if existed {
384:         if let Ok(meta) = fs::metadata(target) {
385:             // Best effort: a read-only target would otherwise hand the copy
386:             // permissions the original never had.
387:             let _ = fs::set_permissions(temp, meta.permissions());
388:         }
389:     }
390:     fs::rename(temp, target).map_err(FsError::from)
391: }
392: 
393: #[cfg(not(windows))]
394: pub fn commit(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
395:     commit_by_rename(target, temp, existed)
396: }
397: 
398: /// ReplaceFileW keeps the creation time and the attributes of the original;
399: /// a file that is not there yet is just renamed into place (spec 8).
400: #[cfg(windows)]
401: pub fn commit(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
402:     if !existed {
403:         return commit_by_rename(target, temp, existed);
404:     }
405:     let replaced = wide(target);
406:     let replacement = wide(temp);
407:     let outcome = unsafe {
408:         ReplaceFileW(
409:             PCWSTR(replaced.as_ptr()),
410:             PCWSTR(replacement.as_ptr()),
411:             PCWSTR::null(),
412:             REPLACEFILE_IGNORE_MERGE_ERRORS,
413:             None,
414:             None,
415:         )
416:     };
417:     if let Err(error) = outcome {
418:         let message = error.message();
419:         return Err(match recover_replacement(target, temp) {
420:             Ok(()) => FsError::io(message),
421:             Err(problem) => FsError::io(format!(
422:                 "{message}; the new text is in {} - {problem}",
423:                 temp.display()
424:             )),
425:         });
426:     }
427:     Ok(())
428: }
429: 
430: #[derive(Debug, Deserialize)]
431: #[serde(rename_all = "camelCase")]
432: pub struct WriteRequest {
433:     pub path: String,
434:     pub text: String,
435:     pub encoding: String,
436:     pub bom: bool,
437:     pub eol: String,
438:     /// Hash of the bytes the buffer was loaded from; `null` for Save As.
439:     pub base_hash: Option<String>,
440:     /// The file was deleted from under us and the user wants it back (8).
441:     #[serde(default)]
442:     pub allow_missing: bool,
443:     /// When set, a successful write also leaves a copy in the history folder
444:     /// under this id, at most one every five minutes (spec 2a).
445:     #[serde(default)]
446:     pub snapshot_id: Option<String>,
447: }
448: 
449: #[derive(Debug, Serialize)]
450: #[serde(rename_all = "camelCase")]
451: pub struct WriteResult {
452:     hash: String,
453:     /// The document was written; keeping a copy of it in the history was not.
454:     /// Two different pieces of news, reported separately (review #5).
455:     snapshot_error: Option<String>,
456: }
457: 
458: /// One lock for every write. A personal editor with one window does not need
459: /// per-path locks, and this also keeps two documents in the same folder from
460: /// racing over their temp files.
461: static WRITE_LOCK: Mutex<()> = Mutex::new(());
462: 
463: /// Exactly the bytes a write would put on disk: BOM, the file's own line
464: /// endings, its own encoding. The history keeps a copy of these (spec 2a).
465: pub fn encoded_bytes(request: &WriteRequest) -> FsResult<Vec<u8>> {
466:     let mut bytes = Vec::new();
467:     if request.bom {
468:         bytes.extend_from_slice(bom_bytes(&request.encoding));
469:     }
470:     bytes.extend_from_slice(&encode(
471:         &apply_eol(&request.text, &request.eol),
472:         &request.encoding,
473:     )?);
474:     Ok(bytes)
475: }
476: 
477: pub fn write_checked(request: &WriteRequest) -> FsResult<String> {
478:     write_written(request).map(|(hash, _)| hash)
479: }
480: 
481: /// The hash and the bytes, so the caller can keep a copy without encoding
482: /// the whole document a second time.
483: fn write_written(request: &WriteRequest) -> FsResult<(String, Vec<u8>)> {
484:     let _guard = WRITE_LOCK.lock().unwrap_or_else(|poisoned| poisoned.into_inner());
485:     let path = PathBuf::from(&request.path);
486: 
487:     let bytes = encoded_bytes(request)?;
488: 
489:     let dir = path
490:         .parent()
491:         .filter(|parent| !parent.as_os_str().is_empty())
492:         .ok_or_else(|| FsError::io("no parent folder"))?;
493:     // The file is gone and nobody asked for it back: nothing may be created,
494:     // not even the folder it used to live in. Decided before staging, because
495:     // staging is what would put that folder back (spec 8).
496:     let existed = path.exists();
497:     if !existed && request.base_hash.is_some() && !request.allow_missing {
498:         return Err(FsError::missing(&path));
499:     }
500: 
501:     // Everything past here is a write the caller is allowed to make, so the
502:     // temp file is prepared now: it makes the gap between checking the file's
503:     // hash and replacing it as short as it can be (spec 8).
504:     let temp = stage(dir, &bytes)?;
505: 
506:     let verdict = match (existed, request.base_hash.as_deref()) {
507:         (true, Some(base)) => fs::read(&path).map_err(FsError::from).and_then(|current| {
508:             if blake3::hash(&current).to_hex().to_string() == base {
509:                 Ok(())
510:             } else {
511:                 Err(FsError::conflict())
512:             }
513:         }),
514:         _ => Ok(()),
515:     };
516: 
517:     if let Err(error) = verdict {
518:         let _ = fs::remove_file(&temp);
519:         return Err(error);
520:     }
521: 
522:     commit(&path, &temp, existed)?;
523:     let hash = blake3::hash(&bytes).to_hex().to_string();
524:     Ok((hash, bytes))
525: }
526: 
527: #[tauri::command]
528: pub fn write_file_atomic(app: tauri::AppHandle, request: WriteRequest) -> FsResult<WriteResult> {
529:     let (hash, bytes) = write_written(&request)?;
530:     // Keeping a copy must not fail the save, but it must not be silent about
531:     // failing either (review #5).
532:     let snapshot_error = match request.snapshot_id.as_deref() {
533:         Some(id) => crate::history::keep(&app, id, &bytes)
534:             .err()
535:             .map(|error| error.message().to_string()),
536:         None => None,
537:     };
538:     Ok(WriteResult {
539:         hash,
540:         snapshot_error,
541:     })
542: }
543: 
544: /// Drafts and state.json: same temp-and-replace, always UTF-8, never
545: /// touching the line endings of the JSON we produced ourselves.
546: #[tauri::command]
547: pub fn write_text_atomic(path: String, text: String) -> FsResult<()> {
548:     write_checked(&WriteRequest {
549:         path,
550:         text,
551:         encoding: "utf-8".into(),
552:         bom: false,
553:         eol: "keep".into(),
554:         base_hash: None,
555:         allow_missing: true,
556:         snapshot_id: None,
557:     })
558:     .map(|_| ())
559: }
560: 
561: /// Recycle Bin, not deletion (spec 6).
562: #[tauri::command]
563: pub fn trash_path(path: String) -> FsResult<()> {
564:     trash::delete(&path).map_err(|error| FsError::io(error.to_string()))
565: }
566: 
567: /* ----------------------------------------------------------------- tests */
568: 
569: #[cfg(test)]
570: mod tests {
571:     use super::*;
572:     use encoding_rs::WINDOWS_1251;
573: 
574:     fn request(path: &Path, text: &str) -> WriteRequest {
575:         WriteRequest {
576:             path: path.to_string_lossy().into_owned(),
577:             text: text.into(),
578:             encoding: "utf-8".into(),
579:             bom: false,
580:             eol: "lf".into(),
581:             base_hash: None,
582:             allow_missing: false,
583:             snapshot_id: None,
584:         }
585:     }
586: 
587:     #[test]
588:     fn writes_a_new_file_and_replaces_it() {
589:         let dir = tempfile::tempdir().unwrap();
590:         let file = dir.path().join("note.md");
## 8. ���࠭����� `[v0.1]` - �����⢥��� ࠧ���, ��� �� �������� �� ���४⭮��

- **���࠭���� - ��筮�, `Ctrl+S`.** �����࠭���� - �������� v0.2 (���� ����ன�� ����প�). �� �����⨨ � ����࠭�� - ���� ������ `save � don't save � cancel` � ᯨ᪮� 䠩���.
- **�������� ����ଠ�஢����** (1.3). ����� ���㬥�� �� `Ctrl+S` �� �����뢠����.
- **�⮬�ୠ� ������.** ������ ⥪�� ���� + ��� �����  Rust: temp-䠩� � ⮩ �� �����  `FlushFileBuffers`  `ReplaceFileW` (��࠭�� creation time � ��ਡ���); 䠩�� ��� - `MoveFileExW`. ��। ������� 䠩� �� ��᪥ �������� � �ࠢ�������� � `baseHash`: �� ᮢ��� - �� �����, ������ ���䫨��. **���� ���������** (㤠�� �����) - �⤥��� ��砩: ���� � �ࠢ���� �����뢠���� ��� �஢�ન ���, ����� `recreated`; ���� ���� ��� 䠩�� ��祣� �� ����. ����� ������ ���㬥�� �믮������� ��᫥����⥫쭮. �� �����襭�� �ᯥ譮� ����� `baseHash` **�ᥣ��** ���������� ��襬 ����ᠭ���� ᭨��� (���� ᫥����� ������ ������� ���䫨�� � ᮡ�⢥��� 䠩���); ����� �஭⥭� �ࠢ������ ⥪�騩 ⥪�� � ᭨����: ࠢ�� - `saved`, �୮��� 㤠��; �� ࠢ�� - ������� `unsaved`, �୮��� ������� (� ���� `baseHash`). �訡�� - ������ `couldn't save - <��稭�> � retry � save as:`. ������ in-place ����� ���.
- **`don't save`** �� �����⨨ - � �⪠� �� �ࠢ��: �������� ������ �⬥�����, �୮��� 㤠�����, �� ᫥���饬 ����᪥ ��祣� �� ����⠭����������.
- **����஢��.** �⥭��: BOM  UTF-8  `chardetng`. ������: � ����஢�� 䠩��, �᫨ �� UTF-8 ��� UTF-16 (BOM ��� ��); legacy  �� ��ࢮ� ��࠭���� �ࠢ�� `convert to UTF-8 � save as: � cancel`. EOL - ���������騩 �⨫� 䠩�� (���� - �� `files.newFileEol`, �� 㬮�砭�� CRLF); ᬥ蠭��  ��ଠ������ � ᮮ�饭���. `utf-8` � �����-��� ��������쭮  `reopen as: (utf-8 � cp1251 � utf-16)`.
- **���譨� ���������.** ��� �������� �ࠢ��  ��� ��१���㧪�, �஫� �� �� �� ���������, ����� `reloaded from disk`, ���� �窠 undo. ���� �ࠢ��  ������ `file changed on disk � reload � save my copy as:` (`reload` - � ���⢥ত����� ���� �ࠢ��; diff - �������� v0.2). �஢�ઠ ���譥� ���ᨨ - �� watcher � �������⥫쭮 �� ������ 䮪�� ����.
- **��୮����.** �� ��������� ����  `%APPDATA%\Plain\drafts\<id>.json` (id - ��� ��� ��� id ����ﭭ��� ����): ⥪��, ����, `baseHash`, ����஢��, BOM, EOL, ���⪠. Debounce 500 ��, �� �����뢭�� ����� - �� ०� ࠧ� � 2 �; ������ temp+rename. �������� ⮫쪮 ��᫥ ���⢥ত񭭮�� ��࠭���� �⮣� ⥪��. �訡�� ����� �୮���� - ����ﭭ� ������ `can't write recovery draft`. �� ���� � ������� `drafts/` - ��࠭ `unsaved work found`: `restore � discard` (discard - � ��২��).
- **�����** - `state.json` (������ 䠩��, ��⨢��, ०���, �஫�/���⪠, ����, ३��, ��᫥���� �����, �������� 20, ����樨 �⥭�� - LRU 300) � debounce 2 � � �� �����⨨.
- **����訥 䠩��.** > 2 �� - ���ᢥ⪠ �몫�祭�, read �� ������ `render anyway`; ����� `large file`. ��祣� �� ��१����� ����.
- **Snapshots** `[v0.2]` - ����� 䠩�� � `%APPDATA%\Plain\history\<id>\<ts>.md` �� `Ctrl+S` �� �� ࠧ� � 5 ���, 30 ����; `> version history`  ᯨ᮪  `restore`.

---

## 9. ������� ���⨭�� � ��뫪� `[v0.1]`

������� ���⨭�� - Tauri asset protocol; scope �뤠���� �������᪨ �� ����� ������⥪� ��� ����� ����⮣� 䠩��. ���⨭�� ��� scope (`../images/x.png` � �����筮�� 䠩��)  ���誠 `outside folder � allow` (�������� �� ����� � scope �� ����). ���譨� ���⨭�� - ���誠 � ��⮬ � `load`. ��������� ���⨭�� �� ��᪥ �� ��᫥�������� (����ᮢ�� - `F5`). ��⠢�� ���⨭�� �� ���� � DnD � ⥪�� - v0.2; ���� - ��࠭��� PNG � `assets/` �㪠�� � ������� `![](assets/x.png)`.

---

## 10. ����ன�� `[v0.1]`

���� `%APPDATA%\Plain\settings.json`, �⠥��� �� ����᪥. ����������� �� **��࠭� ����஥�** (`help  settings:`, `> settings`, `Ctrl+,`) - ��࠭ <� �⨫� ������⥪�> (����� 1e): ��������� `settings`, ᯨ᮪ ��ப `���� � ���祭��` � ⥪�⮢묨 ����஫��� (��४���⥫�-᫮��, �᫠ � `?/+`, �롮� �� ��ਠ�⮢), ��������� �ਬ������� �ࠧ� � ������� � 䠩�; ����� ��뫪� `open settings.json` ��� ��筮� �ࠢ��. ��४���⥫� ⥬� � �����-��� � `Ctrl+Shift+D` ⮦� �����뢠�� `appearance.theme`. ���� JSON �� ����᪥ - ��䮫�� � ������ `settings.json is invalid`. ���祩 ����:

```
appearance.theme         "system" | "light" | "dark"   system
appearance.fontSize      11-20                         13.5
src-tauri/src/history.rs:114:    let temp = crate::fs::stage(dir, bytes)?;
src-tauri/src/history.rs:115:    if let Err(error) = crate::fs::commit(&target, &temp, target.exists()) {
src-tauri/src/fs.rs:267:        read_only: meta.permissions().readonly(),
src-tauri/src/fs.rs:346:pub fn stage(dir: &Path, bytes: &[u8]) -> FsResult<PathBuf> {
src-tauri/src/fs.rs:394:pub fn commit(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
src-tauri/src/fs.rs:401:pub fn commit(target: &Path, temp: &Path, existed: bool) -> FsResult<()> {
src-tauri/src/fs.rs:504:    let temp = stage(dir, &bytes)?;
src-tauri/src/fs.rs:522:    commit(&path, &temp, existed)?;
src-tauri/src/fs.rs:992:        let sloppy = PathBuf::from(file.to_string_lossy().replace('\\', "/").to_lowercase());
src-tauri/src/fs.rs:1001:            None => eprintln!("8.3 names are off on this volume; alias case not covered"),
src-tauri/src/fs.rs:1033:        let temp = stage(dir.path(), b"first
src-tauri/src/fs.rs:1041:        let temp = stage(dir.path(), b"second
src/app/commands.ts:379:  readonly kept: string[] = [];
src/app/commands.ts:545: * `Ctrl+P`. Printing prints what is on screen, so the document has to be the
src/app/commands.ts:548: * because printing is a deliberate detour, not a mode.
src/app/commands.ts:556:  // over the content area — any of them would be what got printed.
src/app/commands.ts:578:  // The read view builds its document asynchronously; printing before it is
src/app/commands.ts:579:  // ready would print an empty page.
src/app/commands.ts:586:    window.removeEventListener("afterprint", restore);
src/app/commands.ts:589:  window.addEventListener("afterprint", restore);
src/app/commands.ts:592:    window.print();
src/app/commands.ts:594:    store.setMessage("couldn't open the print dialog");
?? review/waves/w9-codex-prompt.md
?? review/waves/w9-codex-review.md
?? review/waves/w9-codex-stderr.txt
f32704fe2479e387b23fad70e8dab01c169d0954

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content src/app/paths.ts; Get-Content src/app/close.ts; Get-Content src/editor/keymap.ts; Get-Content src-tauri/tauri.conf.json; Get-Content src-tauri/Cargo.toml; rg -n 'QuitRequested|ExitRequested|onCloseRequested|preventDefault|destroy' src/app src-tauri/src; rg --files C:/Users/Kotenochek/.cargo/registry/src -g menu.rs -g macos.rs -g fs.rs -g lib.rs | Select-Object -First 15" in G:\Claude Projects\md reader
 exited 1 in 210ms:
// Windows paths, but separators are normalised both ways: dialogs return
// backslashes, hand-written config may not.

import type { Doc } from "./store";

export function basename(path: string): string {
  const parts = path.split(/[\\/]/).filter(Boolean);
  return parts[parts.length - 1] ?? path;
}

export function dirname(path: string): string {
  const index = Math.max(path.lastIndexOf("\\"), path.lastIndexOf("/"));
  return index > 0 ? path.slice(0, index) : "";
}

/**
 * Identity of a file, used to keep one buffer per file. Feed it the path
 * Rust canonicalized (`read_file` returns one, `canonical_path` makes one):
 * that is what expands an 8.3 name like `KOTENO~1` and settles the case the
 * disk really uses. This only finishes the job �?" separator and case �?" so a
 * raw path from a dialog or a link still keys consistently with itself.
 */
export function pathKey(path: string): string {
  return path.replace(/\\/g, "/").replace(/\/+$/, "").toLowerCase();
}

function isUnder(path: string, root: string): boolean {
  const a = path.replace(/\\/g, "/").toLowerCase();
  const b = root.replace(/\\/g, "/").replace(/\/$/, "").toLowerCase();
  return a.startsWith(b + "/");
}

/** `folder / file.md` �?" first crumb is the root and is drawn bolder. */
export function breadcrumbs(libraryPath: string | null, doc: Doc | null): string[] {
  if (!doc) return libraryPath ? [basename(libraryPath)] : [];
  if (!doc.path) return [doc.title];
  if (libraryPath && isUnder(doc.path, libraryPath)) {
    const rel = doc.path.slice(libraryPath.length).replace(/^[\\/]+/, "");
    return [basename(libraryPath), ...rel.split(/[\\/]/).filter(Boolean)];
  }
  const parts = doc.path.split(/[\\/]/).filter(Boolean);
  return parts.slice(-2);
}

/**
 * The same crumbs, split into the three parts the mode bar lays out: the
 * root and the file name keep their width, and everything between them is
 * one cell that gives its width up first when the window is narrow.
 */
export interface Crumbs {
  root: string;
  /** Folders between the root and the file; often empty. */
  middle: string[];
  /** The file; null when only the library is open. */
  leaf: string | null;
}

export function crumbParts(libraryPath: string | null, doc: Doc | null): Crumbs | null {
  const parts = breadcrumbs(libraryPath, doc);
  const root = parts[0];
  if (root === undefined) return null;
  if (parts.length === 1) return { root, middle: [], leaf: null };
  return { root, middle: parts.slice(1, -1), leaf: parts[parts.length - 1] ?? null };
}
// Closing a document or the window. Unsaved work is never dropped without
// one explicit answer, and the answer has to still be true when the buffer
// is actually thrown away (spec �8).

import { getCurrentWindow } from "@tauri-apps/api/window";
import { dropBuffer, flushActiveEditor, isDirty } from "../editor";
import { draftsSettled, dropDraft } from "./drafts";
import { inTauri } from "./env";
import { save } from "./save";
import { toSession, writeSession } from "./session";
import { flushSettings } from "./settings";
import { useStore, type Doc } from "./store";

/** Always measured on live editor text, never on a stale `Doc`. */
function unsaved(ids: string[]): Doc[] {
  flushActiveEditor();
  return useStore
    .getState()
    .docs.filter((d) => ids.includes(d.id) && isDirty(d.id, d.text));
}

/**
 * Drops the buffers and their drafts. Resolves once the drafts are really
 * gone: the window may not close while a delete is still in flight, or the
 * next start offers back work the user just discarded.
 */
async function forget(ids: string[]): Promise<void> {
  const store = useStore.getState();
  const going = ids
    .map((id) => store.docs.find((d) => d.id === id))
    .filter((d): d is Doc => d !== undefined);

  for (const id of ids) {
    store.closeDoc(id);
    dropBuffer(id);
  }
  // There is one banner slot and it belonged to a document that is gone.
  store.dismissBanner("conflict");
  store.dismissBanner("save-failed");

  await Promise.all(going.map((d) => dropDraft(d)));
  await draftsSettled();
}

interface CloseOptions {
  /** After everything is saved and agreed, before the documents go. */
  ready?: () => void;
  /** Once they are really gone and their drafts with them. */
  done?: () => void;
}

/**
 * Closes the documents, asking once for all of them if any is unsaved.
 * `done` runs only when everything really closed �?" it is how the window
 * close request knows it may go through.
 */
export function closeDocs(ids: string[], options: CloseOptions | (() => void) = {}): void {
  // A bare callback is the common case (the tree's delete, the rail's �-).
  const hooks: CloseOptions = typeof options === "function" ? { done: options } : options;
  const finish = (targets: string[]) => {
    hooks.ready?.();
    void forget(targets).then(() => hooks.done?.());
  };

  const store = useStore.getState();
  const dirty = unsaved(ids);
  if (dirty.length === 0) {
    finish(ids);
    return;
  }

  store.setDialog({
    title: dirty.length === 1 ? "unsaved changes" : `${dirty.length} files have unsaved changes`,
    lines: dirty.map((d) => d.path ?? d.title),
    actions: [
      {
        label: "save",
        run: () => {
          store.setDialog(null);
          void (async () => {
            const before = new Set(useStore.getState().docs.map((d) => d.id));
            for (const item of dirty) {
              // A failed save leaves its own banner up and stops the close.
              if (!(await save(item.id))) return;
            }
            // Save As gave a nameless buffer a new id; it is the same
            // document, so it is still one of the ones being closed.
            const renamed = useStore
              .getState()
              .docs.filter((d) => !before.has(d.id))
              .map((d) => d.id);
            const targets = [...new Set([...ids, ...renamed])];
            // The saves took time and the editor stayed usable. Nothing typed
            // in the meantime was ever agreed to, so it is asked about again
            // rather than thrown away (spec �8).
            if (unsaved(targets).length > 0) {
              closeDocs(targets, hooks);
              return;
            }
            finish(targets);
          })();
        },
      },
      {
        label: "don't save",
        run: () => {
          store.setDialog(null);
          finish(ids);
        },
      },
      { label: "cancel", run: () => store.setDialog(null) },
    ],
    cancel: () => store.setDialog(null),
  });
}

export function closeActive(): void {
  const { activeId } = useStore.getState();
  if (activeId) closeDocs([activeId]);
}

/** The window's X and Alt+F4 go through the same one question. */
export function installCloseGuard(): () => void {
  if (!inTauri) return () => undefined;
  let leaving = false;
  const window = getCurrentWindow();
  const unlisten = window.onCloseRequested((event) => {
    if (leaving) return;
    event.preventDefault();
    const ids = useStore.getState().docs.map((d) => d.id);
    let session = toSession();
    closeDocs(ids, {
      // Taken after the saves �?" a Save As at the door belongs in the next
      // session �?" and before the documents go, since closing them is what
      // empties the open list (spec �6, �8).
      ready: () => {
        flushActiveEditor();
        session = toSession();
      },
      done: () => {
        leaving = true;
        void Promise.all([writeSession(session), flushSettings()]).then(() => window.destroy());
      },
    });
  });
  return () => void unlisten.then((off) => off());
}
// CodeMirror matches key bindings by `key`, which breaks every letter chord
// in a Russian layout (spec �12). Our commands go through this thin layer
// instead: same chord spelling as the app, matched by KeyboardEvent.code.

import {
  deleteLine,
  indentLess,
  indentMore,
  redo,
  selectAll,
  undo,
} from "@codemirror/commands";
import { foldCode, unfoldCode } from "@codemirror/language";
import { selectNextOccurrence } from "@codemirror/search";
import type { Extension } from "@codemirror/state";
import { ViewPlugin, type Command } from "@codemirror/view";
import { matchesChord, parseChord } from "../app/chords";
import {
  cycleList,
  insertLink,
  tabIndent,
  toggleBold,
  toggleCheckbox,
  toggleCodeBlock,
  toggleHeading,
  toggleInlineCode,
  toggleItalic,
  toggleQuote,
} from "./commands";

export interface CodeBinding {
  chord: string;
  run: Command;
}

/**
 * Runs the first binding whose physical chord matches. This listens on the
 * editor in the capture phase rather than through CodeMirror's own keydown
 * handling, because the app swallows Ctrl+F/Ctrl+G with preventDefault on
 * window (spec �12) and CodeMirror skips its handlers once that is set.
 */
export function codeKeymap(bindings: CodeBinding[]): Extension {
  const compiled = bindings.map((b) => ({ chord: parseChord(b.chord), run: b.run }));
  return ViewPlugin.define((view) => {
    const onKeydown = (event: KeyboardEvent) => {
      if (event.isComposing) return;
      // The find panel has its own keys.
      if ((event.target as HTMLElement | null)?.closest(".cm-panels")) return;
      for (const binding of compiled) {
        if (!matchesChord(binding.chord, event)) continue;
        if (binding.run(view)) event.preventDefault();
        return;
      }
    };
    view.dom.addEventListener("keydown", onKeydown, true);
    return { destroy: () => view.dom.removeEventListener("keydown", onKeydown, true) };
  });
}

/** Chords the app layer owns; CodeMirror must keep its hands off them. */
const yieldToApp: Command = () => true;

export function editorKeymap(): Extension {
  return codeKeymap([
    // formatting (�5.2)
    { chord: "Ctrl+B", run: toggleBold },
    { chord: "Ctrl+I", run: toggleItalic },
    { chord: "Ctrl+E", run: toggleInlineCode },
    { chord: "Ctrl+Shift+`", run: toggleCodeBlock },
    { chord: "Ctrl+Shift+K", run: insertLink },
    { chord: "Ctrl+Shift+Q", run: toggleQuote },
    { chord: "Ctrl+Shift+L", run: cycleList },
    { chord: "Ctrl+Enter", run: toggleCheckbox },
    { chord: "Ctrl+1", run: toggleHeading(1) },
    { chord: "Ctrl+2", run: toggleHeading(2) },
    { chord: "Ctrl+3", run: toggleHeading(3) },
    { chord: "Ctrl+4", run: toggleHeading(4) },
    { chord: "Ctrl+5", run: toggleHeading(5) },
    { chord: "Ctrl+6", run: toggleHeading(6) },
    { chord: "Ctrl+]", run: indentMore },
    { chord: "Ctrl+[", run: indentLess },
    { chord: "Tab", run: tabIndent },
    { chord: "Shift+Tab", run: indentLess },

    // editing (�12)
    { chord: "Ctrl+Z", run: undo },
    { chord: "Ctrl+Shift+Z", run: redo },
    { chord: "Ctrl+Y", run: redo },
    { chord: "Ctrl+A", run: selectAll },
    { chord: "Ctrl+D", run: selectNextOccurrence },
    { chord: "Ctrl+Shift+X", run: deleteLine },
    { chord: "Ctrl+Shift+[", run: foldCode },
    { chord: "Ctrl+Shift+]", run: unfoldCode },

    // Find, replace and go-to-line are app commands now: the registry owns
    // their chords and remote.ts answers the events (spec �12).

    // Ctrl+/ is the app's read/edit switch, not CodeMirror's comment toggle.
    { chord: "Ctrl+/", run: yieldToApp },
  ]);
}
{
  "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
  "productName": "Plain",
  "version": "0.2.0",
  "identifier": "Plain",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "label": "main",
        "title": "Plain",
        "width": 1100,
        "height": 720,
        "minWidth": 480,
        "minHeight": 400,
        "decorations": true,
        "resizable": true,
        "dragDropEnabled": true
      }
    ],
    "security": {
      "csp": "default-src 'self'; img-src 'self' asset: http://asset.localhost https: data: blob:; style-src 'self' 'unsafe-inline'; font-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost ws://localhost:1420",
      "assetProtocol": {
        "enable": true,
        "scope": []
      }
    }
  },
  "bundle": {
    "active": true,
    "targets": [],
    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.ico", "icons/icon.png"],
    "fileAssociations": [
      {
        "ext": ["md", "markdown"],
        "name": "Markdown",
        "description": "Markdown document",
        "role": "Editor"
      }
    ],
    "macOS": {
      "minimumSystemVersion": "13.0"
    }
  }
}
[package]
name = "plain"
version = "0.2.0"
description = "Plain �?" a monospace markdown reader/editor"
edition = "2021"
rust-version = "1.85"

[lib]
name = "plain_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
tauri = { version = "2.11", features = ["protocol-asset"] }
tauri-plugin-fs = "2.5"
tauri-plugin-dialog = "2.7"
tauri-plugin-opener = "2.5"
tauri-plugin-single-instance = "2.4"
tauri-plugin-window-state = "2.4"
tauri-plugin-persisted-scope = "2.3"
tauri-plugin-clipboard-manager = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
notify = "8.2.0"
notify-debouncer-full = "0.7.0"
tempfile = "3.27.0"
trash = "5.2.7"
encoding_rs = "0.8.35"
chardetng = "1.0.0"
blake3 = "1.8.7"
ignore = "0.4.23"
regex = "1.11.1"
rayon = "1.11.0"

# ReplaceFileW and the HKCU file association are Windows and nothing else;
# on macOS the atomic write is `rename` and the association is in the bundle
# (spec �13a).
[target.'cfg(windows)'.dependencies]
windows = { version = "0.62.2", features = ["Win32_Storage_FileSystem", "Win32_Foundation", "Win32_UI_Shell"] }
winreg = "0.56.0"

[profile.release]
# Thin LTO: fat LTO with one codegen unit needs more RAM than this machine has.
lto = "thin"
strip = true
src/app\close.ts:127:  const unlisten = window.onCloseRequested((event) => {
src/app\close.ts:129:    event.preventDefault();
src/app\close.ts:142:        void Promise.all([writeSession(session), flushSettings()]).then(() => window.destroy());
src/app\history.ts:35: * without it can destroy the last copy of that text (review #1).
src/app\keys.ts:41:    if (SWALLOW.some((chord) => matchesChord(chord, event))) event.preventDefault();
src/app\keys.ts:51:    event.preventDefault();
src/app\keys.ts:58:      event.preventDefault();
src/app\restore.test.ts:2:// here that can destroy text nobody has a second copy of (reviews #1, #2, #4),
src/app\restore.test.ts:94:  // only text there is, and replacing it destroys it.
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\alloc-stdlib-0.2.4\tests\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\alloc-stdlib-0.2.4\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\cairo-sys-rs-0.18.2\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\crossbeam-channel-0.5.16\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\zvariant_utils-4.2.0\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\cairo-rs-0.18.5\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\zvariant_derive-5.15.0\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\bytes-1.12.1\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\byteorder-lite-0.1.0\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\zvariant-5.15.0\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\alloc-no-stdlib-2.0.4\tests\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\alloc-no-stdlib-2.0.4\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\bs58-0.5.1\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\dunce-1.0.5\src\lib.rs
C:/Users/Kotenochek/.cargo/registry/src\index.crates.io-1949cf8c6b5b557f\dtor-proc-macro-0.0.6\src\lib.rs

web search: 
web search: site.support.apple.com macOS Sequoia open anyway unsigned control click ...
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$r='"'C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f'; rg -n 'pub fn default|close_window|hide\\(|quit\\(' \""'$r/tauri-2.11.*/src/menu/menu.rs"; Get-ChildItem $r -Directory -Name | Where-Object { $_ -match '"'"'^(tauri-2|tauri-runtime-wry|tauri-plugin-single|wry-|muda-)'"' }; rg -n 'Alt-ArrowUp|Shift-Alt-ArrowUp|mac:.*ArrowUp' node_modules/@codemirror/commands/dist/index.js; rg -n 'pathKey|existing|canonical' src/app/commands.ts src/app/save.ts; rg -n 'ctrl|withCtrl|Enter' src/ui/QuickSearch.tsx" in G:\Claude Projects\md reader
 succeeded in 191ms:
Синтаксическая ошибка в имени файла, имени папки или метке тома. (os error 123)
muda-0.19.3
tauri-2.11.5
tauri-plugin-single-instance-2.4.4
tauri-runtime-wry-2.11.4
wry-0.55.1
1751:    { mac: "Cmd-ArrowUp", run: cursorDocStart, shift: selectDocStart },
1752:    { mac: "Ctrl-ArrowUp", run: cursorPageUp, shift: selectPageUp },
1777:- Alt-ArrowUp: [`moveLineUp`](https://codemirror.net/6/docs/ref/#commands.moveLineUp)
1779:- Shift-Alt-ArrowUp: [`copyLineUp`](https://codemirror.net/6/docs/ref/#commands.copyLineUp)
1781:- Ctrl-Alt-ArrowUp (Cmd-Alt-ArrowUp on macOS): [`addCursorAbove`](https://codemirror.net/6/docs/ref/#commands.addCursorAbove).
1799:    { key: "Alt-ArrowUp", run: moveLineUp },
1800:    { key: "Shift-Alt-ArrowUp", run: copyLineUp },
1803:    { key: "Mod-Alt-ArrowUp", run: addCursorAbove },
src/app/save.ts:21:  canonicalPath,
src/app/save.ts:30:import { dirname, pathKey } from "./paths";
src/app/save.ts:232:  const target = await canonicalPath(picked).catch(() => picked);
src/app/save.ts:233:  const nextId = pathKey(target);
src/app/commands.ts:16:import { basename, dirname, pathKey } from "./paths";
src/app/commands.ts:32:    const known = useStore.getState().docs.find((d) => d.id === pathKey(path));
src/app/commands.ts:43:      const id = pathKey(info.path);
src/app/commands.ts:44:      const existing = useStore.getState().docs.find((d) => d.id === id);
src/app/commands.ts:45:      if (existing) {
src/app/commands.ts:46:        activate(existing.id);
62:  const withCtrl = useRef(false);
156:        if (event.key === "Enter") withCtrl.current = event.ctrlKey;
185:                    const inEdit = withCtrl.current;
186:                    withCtrl.current = false;
208:        <span>ctrl ↵ open in edit</span>

web search: 
web search: https://raw.githubusercontent.com/tauri-apps/tauri-action/v0/src/index.ts
codex
В сохранении нашлись две отдельные проблемы: Unix-пути на фронте всё ещё приводятся к lowercase и Windows-разделителям, а при замене файла переносится только mode с игнорированием ошибок. Также проверяю `⌘Q`: текущая защита несохранённых правок подписана только на закрытие окна.

exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$r='"'C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f'; rg -n -A 110 'pub fn default' \""'$r/tauri-2.11.5/src/menu/menu.rs"; rg -n '"'terminate:|performClose:|Quit|CloseWindow|Hide|KeyW|KeyH' \""'$r/muda-0.19.3/src/platform_impl/macos"; rg -n '"'ExitRequested|shouldTerminate|ShouldTerminate|CloseRequested' \""'$r/tauri-runtime-wry-2.11.4/src/lib.rs"; Get-Content "$r/tauri-plugin-single-instance-2.4.4/src/platform_impl/macos.rs"; rg -n '"'print|runJavaScript|Print' \""'$r/wry-0.55.1/src/wkwebview"; rg -n '"'name = \"tao\"|version = \"0.3' src-tauri/Cargo.lock" in G:\Claude Projects\md reader
 exited 1 in 312ms:
142:  pub fn default(app_handle: &AppHandle<R>) -> crate::Result<Self> {
143-    let pkg_info = app_handle.package_info();
144-    let config = app_handle.config();
145-    let about_metadata = AboutMetadata {
146-      name: Some(pkg_info.name.clone()),
147-      version: Some(pkg_info.version.to_string()),
148-      copyright: config.bundle.copyright.clone(),
149-      authors: config.bundle.publisher.clone().map(|p| vec![p]),
150-      ..Default::default()
151-    };
152-
153-    let window_menu = Submenu::with_id_and_items(
154-      app_handle,
155-      WINDOW_SUBMENU_ID,
156-      "Window",
157-      true,
158-      &[
159-        &PredefinedMenuItem::minimize(app_handle, None)?,
160-        &PredefinedMenuItem::maximize(app_handle, None)?,
161-        #[cfg(target_os = "macos")]
162-        &PredefinedMenuItem::separator(app_handle)?,
163-        &PredefinedMenuItem::close_window(app_handle, None)?,
164-      ],
165-    )?;
166-
167-    let help_menu = Submenu::with_id_and_items(
168-      app_handle,
169-      HELP_SUBMENU_ID,
170-      "Help",
171-      true,
172-      &[
173-        #[cfg(not(target_os = "macos"))]
174-        &PredefinedMenuItem::about(app_handle, None, Some(about_metadata))?,
175-      ],
176-    )?;
177-
178-    let menu = Menu::with_items(
179-      app_handle,
180-      &[
181-        #[cfg(target_os = "macos")]
182-        &Submenu::with_items(
183-          app_handle,
184-          pkg_info.name.clone(),
185-          true,
186-          &[
187-            &PredefinedMenuItem::about(app_handle, None, Some(about_metadata))?,
188-            &PredefinedMenuItem::separator(app_handle)?,
189-            &PredefinedMenuItem::services(app_handle, None)?,
190-            &PredefinedMenuItem::separator(app_handle)?,
191-            &PredefinedMenuItem::hide(app_handle, None)?,
192-            &PredefinedMenuItem::hide_others(app_handle, None)?,
193-            &PredefinedMenuItem::separator(app_handle)?,
194-            &PredefinedMenuItem::quit(app_handle, None)?,
195-          ],
196-        )?,
197-        #[cfg(not(any(
198-          target_os = "linux",
199-          target_os = "dragonfly",
200-          target_os = "freebsd",
201-          target_os = "netbsd",
202-          target_os = "openbsd"
203-        )))]
204-        &Submenu::with_items(
205-          app_handle,
206-          "File",
207-          true,
208-          &[
209-            &PredefinedMenuItem::close_window(app_handle, None)?,
210-            #[cfg(not(target_os = "macos"))]
211-            &PredefinedMenuItem::quit(app_handle, None)?,
212-          ],
213-        )?,
214-        &Submenu::with_items(
215-          app_handle,
216-          "Edit",
217-          true,
218-          &[
219-            &PredefinedMenuItem::undo(app_handle, None)?,
220-            &PredefinedMenuItem::redo(app_handle, None)?,
221-            &PredefinedMenuItem::separator(app_handle)?,
222-            &PredefinedMenuItem::cut(app_handle, None)?,
223-            &PredefinedMenuItem::copy(app_handle, None)?,
224-            &PredefinedMenuItem::paste(app_handle, None)?,
225-            &PredefinedMenuItem::select_all(app_handle, None)?,
226-          ],
227-        )?,
228-        #[cfg(target_os = "macos")]
229-        &Submenu::with_items(
230-          app_handle,
231-          "View",
232-          true,
233-          &[&PredefinedMenuItem::fullscreen(app_handle, None)?],
234-        )?,
235-        &window_menu,
236-        &help_menu,
237-      ],
238-    )?;
239-
240-    Ok(menu)
241-  }
242-
243-  pub(crate) fn inner(&self) -> &muda::Menu {
244-    (*self.0).as_ref()
245-  }
246-
247-  /// The application handle associated with this type.
248-  pub fn app_handle(&self) -> &AppHandle<R> {
249-    &self.0.app_handle
250-  }
251-
252-  /// Returns a unique identifier associated with this menu.
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/muda-0.19.3/src/platform_impl/macos\mod.rs:363:                PredefinedMenuItemType::Hide => format!("Hide {}", app_name()).trim().to_string(),
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/muda-0.19.3/src/platform_impl/macos\mod.rs:364:                PredefinedMenuItemType::Quit => format!("Quit {}", app_name()).trim().to_string(),
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/muda-0.19.3/src/platform_impl/macos\mod.rs:990:            PredefinedMenuItemType::Hide => Some(sel!(hide:)),
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/muda-0.19.3/src/platform_impl/macos\mod.rs:991:            PredefinedMenuItemType::HideOthers => Some(sel!(hideOtherApplications:)),
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/muda-0.19.3/src/platform_impl/macos\mod.rs:993:            PredefinedMenuItemType::CloseWindow => Some(sel!(performClose:)),
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/muda-0.19.3/src/platform_impl/macos\mod.rs:994:            PredefinedMenuItemType::Quit => Some(sel!(terminate:)),
31:  Cookie, DeviceEventFilter, Error, EventLoopProxy, ExitRequestedEventAction, Icon,
4307:          TaoWindowEvent::CloseRequested => {
4316:                callback(RunEvent::ExitRequested { code: None, tx });
4319:                let should_prevent = matches!(recv, Ok(ExitRequestedEventAction::Prevent));
4356:        callback(RunEvent::ExitRequested {
4362:        let should_prevent = matches!(recv, Ok(ExitRequestedEventAction::Prevent));
4454:      handler(&WindowEvent::CloseRequested {
4460:      event: WindowEvent::CloseRequested { signal_tx: tx },
// Copyright 2019-2023 Tauri Programme within The Commons Conservancy
// SPDX-License-Identifier: Apache-2.0
// SPDX-License-Identifier: MIT

use std::{
    io::{BufWriter, Error, ErrorKind, Write},
    os::unix::net::UnixStream,
    path::PathBuf,
};

#[cfg(feature = "semver")]
use crate::semver_compat::semver_compat_string;
use crate::SingleInstanceCallback;
use tauri::{
    plugin::{self, TauriPlugin},
    AppHandle, Config, Manager, RunEvent, Runtime,
};
use tokio::io::AsyncReadExt;

pub fn init<R: Runtime>(cb: Box<SingleInstanceCallback<R>>) -> TauriPlugin<R> {
    plugin::Builder::new("single-instance")
        .setup(|app, _api| {
            let socket = socket_path(app.config(), app.package_info());

            // Notify the singleton which may or may not exist.
            match notify_singleton(&socket) {
                Ok(_) => {
                    std::process::exit(0);
                }
                Err(e) => {
                    match e.kind() {
                        ErrorKind::NotFound | ErrorKind::ConnectionRefused => {
                            // This process claims itself as singleton as likely none exists
                            socket_cleanup(&socket);
                            listen_for_other_instances(socket, app.clone(), cb);
                        }
                        _ => {
                            tracing::debug!(
                                "single_instance failed to notify - launching normally: {}",
                                e
                            );
                        }
                    }
                }
            }
            Ok(())
        })
        .on_event(|app, event| {
            if let RunEvent::Exit = event {
                destroy(app);
            }
        })
        .build()
}

pub fn destroy<R: Runtime, M: Manager<R>>(manager: &M) {
    let socket = socket_path(manager.config(), manager.package_info());
    socket_cleanup(&socket);
}

fn socket_path(config: &Config, _package_info: &tauri::PackageInfo) -> PathBuf {
    let identifier = config.identifier.replace(['.', '-'].as_ref(), "_");

    #[cfg(feature = "semver")]
    let identifier = format!(
        "{identifier}_{}",
        semver_compat_string(&_package_info.version),
    );

    // Use /tmp as socket path must be shorter than 100 chars.
    PathBuf::from(format!("/tmp/{}_si.sock", identifier))
}

fn socket_cleanup(socket: &PathBuf) {
    let _ = std::fs::remove_file(socket);
}

fn notify_singleton(socket: &PathBuf) -> Result<(), Error> {
    let stream = UnixStream::connect(socket)?;
    let mut bf = BufWriter::new(&stream);
    let cwd = std::env::current_dir()
        .unwrap_or_default()
        .to_str()
        .unwrap_or_default()
        .to_string();
    bf.write_all(cwd.as_bytes())?;
    bf.write_all(b"\0\0")?;
    let args_joined = std::env::args().collect::<Vec<String>>().join("\0");
    bf.write_all(args_joined.as_bytes())?;
    bf.flush()?;
    drop(bf);
    Ok(())
}

fn listen_for_other_instances<A: Runtime>(
    socket: PathBuf,
    app: AppHandle<A>,
    mut cb: Box<SingleInstanceCallback<A>>,
) {
    tauri::async_runtime::spawn(async move {
        match tokio::net::UnixListener::bind(socket) {
            Ok(listener) => loop {
                match listener.accept().await {
                    Ok((mut stream, _addr)) => {
                        let mut s = String::new();
                        match stream.read_to_string(&mut s).await {
                            Ok(_) => {
                                let (cwd, args) = s.split_once("\0\0").unwrap_or_default();
                                let args: Vec<String> =
                                    args.split('\0').map(String::from).collect();
                                cb(app.app_handle(), args, cwd.to_string());
                            }
                            Err(e) => {
                                tracing::debug!("single_instance failed to be notified: {e}")
                            }
                        }
                    }
                    Err(err) => {
                        tracing::debug!("single_instance failed to be notified: {}", err);
                        continue;
                    }
                }
            },
            Err(err) => {
                tracing::error!(
                    "single_instance failed to listen to other processes - launching normally: {}",
                    err
                );
            }
        }
    });
}
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\download.rs:113:      eprintln!("Download failed with error: {description}");
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:119:pub struct PrintMargin {
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:127:pub struct PrintOptions {
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:128:  pub margins: PrintMargin,
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:604:      // ns window is required for the print operation
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:858:  pub fn print(&self) -> crate::Result<()> {
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:859:    self.print_with_options(&PrintOptions::default())
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:862:  pub fn print_with_options(&self, _options: &PrintOptions) -> crate::Result<()> {
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:866:      // <https://developer.apple.com/documentation/webkit/wkwebview/printoperation(with:)>
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:868:      let can_print = self
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:870:        .respondsToSelector(objc2::sel!(printOperationWithPrintInfo:));
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:871:      if can_print {
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:872:        // Create a shared print info
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:873:        let print_info = objc2_app_kit::NSPrintInfo::sharedPrintInfo();
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:874:        // let print_info: id = msg_send![print_info, init];
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:875:        print_info.setTopMargin(_options.margins.top.into());
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:876:        print_info.setRightMargin(_options.margins.right.into());
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:877:        print_info.setBottomMargin(_options.margins.bottom.into());
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:878:        print_info.setLeftMargin(_options.margins.left.into());
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:880:        // Create new print operation from the webview content
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:881:        let print_operation = self.webview.printOperationWithPrintInfo(&print_info);
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:884:        print_operation.setCanSpawnSeparateThread(true);
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\mod.rs:888:        print_operation.runOperationModalForWindow_delegate_didRunSelector_contextInfo(
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\ios\WKWebView.rs:521:    #[unsafe(method(printOperationWithPrintInfo:))]
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\ios\WKWebView.rs:522:    pub unsafe fn printOperationWithPrintInfo(
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\ios\WKWebView.rs:524:      print_info: &NSPrintInfo,
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview\ios\WKWebView.rs:525:    ) -> Retained<NSPrintOperation>;

web search: 
web search: site.github.com/rust-lang/rust "target_vendor = " "F_FULLFSYNC" fs.rs ...
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$r='"'C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f'; Get-ChildItem "'$r -Directory -Name | Where-Object { $_ -like '"'tao-*' }; rg -n -A 24 -B 5 'TaoEvent::LoopDestroyed|TaoEvent::UserEvent.*Exit|TaoEvent::Exit|ExitRequested \\{' \""'$r/tauri-runtime-wry-2.11.4/src/lib.rs"; rg -n '"'print|Print|WKUIDelegate' \""'$r/wry-0.55.1/src/wkwebview/class"; rg -n -A 16 '"'fn canonical' src-tauri/src/fs.rs; Get-Content src/app/commands.ts | Select-Object -Skip 578 -First 25; Get-Content src/app/settings.ts | Select-Object -First 45; rg -n 'newFileEol|eol:' src/app/commands.ts src/app/drafts.ts; rg -n 'ctrl|Ctrl' src/editor/toolbar.ts src/ui/FileTree.tsx; Get-Content src-tauri/src/history.rs | Select-Object -Skip 100 -First 32" in G:\Claude Projects\md reader
 succeeded in 227ms:
tao-0.35.3
tao-macros-0.1.4
4311-            let removed = windows.0.borrow_mut().remove(&window_id).is_some();
4312-            if removed {
4313-              let is_empty = windows.0.borrow().is_empty();
4314-              if is_empty {
4315-                let (tx, rx) = channel();
4316:                callback(RunEvent::ExitRequested { code: None, tx });
4317-
4318-                let recv = rx.try_recv();
4319-                let should_prevent = matches!(recv, Ok(ExitRequestedEventAction::Prevent));
4320-
4321-                if !should_prevent {
4322-                  *control_flow = ControlFlow::Exit;
4323-                }
4324-              }
4325-            }
4326-          }
4327-          TaoWindowEvent::Resized(size) => {
4328-            if let Some((Some(window), webviews)) = windows
4329-              .0
4330-              .borrow()
4331-              .get(&window_id)
4332-              .map(|w| (w.inner.clone(), w.webviews.clone()))
4333-            {
4334-              let size = size.to_logical::<f32>(window.scale_factor());
4335-              for webview in webviews {
4336-                if let Some(b) = &*webview.bounds.lock().unwrap() {
4337-                  if let Err(e) = webview.set_bounds(wry::Rect {
4338-                    position: LogicalPosition::new(size.width * b.x_rate, size.height * b.y_rate)
4339-                      .into(),
4340-                    size: LogicalSize::new(size.width * b.width_rate, size.height * b.height_rate)
--
4351-      }
4352-    }
4353-    Event::UserEvent(message) => match message {
4354-      Message::RequestExit(code) => {
4355-        let (tx, rx) = channel();
4356:        callback(RunEvent::ExitRequested {
4357-          code: Some(code),
4358-          tx,
4359-        });
4360-
4361-        let recv = rx.try_recv();
4362-        let should_prevent = matches!(recv, Ok(ExitRequestedEventAction::Prevent));
4363-
4364-        if !should_prevent {
4365-          *control_flow = ControlFlow::Exit;
4366-        }
4367-      }
4368-      Message::Window(id, WindowMessage::Close) => {
4369-        on_close_requested(callback, id, windows);
4370-      }
4371-      Message::Window(id, WindowMessage::Destroy) => {
4372-        on_window_close(id, windows);
4373-      }
4374-      Message::UserEvent(t) => callback(RunEvent::UserEvent(t)),
4375-      message => {
4376-        handle_user_message(
4377-          event_loop,
4378-          message,
4379-          UserMessageContext {
4380-            window_id_map,
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview/class\wry_web_view_ui_delegate.rs:21:  WKFrameInfo, WKMediaCaptureType, WKPermissionDecision, WKSecurityOrigin, WKUIDelegate,
C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/wry-0.55.1/src/wkwebview/class\wry_web_view_ui_delegate.rs:97:  unsafe impl WKUIDelegate for WryWebViewUIDelegate {
217:pub fn canonical(path: &Path) -> PathBuf {
218-    if let Ok(found) = fs::canonicalize(path) {
219-        return plain_path(found);
220-    }
221-    let (Some(parent), Some(name)) = (path.parent(), path.file_name()) else {
222-        return path.to_path_buf();
223-    };
224-    if parent.as_os_str().is_empty() {
225-        return path.to_path_buf();
226-    }
227-    match fs::canonicalize(parent) {
228-        Ok(folder) => plain_path(folder).join(name),
229-        Err(_) => path.to_path_buf(),
230-    }
231-}
232-
233-/// For the paths that do not come back through `read_file`: the Save As
--
236:pub fn canonical_path(path: String) -> String {
237-    canonical(Path::new(&path)).to_string_lossy().into_owned()
238-}
239-
240-fn mtime_ms(meta: &fs::Metadata) -> f64 {
241-    meta.modified()
242-        .ok()
243-        .and_then(|time| time.duration_since(UNIX_EPOCH).ok())
244-        .map(|since| since.as_millis() as f64)
245-        .unwrap_or(0.0)
246-}
247-
248-/// The text comes back exactly as it is on disk, CRLF and all: CodeMirror
249-/// normalizes it, and the front end keeps `eol` to put it back.
250-pub fn read_info(path: &Path, forced: Option<&str>) -> FsResult<FileInfo> {
251-    let bytes = fs::read(path)?;
252-    let meta = fs::metadata(path)?;
  // ready would print an empty page.
  if (covered || was !== "read") {
    await new Promise((done) => setTimeout(done, 400));
    await afterRender();
  }

  const restore = () => {
    window.removeEventListener("afterprint", restore);
    if (was !== "read") useStore.getState().setMode(was);
  };
  window.addEventListener("afterprint", restore);

  try {
    window.print();
  } catch {
    store.setMessage("couldn't open the print dialog");
    restore();
  }
}

/**
 * Export the open document to one self-contained `.html` (spec �2a).
 * The build lives in read/export.ts; this is the dialog and the write.
 */
export async function exportHtmlFile(): Promise<void> {
// %APPDATA%\Plain\settings.json �?" read once at startup (spec �10).
// Broken JSON falls back to defaults and raises a banner; a single bad value
// falls back to that key's default and the rest of the file still applies.

import { invoke } from "@tauri-apps/api/core";
import { appDataDir, join } from "@tauri-apps/api/path";
import { exists, readTextFile } from "@tauri-apps/plugin-fs";
import { inTauri } from "./env";
import { isMac } from "./platform";

export type Theme = "system" | "light" | "dark";
export type IndentUnit = "tab" | 2 | 4;
export type Eol = "crlf" | "lf";

export interface Settings {
  appearance: { theme: Theme; fontSize: number; contentWidth: number };
  read: { codeWrap: boolean };
  edit: { lineNumbers: boolean; indentUnit: IndentUnit; spellcheck: boolean };
  files: { newFileEol: Eol; autosave: number };
  library: { extensions: string[] };
}

export const SETTINGS_FILE = "settings.json";

/** macOS writes LF, Windows writes CRLF (spec �13a). */
export function defaultEol(): Eol {
  return isMac() ? "lf" : "crlf";
}

export const DEFAULTS: Settings = {
  appearance: { theme: "system", fontSize: 13.5, contentWidth: 560 },
  read: { codeWrap: false },
  edit: { lineNumbers: true, indentUnit: 2, spellcheck: true },
  files: { newFileEol: "crlf", autosave: 0 },
  library: { extensions: [".md", ".markdown"] },
};

/** `files.autosave` bounds, from spec �2a. 0 turns it off. */
export const AUTOSAVE = { min: 0, max: 60, step: 1 };

type Json = Record<string, unknown>;

function section(raw: unknown, key: string): Json {
  const root = raw as Json | null;
  const value = root && typeof root === "object" ? root[key] : undefined;
src/app/commands.ts:109:      eol: store.settings.files.newFileEol,
src/app/commands.ts:528:    await writeTextAtomic(picked, serialize(text, store.settings.files.newFileEol));
src/app/drafts.ts:33:  eol: Eol;
src/app/drafts.ts:191:      eol: doc.eol,
src/app/drafts.ts:319:    eol: draft.eol ?? "crlf",

/// Through the same temp-and-rename a document write uses. A half-written
/// snapshot would look like a version in the history and, with its fresh
/// mtime, block the next attempt for five minutes (review #5).
fn write_snapshot(dir: &Path, bytes: &[u8], now: SystemTime) -> FsResult<PathBuf> {
    let mut target = dir.join(format!("{}.md", stamp(now)));
    // A second save inside the same second only happens on a forced snapshot.
    for extra in 1..100 {
        if !target.exists() {
            break;
        }
        target = dir.join(format!("{}-{extra}.md", stamp(now)));
    }
    let temp = crate::fs::stage(dir, bytes)?;
    if let Err(error) = crate::fs::commit(&target, &temp, target.exists()) {
        let _ = fs::remove_file(&temp);
        return Err(error);
    }
    Ok(target)
}

/// Called straight after a successful write, with the bytes that were
/// written. Failing to keep a copy must never fail the save, so the caller
/// throws the result away.
pub fn keep<R: Runtime>(app: &AppHandle<R>, id: &str, bytes: &[u8]) -> FsResult<Option<String>> {
    let Some(dir) = folder(app, id) else {
        return Ok(None);
    };
    let now = SystemTime::now();
    if dir.is_dir() && !due(&dir, now) {
        return Ok(None);
    }

web search: 
web search: https://doc.rust-lang.org/stable/std/fs/struct.Permissions.html
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$r='"'C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f'; rg -n -A 35 -B 8 'fn application_should_terminate|fn should_terminate|applicationShouldTerminate' \""'$r/tao-0.35.3/src/platform_impl/macos"; rg -n -A 15 -B 4 '"'Quit|Hide|CloseWindow' \""'$r/muda-0.19.3/src/items/predefined.rs"; rg -n -A 16 '"'loadSettings|defaultEol' src/app/settings.ts; rg -n 'print|Print' \""'$r/wry-0.55.1/src/wkwebview/class/wry_web_view_ui_delegate.rs"; rg -n '"'readOnly|read_only' src/app/save.ts; rg -n 'replace.*\\\\\\\\|toLowerCase' src/read/links.ts src/library src/app/history.ts" in G:\Claude Projects\md reader
 succeeded in 279ms:
102-    pub fn fullscreen(text: Option<&str>) -> PredefinedMenuItem {
103-        PredefinedMenuItem::new(PredefinedMenuItemType::Fullscreen, text)
104-    }
105-
106:    /// Hide window menu item
107-    ///
108-    /// ## Platform-specific:
109-    ///
110-    /// - **Linux:** Unsupported.
111-    pub fn hide(text: Option<&str>) -> PredefinedMenuItem {
112:        PredefinedMenuItem::new(PredefinedMenuItemType::Hide, text)
113-    }
114-
115:    /// Hide other windows menu item
116-    ///
117-    /// ## Platform-specific:
118-    ///
119-    /// - **Linux:** Unsupported.
120-    pub fn hide_others(text: Option<&str>) -> PredefinedMenuItem {
121:        PredefinedMenuItem::new(PredefinedMenuItemType::HideOthers, text)
122-    }
123-
124-    /// Show all app windows menu item
125-    ///
126-    /// ## Platform-specific:
127-    ///
128-    /// - **Windows / Linux:** Unsupported.
129-    pub fn show_all(text: Option<&str>) -> PredefinedMenuItem {
130-        PredefinedMenuItem::new(PredefinedMenuItemType::ShowAll, text)
131-    }
132-
133-    /// Close window menu item
134-    ///
135-    /// ## Platform-specific:
136-    ///
137-    /// - **Linux:** Unsupported.
138-    pub fn close_window(text: Option<&str>) -> PredefinedMenuItem {
139:        PredefinedMenuItem::new(PredefinedMenuItemType::CloseWindow, text)
140-    }
141-
142:    /// Quit app menu item
143-    ///
144-    /// ## Platform-specific:
145-    ///
146-    /// - **Linux:** Unsupported.
147-    pub fn quit(text: Option<&str>) -> PredefinedMenuItem {
148:        PredefinedMenuItem::new(PredefinedMenuItemType::Quit, text)
149-    }
150-
151-    /// About app menu item
152-    pub fn about(text: Option<&str>, metadata: Option<AboutMetadata>) -> PredefinedMenuItem {
153-        PredefinedMenuItem::new(PredefinedMenuItemType::About(metadata), text)
154-    }
155-
156-    /// Services menu item
157-    ///
158-    /// ## Platform-specific:
159-    ///
160-    /// - **Windows / Linux:** Unsupported.
161-    pub fn services(text: Option<&str>) -> PredefinedMenuItem {
162-        PredefinedMenuItem::new(PredefinedMenuItemType::Services, text)
163-    }
--
251-    Redo,
252-    Minimize,
253-    Maximize,
254-    Fullscreen,
255:    Hide,
256:    HideOthers,
257-    ShowAll,
258:    CloseWindow,
259:    Quit,
260-    About(Option<AboutMetadata>),
261-    Services,
262-    BringAllToFront,
263-    #[default]
264-    None,
265-}
266-
267-impl PredefinedMenuItemType {
268-    pub(crate) fn text(&self) -> &str {
269-        match self {
270-            PredefinedMenuItemType::Separator => "",
271-            PredefinedMenuItemType::Copy => "&Copy",
272-            PredefinedMenuItemType::Cut => "Cu&t",
273-            PredefinedMenuItemType::Paste => "&Paste",
274-            PredefinedMenuItemType::SelectAll => "Select &All",
--
279-            PredefinedMenuItemType::Maximize => "Zoom",
280-            #[cfg(not(target_os = "macos"))]
281-            PredefinedMenuItemType::Maximize => "Ma&ximize",
282-            PredefinedMenuItemType::Fullscreen => "Toggle Full Screen",
283:            PredefinedMenuItemType::Hide => "&Hide",
284:            PredefinedMenuItemType::HideOthers => "Hide Others",
285-            PredefinedMenuItemType::ShowAll => "Show All",
286-            #[cfg(windows)]
287:            PredefinedMenuItemType::CloseWindow => "Close",
288-            #[cfg(not(windows))]
289:            PredefinedMenuItemType::CloseWindow => "C&lose Window",
290-            #[cfg(windows)]
291:            PredefinedMenuItemType::Quit => "&Exit",
292-            #[cfg(not(windows))]
293:            PredefinedMenuItemType::Quit => "&Quit",
294-            PredefinedMenuItemType::About(_) => "&About",
295-            PredefinedMenuItemType::Services => "Services",
296-            PredefinedMenuItemType::BringAllToFront => "Bring All to Front",
297-            PredefinedMenuItemType::None => "",
298-        }
299-    }
300-
301-    pub(crate) fn accelerator(&self) -> Option<Accelerator> {
302-        match self {
303-            PredefinedMenuItemType::Copy => Some(Accelerator::new(Some(CMD_OR_CTRL), Code::KeyC)),
304-            PredefinedMenuItemType::Cut => Some(Accelerator::new(Some(CMD_OR_CTRL), Code::KeyX)),
305-            PredefinedMenuItemType::Paste => Some(Accelerator::new(Some(CMD_OR_CTRL), Code::KeyV)),
306-            PredefinedMenuItemType::Undo => Some(Accelerator::new(Some(CMD_OR_CTRL), Code::KeyZ)),
307-            #[cfg(target_os = "macos")]
308-            PredefinedMenuItemType::Redo => Some(Accelerator::new(
--
321-            PredefinedMenuItemType::Fullscreen => Some(Accelerator::new(
322-                Some(Modifiers::META | Modifiers::CONTROL),
323-                Code::KeyF,
324-            )),
325:            PredefinedMenuItemType::Hide => Some(Accelerator::new(Some(CMD_OR_CTRL), Code::KeyH)),
326:            PredefinedMenuItemType::HideOthers => Some(Accelerator::new(
327-                Some(CMD_OR_CTRL | Modifiers::ALT),
328-                Code::KeyH,
329-            )),
330-            #[cfg(target_os = "macos")]
331:            PredefinedMenuItemType::CloseWindow => {
332-                Some(Accelerator::new(Some(CMD_OR_CTRL), Code::KeyW))
333-            }
334-            #[cfg(not(target_os = "macos"))]
335:            PredefinedMenuItemType::CloseWindow => {
336-                Some(Accelerator::new(Some(Modifiers::ALT), Code::F4))
337-            }
338-            #[cfg(target_os = "macos")]
339:            PredefinedMenuItemType::Quit => Some(Accelerator::new(Some(CMD_OR_CTRL), Code::KeyQ)),
340-            _ => None,
341-        }
342-    }
343-}
26:export function defaultEol(): Eol {
27-  return isMac() ? "lf" : "crlf";
28-}
29-
30-export const DEFAULTS: Settings = {
31-  appearance: { theme: "system", fontSize: 13.5, contentWidth: 560 },
32-  read: { codeWrap: false },
33-  edit: { lineNumbers: true, indentUnit: 2, spellcheck: true },
34-  files: { newFileEol: "crlf", autosave: 0 },
35-  library: { extensions: [".md", ".markdown"] },
36-};
37-
38-/** `files.autosave` bounds, from spec §2a. 0 turns it off. */
39-export const AUTOSAVE = { min: 0, max: 60, step: 1 };
40-
41-type Json = Record<string, unknown>;
42-
--
121:export async function loadSettings(): Promise<ParsedSettings> {
122-  if (!inTauri) return { settings: DEFAULTS, invalid: false };
123-  try {
124-    const file = await settingsPath();
125-    if (!(await exists(file))) return { settings: DEFAULTS, invalid: false };
126-    return parseSettings(await readTextFile(file));
127-  } catch {
128-    return { settings: DEFAULTS, invalid: true };
129-  }
130-}
131-
132-/* ------------------------------------------------------------------ write */
133-
134-/**
135- * The whole file, all eight keys, indented by two — writing only what changed
136- * would need a diff against the file on disk, and the file is eight lines.
137- */
178:  if (current.readOnly) {
268:      readOnly: false,
273:    // with the buffer still holds `readOnly.of(true)` and would swallow every
471:      setReadOnly(id, fields.readOnly);
586:          if (!doc.path || doc.readOnly) continue;
598:      if (!doc.path || doc.readOnly) continue;
672:      setReadOnly(id, fields.readOnly);
src/app/history.ts:78:        id: snapshot.path.toLowerCase(),
src/read/links.ts:32:  const parts = dir.replace(/\\/g, "/").replace(/\/+$/, "").split("/");
src/read/links.ts:33:  for (const segment of rel.replace(/\\/g, "/").split("/")) {
src/read/links.ts:45:  return path.replace(/\\/g, "/").replace(/\/+$/, "");
src/read/links.ts:50:  const a = normalizePath(path).toLowerCase();
src/read/links.ts:51:  const b = normalizePath(dir).toLowerCase();
src/read/links.ts:78:  const left = a.toLowerCase().split("/");
src/read/links.ts:79:  const right = b.toLowerCase().split("/");
src/read/links.ts:92:  const needle = normalizePath(wanted).toLowerCase();
src/read/links.ts:99:    const low = path.toLowerCase();
src/read/links.ts:136:    const here = relative?.toLowerCase();
src/read/links.ts:137:    const known = here !== undefined && index.some((p) => normalizePath(p).toLowerCase() === here);
src/library\tree.ts:50:  const key = path.toLowerCase();
src/library\tree.ts:53:      if (node.path.toLowerCase() === key) return node;
src/library\tree.ts:65:  return `${parent.replace(/[\\/]+$/, "")}${separator}${name}`;
src/library\tree.test.ts:6:  return `C:\\lib\\${rel.replace(/\//g, "\\")}`;
src/library\ops.test.ts:113:    const doc = makeDoc({ id: path.toLowerCase().replace(/\\/g, "/"), path, text: "x" });
src/library\library.test.ts:10:    path: `C:\\lib\\${rel.replace(/\//g, "\\")}`,
src/library\library.test.ts:24:    path: `C:\\lib\\${rel.replace(/\//g, "\\")}`,
src/library\names.ts:17:  for (const name of taken) used.add(name.toLowerCase());
src/library\names.ts:18:  if (!used.has(wanted.toLowerCase())) return wanted;
src/library\names.ts:23:    if (!used.has(candidate.toLowerCase())) return candidate;
src/library\fuzzy.test.ts:44:    expect(shown.toLowerCase()).toBe("plain");
src/library\backlinks.test.ts:40:  useStore.getState().openDoc(makeDoc({ id: path.toLowerCase(), path, text: "x" }));

web search: 
web search: 'printFrame'
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "git -c safe.directory='G:/Claude Projects/md reader' diff 54129c7..f32704f -- src/app/settings.ts; "'$r='"'C:/Users/Kotenochek/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f'; rg -n -i -A 18 -B 4 'terminate|termination' \""'$r/tao-0.35.3/src/platform_impl/macos/app_delegate.rs"; rg -n '"'defaultEol|newFileEol' src/app/mac.test.ts; rg -n 'sync_all|fsync' \""'$r/tauri-2.11.5/src" -g '"'*.rs' | Select-Object -First 2; rg -n 'read_only|hard|permission|rename' src-tauri/src/fs.rs | Select-Object -Last 12" in G:\Claude Projects\md reader
 succeeded in 363ms:
59-    sel!(applicationDidFinishLaunching:),
60-    did_finish_launching as extern "C" fn(_, _, _),
61-  );
62-  decl.add_method(
63:    sel!(applicationWillTerminate:),
64:    application_will_terminate as extern "C" fn(_, _, _),
65-  );
66-  decl.add_method(
67-    sel!(application:openURLs:),
68-    application_open_urls as extern "C" fn(_, _, _, _),
69-  );
70-  decl.add_method(
71-    sel!(application:willContinueUserActivityWithType:),
72-    application_will_continue_user_activity_with_type as extern "C" fn(_, _, _, _) -> _,
73-  );
74-  decl.add_method(
75-    sel!(application:continueUserActivity:restorationHandler:),
76-    application_continue_user_activity as extern "C" fn(_, _, _, _, _) -> _,
77-  );
78-  decl.add_method(
79-    sel!(applicationShouldHandleReopen:hasVisibleWindows:),
80-    application_should_handle_reopen as extern "C" fn(_, _, _, _) -> _,
81-  );
82-  decl.add_method(
--
127-  AppState::launched(this);
128-  trace!("Completed `applicationDidFinishLaunching`");
129-}
130-
131:extern "C" fn application_will_terminate(_: &Object, _: Sel, _: id) {
132:  trace!("Triggered `applicationWillTerminate`");
133-  AppState::exit();
134:  trace!("Completed `applicationWillTerminate`");
135-}
136-
137-extern "C" fn application_open_urls(_: &Object, _: Sel, _: id, urls: &NSArray<NSURL>) {
138-  trace!("Trigger `application:openURLs:`");
139-
140-  let urls = unsafe {
141-    (0..urls.count())
142-      .flat_map(|i| url::Url::parse(&urls.objectAtIndex(i).absoluteString().unwrap().to_string()))
143-      .collect::<Vec<_>>()
144-  };
145-  trace!("Get `application:openURLs:` URLs: {:?}", urls);
146-  AppState::open_urls(urls);
147-  trace!("Completed `application:openURLs:`");
148-}
149-
150-extern "C" fn application_will_continue_user_activity_with_type(
151-  _: &Object,
152-  _: Sel,
8:import { defaultEol } from "./settings";
90:    expect(defaultEol()).toBe("lf");
92:    expect(defaultEol()).toBe("crlf");
386:            // permissions the original never had.
387:            let _ = fs::set_permissions(temp, meta.permissions());
390:    fs::rename(temp, target).map_err(FsError::from)
395:    commit_by_rename(target, temp, existed)
399:/// a file that is not there yet is just renamed into place (spec §8).
403:        return commit_by_rename(target, temp, existed);
431:#[serde(rename_all = "camelCase")]
450:#[serde(rename_all = "camelCase")]
1025:    /// `rename` replaces the target on Windows too, so the behaviour this
1028:    fn a_rename_commit_replaces_the_target_and_keeps_its_mode() {
1035:        commit_by_rename(&target, &temp, false).unwrap();
1043:        commit_by_rename(&target, &temp, true).unwrap();

codex
Подтвердился ещё один функциональный пропуск: `defaultEol()` проверяется тестом и показывается в Settings, но реальные defaults по-прежнему задают CRLF. По исходникам зависимостей также подтверждены обход close guard через native Quit и отсутствие обработки JavaScript-печати в Wry; включу их в отзыв с конкретными местами исправления.

codex
Ревью `54129c7..f32704f`, без изменений файлов и запуска тестов. Native-поведение проверял по исходникам установленных Tauri/Wry/Muda/Tao; живого запуска на Mac не было.

**Критично**

1. **[src/app/close.ts:127](</G:/Claude Projects/md reader/src/app/close.ts:127>), [src-tauri/src/lib.rs:270](</G:/Claude Projects/md reader/src-tauri/src/lib.rs:270>) — `⌘Q` обходит защиту несохранённых правок.**  
   Guard обрабатывает только `CloseRequested`. Default menu вызывает native `terminate:`, а Tao 0.35.3 обрабатывает уже `applicationWillTerminate`; это не последовательное закрытие окон через frontend. Диалога `save / don't save / cancel` нет, последние правки могут не успеть попасть в debounce draft.  
   **Предложение:** заменить native Quit управляемым пунктом, направленным в общий сценарий сохранения и завершения. Отдельно проверить Dock → Quit. Одной подписки на `ExitRequested` для этого native-пути недостаточно. Здесь требование §13a оставить default menu конфликтует с §8.

2. **[src/app/paths.ts:22](</G:/Claude Projects/md reader/src/app/paths.ts:22>) — разные Unix-файлы получают один document ID.**  
   `pathKey()` безусловно заменяет `\` на `/` и вызывает `toLowerCase()`. На case-sensitive APFS `A.md` и `a.md` — разные файлы; имя с literal backslash тоже допустимо. `openPaths()` принимает совпадение ID за уже открытый документ. Коллизия затрагивает buffers, drafts и Save As. Аналогичные предположения остались в `read/links.ts` и `library/tree.ts`.  
   **Предложение:** ограничить Windows-нормализацию платформой; на Unix использовать возвращённый Rust canonical path без изменения регистра и разделителей. Проверить два разных файла одновременно, включая Save As и recovery.

3. **[src-tauri/src/fs.rs:384](</G:/Claude Projects/md reader/src-tauri/src/fs.rs:384>) — save уничтожает часть metadata и может изменить доступ к файлу.**  
   Новый inode получает владельца temp-файла; исходные owner/group, ACL, xattr, Finder tags и `com.apple.quarantine` не переносятся. Например, исчезновение ограничивающего ACL при сохранённом permissive mode расширяет доступ. Даже обязательный по §13a перенос mode выполняется best effort: ошибки `metadata` и `set_permissions` скрываются.  
   **Предложение:** определить сохраняемую metadata и переносить её до commit с обработкой ошибок; если обязательные права сохранить нельзя — останавливать замену. Тест `a_rename_commit_replaces_the_target_and_keeps_its_mode` сейчас вообще не проверяет mode.

4. **[src-tauri/src/fs.rs:390](</G:/Claude Projects/md reader/src-tauri/src/fs.rs:390>) — durability заканчивается до изменения имени.**  
   `sync_all()` вызывается до chmod и rename; после rename нет синхронизации каталога. Атомарная видимость замены не доказывает сохранение нового directory entry после power loss, включая APFS. Это относится также к drafts/state/history, использующим общий commit.  
   **Предложение:** перенести metadata перед последним file sync, добавить проверяемый macOS-протокол синхронизации directory metadata после rename. Ошибку после состоявшегося rename отличать от «файл не записан», иначе retry может получить конфликт с собственной записью. При этом не нужно механически добавлять `F_FULLFSYNC` вместо `sync_all`: Rust уже использует его на Apple. [Исходник Rust](https://raw.githubusercontent.com/rust-lang/rust/1.89.0/library/std/src/sys/fs/unix.rs), [семантика flush у Apple](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man2/fsync.2.html).

**Стоит поправить**

5. **[src/app/registry.ts:141](</G:/Claude Projects/md reader/src/app/registry.ts:141>), [registry.ts:285](</G:/Claude Projects/md reader/src/app/registry.ts:285>) — конфликты с native menu.**  
   `⌘W` принадлежит Close Window, поэтому вместо закрытия текущего документа запускается закрытие всего окна. `⌘H` принадлежит Hide, поэтому Replace скрывает приложение. Frontend `preventDefault()` не решает перехват native accelerator.  
   **Предложение:** согласовать native Close с `closeActive()` либо выбрать документу другой chord; Replace перенести, например, на `⌘⌥F`. Default menu также владеет `⌃⌘F`, но там действие совпадает. `⌘,` в прочитанном default menu отсутствует, поэтому аналогичного конфликта Settings нет. [Default menu Tauri](https://docs.rs/tauri/latest/src/tauri/menu/menu.rs.html).

6. **[src/app/registry.ts:296](</G:/Claude Projects/md reader/src/app/registry.ts:296>) — переключение документов превращено в переключение приложений.**  
   `Ctrl+Tab` и `Ctrl+Shift+Tab` парсятся как `⌘Tab` / `⌘⇧Tab`, которые забирает macOS.  
   **Предложение:** добавить в `macOverrides` буквальные `Control+Tab` и `Control+Shift+Tab`.

7. **[src/app/registry.ts:219](</G:/Claude Projects/md reader/src/app/registry.ts:219>) — клавиши zoom не зарегистрированы.**  
   Все три сочетания остались `hint`; `bindings()` их пропускает. Наличие `setZoom()` обеспечивает пункты меню, но не клавиши; WKWebView не предоставляет WebView2 accelerators.  
   **Предложение:** на macOS назначить `chord` для zoom in/out/reset. Проверять реальный zoom и восстановление из session.

8. **[src/app/settings.ts:34](</G:/Claude Projects/md reader/src/app/settings.ts:34>) — новые файлы на Mac всё ещё CRLF.**  
   `defaultEol()` нигде не участвует в формировании defaults. `loadSettings()` возвращает `DEFAULTS`, где `"crlf"`; `newDoc()` использует именно это значение. Settings при этом сообщает пользователю `lf here`. Тест проверяет лишь отдельную функцию.  
   **Предложение:** подключить platform default к defaults и parsing fallback, сохраняя явный пользовательский выбор. Проверить цепочку «нет settings.json → bootstrap → newDoc». Существующие документы и drafts должны сохранять собственный EOL.

9. **[src/app/commands.ts:592](</G:/Claude Projects/md reader/src/app/commands.ts:592>) — `window.print()` не подключён к native printing WKWebView.**  
   В Wry 0.55.1 есть отдельный native `print()`, но его UI delegate не реализует обработчики JavaScript print. Поэтому вызов может завершиться без диалога и без исключения; `catch` этого не обнаружит. Это вывод по исходникам, не результат запуска.  
   **Предложение:** использовать Tauri `Webview::print()` через Rust command и явно организовать восстановление режима, не полагаясь исключительно на DOM `afterprint`. [Путь обработки print в WebKit](https://raw.githubusercontent.com/WebKit/WebKit/main/Source/WebKit/UIProcess/Cocoa/UIDelegate.mm).

10. **[src/ui/QuickSearch.tsx:156](</G:/Claude Projects/md reader/src/ui/QuickSearch.tsx:156>) — `⌘↩` не открывает результат в edit.**  
    Осталась проверка `event.ctrlKey`; строка 208 показывает literal `ctrl ↵`.  
    **Предложение:** проверять primary modifier общей функцией, отображать через `chordText("Ctrl+Enter")`, использовать `event.code`. В ReadView уже проверяются оба modifier; paste изображений работает через `paste`, поэтому отдельная замена `Ctrl+V` там не требуется.

11. **[src/app/registry.ts:343](</G:/Claude Projects/md reader/src/app/registry.ts:343>), [README.md:64](</G:/Claude Projects/md reader/README.md:64>) — history shortcuts расходятся с нормой и документацией.**  
    Реализованы `⌘⌥←/→`, тогда как §13a и README обещают `⌘[/]`. Это сознательное изменение из комментария к коду, но формально несоответствие осталось.  
    **Предложение:** согласовать выбор с ТЗ и README; если возвращать brackets, одновременно разрешить конфликт с editor indent, иначе одно сочетание будет иметь двух владельцев.

12. **[src-tauri/src/fs.rs:267](</G:/Claude Projects/md reader/src-tauri/src/fs.rs:267>) — `read_only` не отражает effective access.**  
    Уточнение к вопросу: Rust проверяет **все три write bits**, не только owner-write. ACL, текущего владельца/group membership и read-only mount он не учитывает. Возможны как доступный editor с ошибкой save, так и запрет редактирования файла, доступного через ACL.  
    **Предложение:** сохранить mode-based значение как явно ограниченный признак либо добавить effective-access проверку для UX. Окончательное решение всё равно принимать по результатам операций; atomic replace зависит также от прав каталога. [Документация Rust](https://doc.rust-lang.org/stable/std/fs/struct.Permissions.html#method.readonly).

13. **[src-tauri/src/fs.rs:390](</G:/Claude Projects/md reader/src-tauri/src/fs.rs:390>) — hard links молча расходятся.**  
    Rename заменяет только одно имя новым inode; остальные hard links продолжают показывать старый текст. `canonicalize()` такие имена не объединяет.  
    **Предложение:** при `nlink > 1` явно предупреждать или предлагать Save As. Сохранить одновременно atomic replacement и обновление всех hard links этим алгоритмом нельзя; переходить на in-place запись вопреки §8 не следует.

14. **[README.md:62](</G:/Claude Projects/md reader/README.md:62>) — инструкция первого запуска устарела для Sequoia.**  
    Начиная с macOS 15 Control/right-click → Open больше не обходит Gatekeeper для такого приложения. Apple прямо указывает System Settings → Privacy & Security. `xattr -cr` дополнительно удаляет все extended attributes, а не только quarantine.  
    **Предложение:** основной путь — попытка запуска → Privacy & Security → Open Anyway. Если оставлять Terminal-вариант, использовать адресное `xattr -dr com.apple.quarantine /Applications/Plain.app`. Уточнить «ad-hoc signed, без Developer ID/notarization». Исправление требуется и в §13a. [Объявление Apple для Sequoia](https://developer.apple.com/news/?id=saqachfa).

15. **[.github/workflows/macos.yml:17](</G:/Claude Projects/md reader/.github/workflows/macos.yml:17>) — write token выдан всему build job, включая dispatch.**  
    `contents: write` распространяется и на `npm ci`, тесты, сборку; checkout по умолчанию сохраняет credentials. Для artifact-only запуска это лишнее.  
    **Предложение:** build/test с `contents: read` и `persist-credentials: false`; публикация — отдельный tag-only job с write permission. Дополнительно: dispatch, запущенный на теге, тоже удовлетворяет `startsWith(github.ref, 'refs/tags/')` и публикует release, вопреки комментарию «manual run only builds». Если это нежелательно, проверять также `github.event_name`.

По остальным спорным пунктам оснований для отдельных дефектов не нашёл: `to_file_path()` декодирует percent-encoding/кириллицу; paths/folders разделены, очередь переживает отсутствие listener; single-instance 2.4.4 имеет macOS-реализацию. `ext/name/role` корректны, `mimeType` не обязателен; `beside_exe = None` и сигнатуры assoc согласованы. `MacIntel` подходит текущему detector; Highlight fallback допускает macOS 13; idle централизован. `⌥↑/↓` и `⌥⇧↑/↓` остаются в CodeMirror keymap. Spellcheck/context menu требуют живой проверки словарей и suggestions.

Пустой `tagName` при отсутствии `releaseId` действительно не создаёт release — это подтверждается [исходником tauri-action](https://raw.githubusercontent.com/tauri-apps/tauri-action/v0/src/index.ts). Путь artifact и workspace cache соответствуют universal target. Другой том сам по себе не создаёт EXDEV, поскольку temp находится рядом с target; rename файла поверх каталога завершится ошибкой, однако текущий document-save оставит staged temp без cleanup и без сообщения о его пути.

Вкусовщину отдельно не включал.
tokens used
89,080
