Ты ревьюишь незакоммиченный диф (`git diff` + новые файлы из `git status`) проекта Plain — личного Markdown reader/editor (Tauri 2 + React 19 + TS + CodeMirror 6 + Rust): партия v0.2.1. Ничего не правь — только читай и пиши отзыв.

Норма — `ТЗ-Plain.md` v2.6: §5.2 «Команды» (добавленный абзац: strike, highlight, hr, table, image, footnote, math inline/block, callout×5, wikilink, три вида списков, хоткеи `Alt+Shift+…`), §5.2 «Панель форматирования» (`edit.toolbar`), §2a «Теги» (`library.tags`, секция только при наличии тегов). В дифе: `src/editor/commands.ts` (новые команды, `toggleFence`, `listCommand`, `nextFootnote`), `keymap.ts`, `src/editor/active.ts` (`activeFormats` по syntaxTree), `src/ui/FormatBar.tsx`/`formatbar.css`, `DocView.tsx`, `src/editor/images.ts` (`insertImageFromFile` через dialog `open`), `src/editor/sync.ts` (`setLiveEditor/liveEditor`), `registry.ts`/`menu.ts` (`edit.*`, `insert ▸`, `callout ▸`), `settings.ts`/`Settings.tsx` (`edit.toolbar`, `library.tags`), `src/library/tags.ts` (`showsTags`), `Rail.tsx`, `ReadView.tsx` (гейт тегов).

Ищем, по приоритету:
1. Корректность текстовых команд (§1.3 — байты, которых не касались, не меняются): toggle-семантика (повтор снимает ровно то, что вставил), мультивыделение, границы строк/документа (первая/последняя строка, пустой документ, документ без финального `\n`), CRLF (в буфере LF — ок), `toggleFence` для `$$` vs ```` ``` ````, `insertFootnote` нумерация при `[^1]` внутри code, `toggleCallout` на строках уже цитаты/другого callout, `insertTable` внутри списка/цитаты, `insertRule` рядом с текстом (нужны пустые строки), `toggleHighlight` внутри code/ссылки, read-only guard для всех новых команд.
2. Хоткеи: `Alt+Shift+…` — конфликты с CM6 `Alt+Shift+↑/↓` (нет), с системными Windows (`Alt+Shift` переключает раскладку! — `Alt+Shift+S` в русской раскладке: `code` матчинг спасает, но само нажатие Alt+Shift меняет раскладку у пользователя с двумя языками — это реальная проблема: оцени и предложи альтернативу, например `Ctrl+Alt+…`), `Ctrl+Alt+Shift+M`; `hint` vs `chord` в реестре — не вызываются ли дважды (FormatBar → команда; меню → `runEditorCommand`; hotkey → code-слой).
3. FormatBar: `mousedown preventDefault` — не ломает ли клик по кнопкам для клавиатуры (Enter/Space), `activeFormats` на 1 МБ (syntaxTree resolve на каждое selectionSet с debounce 100 мс — ок?), `useSyncExternalStore` подписка/утечка при размене документов, скрытие в read и при `edit.toolbar=false`, split (над левой панелью), `title` через `chordText` на mac.
4. `insertImageFromFile`: путь без библиотеки, коллизии имён, `open` dialog permissions, документ без пути.
5. Теги: `showsTags`, отключение сбора при `library.tags=false`, подписка на изменение настройки (утечка?), `ReadView` гейт.
6. Оверинжиниринг («окупится ли за месяц одним человеком»).

Формат: находки файл:строка, суть, почему важно, предложение; «критично / стоит поправить / вкусовщина». Без пересказа и похвал. По-русски, термины по-английски.
